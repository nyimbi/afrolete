# AfroLete Frontend

TypeScript frontend for AfroLete V2.

The frontend is an operational SaaS interface for sport organizations. It should
stay thin: business rules, persistence, authorization, and integration logic
belong in the FastAPI backend.

## Development

```bash
cd frontend
pnpm install
pnpm dev
pnpm typecheck
```

