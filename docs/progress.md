# AfroLete Build Progress

This log tracks concrete movement toward the full AfroLete objective: implement
the complete product scope described in `docs/prd.md` and `docs/ext*.md` as an
ergonomic, functional, capable SaaS where AI agents are first-class ecosystem
members.

## Current Goal

Fully implement AfroLete as a production-grade sports operations and
athlete-development platform:

- FastAPI backend and TypeScript frontend.
- PJS shared infrastructure: `db.lindela.io`, Keycloak, SpiceDB, OpenBao,
  Redis, Temporal, MinIO.
- Complete coverage of core PRD and extension features over time.
- Beautiful, novel, operationally efficient UI/UX.
- AI agents as first-class actors with identity, permissions, tasks, and audit.
- Regular commits and pushes.

## Progress Log

### 2026-05-27

- Initialized fresh V2 repository from retained product docs.
- Added project charter in `README.md`.
- Added V2 directory scaffold for backend, frontend, infra, docs, scripts,
  tools, and tests.
- Started implementation slice 001: executable SaaS foundation.
- Implemented slice 001 foundation:
  - FastAPI backend with health/platform routes.
  - SQLAlchemy domain model foundation for identity, organizations, teams,
    athletes, guardians, events, attendance, AI agents, agent assignments, and
    agent tasks.
  - Backend test setup with passing platform route tests.
  - Next/TypeScript frontend shell with a polished SaaS command-center first
    screen.
  - Frontend dependency policy updated to exact Next.js `16.2.6` after the
    reported Next.js/React security release concerns; pnpm build-script policy
    explicitly denies `sharp` install scripts by default.
  - Initial PJS infra artifacts for Postgres, Keycloak, SpiceDB, OpenBao,
    MinIO, Temporal, and systemd.
  - Verification: `uv run ruff check .`, `uv run pytest`,
    `pnpm --filter @afrolete/frontend typecheck`.
- Implemented slice 002 tenant graph foundation:
  - Membership model changed from person-only to polymorphic membership.
  - Associations can have member associations, organizations, teams, and
    individuals.
  - Clubs and schools are organizations that can own teams and can themselves be
    members of associations or other organizations.
  - Association levels added: national, regional, local, and special.
  - Committees added for associations and organizations, with person committee
    memberships so one member can serve across multiple committees at different
    levels.
  - Teams broadened for both team sports and individual sports, with captains,
    vice captains, starters, bench, substitutes, reserves, individual athletes,
    support roles, and team committees.
  - Added local identity bridge and in-memory authorization service boundaries
    that can be replaced by Keycloak and SpiceDB adapters.
  - Added organization, membership, committee, team, roster, and team committee
    API routes.
  - Verification: `uv run ruff check .`, `uv run pytest`,
    `pnpm --filter @afrolete/frontend typecheck`.
- Implemented slice 003 database migration baseline:
  - Added Alembic migration template and initial schema revision for the
    current identity, organization, committee, team, roster, event, attendance,
    and AI-agent tables.
  - Normalized SQLAlchemy enum persistence to use public lowercase API values
    before the baseline migration.
  - Added local SQLite database artifacts to `.gitignore`.
  - Verification: `uv run alembic upgrade head`,
    `uv run alembic downgrade base`, `uv run ruff check .`, `uv run pytest`.
- Implemented slice 004 safeguarding, consent capture, and tenant branding:
  - Added organization contact, public name, subdomain, logo, and brand color
    fields for club/school/association managed sites.
  - Added guardian relationship details for parent, legal guardian, caregiver,
    emergency contact, pickup, medical visibility, and consent authority.
  - Added consent requests and activity consents for organization, team, and
    event scopes.
  - Consent can be captured by one-use web links, SMS, WhatsApp, Telegram,
    email, or manual administrative recording.
  - Added event participation clearance for minors, including guardian and
    consent-required states.
  - Switched the backend default local database to
    `postgresql+asyncpg:///afrolete`.
  - Verification: local PostgreSQL `alembic upgrade head`, PostgreSQL
    `alembic downgrade 537570abceee`, PostgreSQL `alembic upgrade head`,
    `uv run ruff check .`, `uv run pytest`.
- Implemented slice 005 event scheduling and consent-aware attendance:
  - Added event APIs for creating, listing, and reading organization/team
    events.
  - Added attendance APIs for seeding invitations from a team roster, recording
    RSVP/check-in status, and listing event attendance.
  - Attendance check-in now reuses safeguarding clearance so minors cannot be
    marked confirmed or present without a valid guardian consent.
  - SpiceDB event participant and guardian relations now support `person`
    subjects, not only application users.
  - Verification: `uv run ruff check .`, `uv run pytest`.
- Implemented slice 006 Keycloak bearer-token authentication:
  - Added `AFROLETE_AUTH_MODE=keycloak` so deployed APIs require bearer tokens
    instead of local identity headers.
  - Added Keycloak JWKS retrieval, signing-key selection by token `kid`, fixed
    algorithm allow-listing, issuer validation, audience validation, and
    required `exp`/`iat`/`sub` claims.
  - User provisioning now remains behind the existing identity bridge, fed by
    verified token claims instead of test headers.
  - Local development keeps `AFROLETE_AUTH_MODE=local` for trusted local tools
    and unit tests.
  - Verification: `uv run ruff check .`, `uv run pytest`.
- Implemented slice 007 SpiceDB authorization adapter:
  - Added `AFROLETE_AUTHZ_MODE=spicedb` so deployed APIs use the shared PJS
    SpiceDB service instead of the in-memory local authorization set.
  - Relationship writes now map AfroLete relationships to SpiceDB `TOUCH`
    updates for idempotent role and membership synchronization.
  - Permission checks now map resource, permission, and internal app user
    subject IDs to SpiceDB `CheckPermission` calls.
  - SpiceDB check failures fail closed; write failures surface instead of
    silently dropping authorization relationships.
  - Verification: `uv run ruff check .`, focused SpiceDB unit tests.
- Implemented slice 008 operational SaaS console:
  - Replaced the static first screen with a real Next.js operations console for
    local-mode organization creation, team creation, athlete intake, roster
    assignment, event scheduling, roster-based attendance seeding, attendance
    recording, guardian linking, consent request creation, and event clearance
    checks.
  - Added a frontend API client that sends local identity headers in development
    and surfaces backend validation errors in the activity feed.
  - Added a public one-use guardian consent page at `/consent/[token]` that
    captures grant/deny responses through the backend token endpoint.
  - Fixed default backend CORS to allow the actual Next development origin at
    `http://127.0.0.1:3000`.
  - Verification: `pnpm --filter @afrolete/frontend typecheck`,
    `pnpm --filter @afrolete/frontend build`, production Playwright screenshots
    at desktop and mobile sizes.
- Implemented slice 009 first-class AI agent operations:
  - Added agent APIs for organization-scoped agent creation, assignment to
    organization/team/event/athlete-profile scopes, task queueing, task listing,
    and human review/status updates.
  - Agent creation now writes the agent owner relationship; assignment writes
    `assigned_agent` relationships to SpiceDB-compatible resources.
  - Added cross-organization scope validation so agents cannot be assigned to
    resources outside their tenant.
  - Added the agent identity and task inbox lane to the operational console,
    including agent creation, assignment buttons, task queueing, and review
    status transitions.
  - Verification: `uv run ruff check .`, `uv run pytest`,
    `uv run alembic upgrade head`, `pnpm --filter @afrolete/frontend
    typecheck`, `pnpm --filter @afrolete/frontend build`.
- Implemented slice 010 athlete performance metrics and assessments:
  - Added performance metric definitions with sport, category, units, value
    ranges, weights, and directionality.
  - Added athlete performance observations with event context, source,
    confidence, verification status, raw value, notes, and recorder provenance.
  - Added athlete assessments with physical, technical, tactical, mental, and
    computed AfroLete Score components using the PRD weighting model.
  - Added summary APIs for latest score, rating, observation count, and
    assessment count.
  - Added the performance lane to the console for metric creation, observation
    recording, assessment recording, and score review.
  - Verification: `uv run ruff check .`, `uv run pytest`, PostgreSQL
    `alembic upgrade head`, PostgreSQL `alembic downgrade 6138f95a6b16`,
    PostgreSQL `alembic upgrade head`, `pnpm --filter @afrolete/frontend
    typecheck`, `pnpm --filter @afrolete/frontend build`.
- Implemented slice 011 training and coaching plans:
  - Added a training drill library with sport, focus area, category, equipment,
    age suitability, duration, intensity, description, and coaching points.
  - Added training plans that can be scoped to an organization, team, and/or
    athlete profile, with AI-generated provenance, source summary, load
    guidance, recovery protocol, and progress checkpoints.
  - Added weekly training plan items/blocks linked to drills and carrying
    sequence, day label, focus area, duration, intensity, and coaching notes.
  - Added session planning with team/event/plan scope validation and target
    training load computed as `duration_minutes * rpe_target`.
  - Added the Training console lane for drill creation, plan creation, weekly
    block creation, and load-aware session planning.
  - Verification: `uv run ruff check .`, `uv run pytest`, PostgreSQL
    `alembic upgrade head`, PostgreSQL `alembic downgrade dea9ca28416e`,
    PostgreSQL `alembic upgrade head`, `pnpm --filter @afrolete/frontend
    typecheck`, `pnpm --filter @afrolete/frontend build`, production
    Playwright screenshot at `/private/tmp/afrolete-training-console.png`.
- Implemented slice 012 competition management foundation:
  - Added competition records for leagues, tournaments, cups, and friendly
    series with sport, format, season dates, points rules, tiebreakers, status,
    and rules summary.
  - Added registered competition participants with team, seed, group, and
    tenant-scoped validation.
  - Added fixtures with home/away teams, schedule, stage/round, venue, linked
    event support, result confirmation, and final score recording.
  - Added official assignments with role, status, certification level, and
    conflict notes, restricted to organization members.
  - Added match event logging for goals, cards, substitutions, injuries, and
    notes.
  - Added computed standings with played/win/draw/loss, goals for/against,
    goal difference, and points using the competition scoring rules.
  - Added the Competition console lane for competition creation, team
    registration, fixture creation, result confirmation, official assignment,
    match event logging, readiness checks, and standings review.
  - Verification: `uv run ruff check .`, `uv run pytest`, PostgreSQL
    `alembic upgrade head`, PostgreSQL `alembic downgrade b23079d70437`,
    PostgreSQL `alembic upgrade head`, `pnpm --filter @afrolete/frontend
    typecheck`, `pnpm --filter @afrolete/frontend build`, production
    Playwright screenshot at `/private/tmp/afrolete-competition-console.png`.
- Implemented slice 013 communications and notifications:
  - Added reusable communication templates with message type, channel, subject,
    body, variables, locale, and active state.
  - Added scoped communication messages for organization, team, event, and
    person targets, with tenant-safe recipient expansion and urgent/quiet-hours
    controls.
  - Added recipient delivery records with channel destination, queued,
    suppressed, delivered, read, and failed states plus delivery/read
    timestamps.
  - Added notification preferences for frequency, preferred channel, language,
    quiet hours, and emergency override.
  - Added minor-safe guardian copying so direct person messages to minors can
    include guardians with consent authority.
  - Added the Communications console lane for template creation, scoped
    broadcasts, recipient review, read-receipt updates, and preference
    management.
  - Verification: `uv run ruff check .`, `uv run pytest`, PostgreSQL
    `alembic upgrade head`, PostgreSQL `alembic downgrade 5ae823a88e0f`,
    PostgreSQL `alembic upgrade head`, `pnpm --filter @afrolete/frontend
    typecheck`, `pnpm --filter @afrolete/frontend build`, production
    Playwright screenshot at
    `/private/tmp/afrolete-communications-console-v2.png`.
- Implemented slice 014 equipment, facilities, and asset operations:
  - Added facility profiles for fields, courts, stadiums, gyms, pools,
    clubhouses, storage, capacity, surface, amenities, insurance, maintenance
    budgets, conditions, and inspection dates.
  - Added equipment inventory with category/subcategory, brand/model, QR/RFID
    tag codes, serial numbers, total and available quantity, condition,
    storage location, reorder thresholds, value, depreciation, warranty, audit,
    and photo metadata.
  - Added equipment checkout and return workflows that decrement and restore
    availability, capture borrower/team/event context, due dates, conditions,
    damage reports, and late fees.
  - Added facility bookings with overlap rejection, requester details, event and
    team linkage, attendee counts, rate/deposit, insurance certificates,
    requirements, and access codes.
  - Added maintenance work orders with facility/equipment linkage, assignment,
    priority, due dates, vendor, estimated/actual cost, safety flag, compliance
    reference, and completion tracking.
  - Added an asset summary endpoint for facilities, inventory counts, stock
    alerts, open/overdue checkouts, open/safety work orders, upcoming bookings,
    booked hours, and projected revenue.
  - Added the Assets console lane for facility creation, booking, inventory
    creation, checkout/return, maintenance work orders, and readiness checks.
  - Verification: `uv run ruff check .`, `uv run pytest`, PostgreSQL
    `alembic upgrade head`, PostgreSQL `alembic downgrade 001fdd0cead7`,
    PostgreSQL `alembic upgrade head`, `pnpm --filter @afrolete/frontend
    typecheck`, `pnpm --filter @afrolete/frontend build`, production
    Playwright screenshot at
    `/private/tmp/afrolete-assets-console-viewport.png`.
- Implemented slice 015 commercial operations fast surface:
  - Added sponsors with industry, contacts, websites, brand assets, and notes.
  - Added sponsorship agreements with event linkage, tiers, value, currency,
    dates, deliverables, activation notes, ROI notes, and lifecycle status.
  - Added fundraising campaigns and donations with goals, raised totals,
    donor records, external references, messages, and automatic campaign
    completion when the goal is met.
  - Added ticket products, paid ticket orders, issued QR-token tickets, and
    ticket check-in with gate and timestamp.
  - Added finance invoices and payments with partial/paid status tracking.
  - Added a commercial summary endpoint for sponsorship value, fundraising
    goals/raised totals, ticket revenue, invoice outstanding, sponsors,
    campaigns, tickets sold, and tickets checked in.
  - Added the Commerce console lane for sponsorship activation, fundraising
    donation capture, ticket sale/check-in, invoice/payment capture, and
    commercial summary review.
  - Verification: `uv run ruff check .`, PostgreSQL `alembic upgrade head`,
    `pnpm --filter @afrolete/frontend typecheck`. Full test/build/screenshot
    verification deferred by user instruction during low-battery fast delivery.
- Implemented slice 016 reporting and intelligence fast surface:
  - Added report definitions across performance, administrative, operational,
    financial, compliance, and intelligence categories.
  - Added generated report runs with team, athlete, event, and competition
    scope, period parameters, online/PDF/Excel/CSV/API output targets, summary,
    findings, recommendations, shared tokens, and expiry metadata.
  - Added scheduled report delivery with daily/weekly/monthly/quarterly/on
    trigger frequency, delivery channels, recipients, next-run, and last-run
    tracking.
  - Added intelligence insights with AI-agent linkage, athlete/team/event scope,
    severity, confidence, evidence, recommendations, model name, review status,
    and human reviewer tracking.
  - Added predictive risk scores with model name, score, risk band, drivers,
    recommendations, and validity date.
  - Added report export jobs for PDF/Excel/CSV/API destinations and webhook
    delivery status.
  - Added a reporting summary endpoint and Reports console lane for generating
    reports, scheduling delivery, exporting artifacts, creating insights,
    recording risk scores, and actioning insights.
  - Verification: `uv run ruff check .`, PostgreSQL `alembic upgrade head`,
    `pnpm --filter @afrolete/frontend typecheck`. Full test/build/screenshot
    verification deferred by user instruction during low-battery fast delivery.
- Implemented slice 017 SaaS billing and subscription fast surface:
  - Added SaaS billing plans with code, tier name, pricing, billing cycle,
    included athletes, teams, AI-agent tasks, storage, per-athlete pricing,
    per-agent-task pricing, feature text, and status.
  - Added tenant subscriptions with organization linkage, plan, billing cycle,
    active/trial/past-due/cancelled states, period dates, trial and next billing
    dates, purchased seats, negotiated pricing, discount codes, external
    customer/subscription IDs, and cancellation flags.
  - Added usage meters and usage records for athletes, teams, agent tasks,
    reports, storage, and messages with included quantities, overage pricing,
    aggregation, sources, and external references.
  - Added SaaS invoices and payments with line-item summaries, subtotal, tax,
    discount, total, amount paid, status, due dates, external IDs, provider, and
    collection metadata.
  - Added billing entitlements for feature limits, usage, reset dates, and
    status.
  - Added a billing summary endpoint for active subscriptions, plans, usage
    meters, usage records, open invoices, monthly recurring revenue,
    outstanding invoice value, and entitlements.
  - Added the Billing console lane for plan/subscription creation, usage
    metering, invoice/payment capture, and entitlement assignment.
  - Verification: `uv run ruff check .`, PostgreSQL `alembic upgrade head`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`. Full
    test/build/screenshot verification deferred by user instruction during
    low-battery fast delivery.
- Implemented slice 018 frontend Keycloak session handling:
  - Added frontend runtime configuration for `NEXT_PUBLIC_AFROLETE_AUTH_MODE`,
    Keycloak issuer, web client ID, and OIDC scopes.
  - Added a no-new-dependency browser OIDC authorization-code + PKCE flow for
    the `afrolete-web` public client, including state verification, token
    exchange, stored session expiry, claim extraction, and logout URL support.
  - API calls now automatically attach a stored Keycloak bearer token while
    preserving local-mode AfroLete identity headers for development.
  - The operations console now exposes Keycloak sign-in/sign-out controls,
    displays the active browser session, derives operator identity from token
    claims, and waits for a session before synchronizing protected data in
    Keycloak mode.
  - Verification: `pnpm --filter @afrolete/frontend typecheck`,
    `git diff --check`. Live Keycloak realm/browser redirect, full build, and
    screenshot verification deferred by user instruction during low-battery
    fast delivery.
- Implemented slice 019 communication delivery adapter fast surface:
  - Added backend configuration for record-only vs webhook communication
    delivery, channel-specific webhook URLs, a shared delivery webhook key, and
    delivery timeout controls.
  - Added a dispatch service and API endpoint that sends email, SMS, WhatsApp,
    Telegram, and push payloads to configured HTTP adapters while preserving
    queued status when no transport is configured.
  - In-app messages are marked delivered immediately, suppressed recipients
    remain suppressed, and failed webhook responses are captured on recipient
    failure reasons.
  - Added a provider callback endpoint for delivery/read/failure events guarded
    by `AFROLETE_COMMUNICATION_WEBHOOK_KEY` outside local mode.
  - Added a console dispatch action that refreshes recipient delivery state and
    surfaces sent, failed, and queued counts.
  - Verification: `uv run ruff check .`,
    `uv run pytest tests/unit/test_communications.py -q` (4/4),
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`. Full
    suite/build/screenshot verification deferred by user instruction during
    low-battery fast delivery.
- Implemented slice 020 agent task execution fast surface:
  - Added backend configuration for deterministic local agent execution,
    optional provider-neutral webhook execution, webhook keys, default model
    policy, and execution timeouts.
  - Added an agent task execution endpoint that moves queued tasks through a
    real run boundary, generates deterministic review-ready output by default,
    or posts provider-neutral payloads to an external agent worker when
    configured.
  - Webhook execution captures output references, review notes, returned
    statuses, HTTP failures, and transport errors on the task record.
  - The console Run control now invokes the execution endpoint and displays
    output references and review notes instead of only manually setting a task
    to running.
  - Verification: `uv run ruff check .`,
    `uv run pytest tests/unit/test_agents.py -q` (2/2),
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`. Full
    suite/build/screenshot and live provider execution deferred by user
    instruction during low-battery fast delivery.
- Implemented slice 021 communication inbox, digests, and AI drafting:
  - Added a person-scoped communications inbox API for athletes, guardians,
    parents, and members, with self-access for the recipient and manager access
    for organization operators.
  - Added digest generation that collects unread inbox items, chooses an
    in-app/email/SMS channel from notification preferences, creates a digest
    message, and records a recipient delivery state.
  - Added deterministic AI-assisted message drafting for selected organization,
    team, event, or person scopes, including tone, audience, guardian context,
    model attribution, and mandatory human review.
  - Extended the operations console with Draft, Digest, inbox review, digest
    summary, and read-action controls so communication work is not API-only.
  - Verification: `uv run ruff check .`,
    `uv run pytest tests/unit/test_communications.py -q` (5/5),
    `uv run pytest` (44/44), `pnpm --filter @afrolete/frontend typecheck`,
    `pnpm --filter @afrolete/frontend build`, `git diff --check`.
- Implemented slice 022 asset procurement, scan, supplier, lease, and
  utilization intelligence:
  - Added RFID/barcode-style equipment scan lookup by tag code or serial number
    with organization scoping and manager authorization.
  - Added equipment photo metadata updates for audit/photo capture workflows.
  - Added procurement recommendations from reorder points, minimum stock,
    available quantity, unit value, and supplier hints.
  - Added supplier scorecards from maintenance vendor work orders, completion
    rates, safety work, estimated cost, and actual cost variance.
  - Added lease quote estimates for equipment planning using replacement value,
    depreciation, quantity, and term.
  - Added asset utilization recommendations for low stock, unused stock,
    overdue checkouts, and open safety work orders.
  - Extended the operations console with Scan, Photo, Lease, procurement,
    supplier, and utilization controls.
  - Verification: `uv run ruff check .`,
    `uv run pytest tests/unit/test_assets.py -q` (4/4),
    `uv run pytest` (45/45), `pnpm --filter @afrolete/frontend typecheck`,
    `pnpm --filter @afrolete/frontend build`, `git diff --check`.
- Implemented slice 023 commercial finance controls:
  - Added provider-neutral tax quote estimates for invoice, ticketing, donation,
    and checkout review, including jurisdiction, tax rate, reverse-charge
    support, tax amount, and total.
  - Added payment settlement summaries across ticket revenue, invoice payments,
    and donations with provider name, fee rate, fixed fee, gross amount, fees,
    net payout, payout reference, and line count.
  - Added ticket refunds that mutate ticket status, decrement sold inventory,
    and adjust order status for partial/full refunds.
  - Added invoice refunds/credit notes that reduce amount paid and recalculate
    invoice status.
  - Added accounting export rows for invoice payments and donations with debit,
    credit, account codes, external references, basis, and target system.
  - Added sponsorship dashboard metrics for agreement count, active/contracted
    value, deliverables, activations, ROI score, and renewal recommendations.
  - Extended the operations console with Tax, Settle, Export, Refund ticket,
    Refund invoice, and sponsorship ROI cards.
  - Verification: `uv run ruff check .`,
    `uv run pytest tests/unit/test_commercial.py -q` (1/1),
    `uv run pytest` (46/46), `pnpm --filter @afrolete/frontend typecheck`,
    `pnpm --filter @afrolete/frontend build`, `git diff --check`.
- Implemented slice 024 reporting output intelligence:
  - Added report artifact rendering endpoints with selected output format,
    content type, artifact reference, checksum, body preview, and PDF/Excel
    page/sheet metadata.
  - Added report verification scoring for readiness, narrative completeness,
    share expiry, period validity, and rendered artifact presence.
  - Added chart-ready reporting summaries for throughput, insight severity,
    and predictive risk bands.
  - Added benchmark model summaries from predictive risk scores, including
    sample size, average score, high-risk counts, banding, and recommended
    follow-up.
  - Added deterministic AI-generated reporting review insights from the latest
    report/export/open-insight/risk signals.
  - Extended the operations console with Render, Verify, AI Review, chart cards,
    artifact cards, verification cards, and benchmark cards.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend production
    build, Playwright screenshots, and real binary PDF/Excel file generation.
- Implemented slice 025 SaaS billing operations:
  - Added localized SaaS tax quote estimates with jurisdiction rates,
    reverse-charge support, totals, and filing hints.
  - Added subscription plan-change proration quotes with remaining-period
    credits, new charges, net amount, and recommendations.
  - Added dunning notice preparation for overdue SaaS invoices with severity,
    channel, amount due, message, and next action.
  - Added provider-neutral payment webhook intake for succeeded invoice payment
    events, including invoice status mutation and payment records.
  - Extended the operations console with Tax, Prorate, Dunning, and Webhook
    controls plus result cards for tax, proration, dunning, and webhook intake.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend production
    build, Playwright screenshots, real Stripe/webhook signature validation,
    external dunning delivery, and jurisdiction-specific filing integrations.
- Implemented slice 026 competition automation:
  - Added deterministic round-robin fixture generation from registered
    competition participants, with configurable start time, interval, match
    spacing, venue, stage label, and double round-robin support.
  - Added bracket projections from existing fixtures or seeded participants,
    including round/stage grouping, slots, fixture links, status, and winner
    names where results exist.
  - Added schedule conflict detection for duplicate matchups, missing
    officials, team rest-window conflicts, and venue overlaps with severity and
    recommended actions.
  - Extended the operations console with Auto fixtures and Conflicts controls,
    generated-fixture summary cards, bracket cards, and conflict cards.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend production
    build, Playwright screenshots, and complex tournament bracket advancement.
- Implemented slice 027 AI training plan generation:
  - Added an AI-assisted training plan generation endpoint that creates a plan
    and session blocks from organization/team/athlete scope, readiness score,
    period, weekly session target, drill library, recent assessments,
    observations, and upcoming competition fixtures.
  - Added deterministic focus inference from readiness and lowest assessment
    dimension, load guidance, recovery protocol, competition tapering, and
    readiness-aware intensity progression.
  - Added generated plan response metadata with rationale, load balance,
    readiness score, next competition date, plan, and generated items.
  - Extended the operations console with AI Plan controls, readiness/session
    inputs, and generated-plan rationale cards.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend production
    build, Playwright screenshots, live model generation, availability
    calendars, and post-session feedback loops.
- Implemented slice 028 performance ingestion and review:
  - Added provider-neutral performance evidence ingestion for video analysis,
    audio narration, text/manual evidence, wearable feeds, official stats, and
    agent-extracted observations.
  - Added deterministic metric value extraction, source-specific confidence,
    extractor naming, evidence references, pending-review observations, and
    ingestion summaries without adding new storage tables.
  - Added structured provider payload parsing and metric-specific narrative
    parsing metadata in slice 241.
  - Added human review workflow for ingested observations, including value
    correction, verification status promotion/rejection, and review notes.
  - Extended the operations console with evidence reference/text fields,
    Ingest and Review controls, ingestion summary cards, and selectable
    observation review cards.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend production
    build, Playwright screenshots, richer provider parsing, and model-backed
    extraction accuracy.
- Implemented slice 029 AI agent governance telemetry:
  - Added derived agent run records that join tasks to agent identity, kind,
    model policy, status, input/output refs, review requirement, and governance
    notes.
  - Added agent governance summary metrics for queued, running, waiting for
    review, completed, failed, cancelled, and human-review-required tasks.
  - Added credential-boundary status from runtime settings, including execution
    mode, default model, webhook URL/key readiness, local/OpenBao boundary, and
    production hardening recommendations.
  - Extended the operations console with a Telemetry control, governance metric
    cards, credential-boundary cards, and recent run ledger cards.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend production
    build, Playwright screenshots, live model providers, OpenBao secret fetch,
    persisted run-history tables, and replay/audit immutability.
- Implemented slice 030 binary report artifacts:
  - Added authenticated report artifact download responses for PDF, Excel
    workbook, CSV, JSON/API, and HTML output formats.
  - Added dependency-free PDF byte generation, valid minimal XLSX workbook
    generation, CSV generation, HTML rendering, content type selection,
    checksums, and stable filenames.
  - Updated render metadata to checksum and size the actual generated artifact
    bytes rather than only the preview text.
  - Extended the operations console with a Download control that fetches the
    selected report artifact, applies local/Keycloak auth headers, saves the
    returned file, and refreshes reporting state.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend production
    build, Playwright screenshots, MinIO/object storage persistence, and
    visual report layout QA.
- Implemented slice 031 report artifact storage boundary:
  - Added configurable local report artifact storage settings for artifact
    directory and URL prefix.
  - Persisted rendered/downloaded report artifact bytes under the ignored
    local artifact directory using organization, report, checksum, and stable
    filename pathing.
  - Returned stored artifact URLs through report metadata and download response
    headers so the API shape can map to MinIO/S3-compatible storage later.
  - Added artifact storage environment examples for local development and
    deployment configuration.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend production
    build, Playwright screenshots, MinIO client persistence, object lifecycle
    rules, and signed object URLs.
- Implemented slice 055 background communication digest scheduler:
  - Added a manager-triggered digest scheduler API that scans daily/weekly
    notification preferences for an organization, rejects immediate-mode runs,
    skips people without unread non-digest inbox items, and reuses the existing
    per-person digest creation path.
  - Extended the operations console with a Run digests control and batch run
    summary showing checked, created, and skipped counts.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend
    production build, browser QA, and real cron/background worker execution.
- Implemented slice 056 family inbox portal:
  - Added an authenticated my-inbox API so guardians, parents, athletes, and
    members can load their own messages without supplying a person id.
  - Added a self-service inbox read endpoint that lets recipients mark their
    own messages read while preserving manager override for operators.
  - Added a `/family` portal page with organization/account controls, unread
    metrics, message list, message detail, and read acknowledgement.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend
    production build, browser QA, and live Keycloak guardian-account mapping.
- Implemented slice 057 family athlete consent dashboard:
  - Added an authenticated family summary API that lists athletes linked to the
    current guardian account inside an organization.
  - Included relationship role, consent signing flags, pending consent-request
    counts, and latest consent status/scope/signing time for each child.
  - Extended the `/family` portal with child cards and consent counters
    alongside the inbox workflow.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend
    production build, browser QA, and live Keycloak guardian-account mapping.
- Implemented slice 058 guardian account email binding:
  - Updated the identity bridge so a first login with an email already recorded
    on a guardian/member `Person` binds the `AppUser` to that existing person
    instead of creating a duplicate.
  - Existing app users with missing or broken person links are repaired against
    the same email-based person lookup before falling back to new person
    creation.
  - User email/display name are refreshed from the verified principal during
    login.
  - Verification: `uv run ruff check .`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite and live Keycloak
    guardian-account login.
- Implemented slice 059 family schedule and clearance view:
  - Added a guardian-scoped family events API that derives upcoming child
    activities from attendance invitations and team roster membership.
  - Reused the existing safeguarding clearance logic so family users see the
    same consent-required, denied, expired, no-guardian, and cleared states
    that operators enforce at check-in.
  - Extended `/family` with upcoming event cards showing child, event type,
    date, attendance status, and clearance status alongside inbox and consent
    summaries.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend
    production build, browser QA, and live guardian account smoke.
- Implemented slice 060 family event RSVP:
  - Added a guardian-owned RSVP API for linked child events, allowing confirmed
    or declined responses only.
  - Confirmation reuses safeguarding clearance and blocks when consent is
    missing, denied, expired, or otherwise not cleared; declined responses can
    still be recorded.
  - Extended `/family` event cards with Confirm and Decline controls that
    update the schedule state in place.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend
    production build, browser QA, and live guardian account smoke.
- Implemented slice 061 family portal consent responses:
  - Added guardian-owned pending consent request listing for `/family`, scoped
    to the authenticated guardian and linked athletes with consent authority.
  - Added direct family portal consent responses for grant/deny decisions,
    including expiry handling, request fulfillment, and consent upsert through
    the existing safeguarding consent path.
  - Extended `/family` with pending consent cards and Grant/Deny controls that
    update child consent summaries in place.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend
    production build, browser QA, and live guardian account smoke.
- Implemented slice 062 branded public organization sites:
  - Added a public organization site API addressable by slug or subdomain,
    returning brand fields, contact details, teams, and upcoming events without
    requiring an authenticated session.
  - Added a branded `/site/[slug]` frontend page that applies tenant colors,
    logo/public name, mission, contact links, team cards, and schedule cards.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend
    production build, browser QA, and live subdomain routing.
- Implemented slice 063 public registration inquiry funnel:
  - Added persisted registration inquiries for public organization sites,
    including athlete, guardian, contact, team, age-group, sport-interest,
    source URL, and review status fields.
  - Added public inquiry submission by organization slug/subdomain and an
    authenticated operator inquiry list for organization managers.
  - Extended `/site/[slug]` with a branded registration inquiry form tied to
    the site's team list.
  - Added Alembic migration `f2d4c6a8b901_add_registration_inquiries.py`.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, migration
    upgrade/downgrade execution, frontend production build, and browser QA.
- Implemented slice 064 registration inquiry conversion:
  - Added an operator conversion API for public registration inquiries that
    creates athlete person/profile records, athlete organization membership,
    optional team roster entry, and optional guardian relationship from the
    inquiry contact details.
  - Converted inquiries are marked `converted` and protected from repeat
    conversion.
  - Extended the operations console tenant panel with recent inquiry cards and
    Convert actions that add the converted athlete into the local roster state.
  - Verification: `uv run ruff check .`,
    `pnpm --filter @afrolete/frontend typecheck`, `git diff --check`.
  - Not tested in this fast slice: full backend test suite, frontend
    production build, browser QA, and bulk inquiry deduplication.

## Implementation Slices

| Slice | Status | Evidence | Notes |
| --- | --- | --- | --- |
| 000 - Fresh V2 repository | Complete | Commit `d723d61`; commit `0affab9` | Repo initialized, README charter pushed. |
| 001 - Executable SaaS foundation | Complete | Slice 001 foundation commit | Backend/frontend/infra starter code added and verified. |
| 002 - Identity, tenant, and authorization vertical | Partial | Backend tests 22/22 plus SpiceDB adapter tests; frontend typecheck | Tenant graph, authz boundary, Keycloak bearer-token validation, frontend PKCE session handling, user provisioning, SpiceDB adapter, organization APIs, team APIs, and committee APIs implemented; live service smoke tests remain. |
| 003 - Database migration baseline | Complete | Alembic upgrade/downgrade; backend tests 11/11 | Baseline revision captures the current schema; production execution against `db.lindela.io` remains a deployment task. |
| 004 - Safeguarding, consent, and tenant branding | Partial | Local PostgreSQL migration verified; backend tests 14/14 | Backend model/API support for guardians, consent requests, consent capture channels, minor event clearance, and branded organization sites. |
| 005 - Event scheduling and attendance | Partial | Backend tests 16/16 | Event APIs, roster invitation seeding, attendance recording/listing, and consent-aware check-in implemented; frontend event workflows remain. |
| 006 - Keycloak authentication | Partial | Backend tests 22/22; frontend typecheck | Keycloak JWT validation, frontend PKCE sign-in/session handling, bearer API calls, logout wiring, and user provisioning are implemented behind runtime mode; live realm smoke test remains. |
| 007 - SpiceDB authorization adapter | Partial | Adapter tests 4/4 | Official Python gRPC client wired behind runtime mode; live schema/write/check smoke test remains. |
| 008 - Operational SaaS console | Partial | Frontend typecheck/build; desktop/mobile screenshots | Console now drives tenant, team, roster, event, attendance, guardian consent, and clearance workflows, with local-mode identity and production Keycloak session controls; full live deployed UX smoke remains. |
| 009 - AI agent operations | Partial | Backend tests 29/29; frontend build | Agents can be created, permissioned, assigned to scopes, queued for work, and reviewed from the console; real model execution and AI governance dashboards remain. |
| 010 - Athlete performance metrics and assessments | Partial | Backend tests 31/31; PostgreSQL migration upgrade/downgrade; frontend build | Metric definitions, observations, ALS-style assessments, summaries, and console recording flows are implemented; automated video/audio/wearable ingestion remains. |
| 011 - Training and coaching plans | Partial | Backend tests 34/34; PostgreSQL migration upgrade/downgrade; frontend build; Playwright screenshot | Drill library, scoped plans, weekly plan blocks, session load planning, and console workflows are implemented; automatic AI plan generation, readiness check-ins, and post-session feedback loops remain. |
| 012 - Competition management foundation | Partial | Backend tests 36/36; PostgreSQL migration upgrade/downgrade; frontend build; Playwright screenshot | Competition records, team registration, fixtures, official assignments, match events, result confirmation, computed standings, and console workflows are implemented; automated fixture generation, bracket visualization, conflict optimization, ticketing, and broadcast operations remain. |
| 013 - Communications and notifications | Partial | Backend tests 39/39; PostgreSQL migration upgrade/downgrade; frontend build; Playwright screenshot | Templates, scoped broadcasts, recipient expansion, delivery/read audit records, preferences, quiet hours, emergency override, guardian copy for minors, and console workflows are implemented; later slices add delivery adapters, inbox, digests, and drafting. |
| 014 - Equipment, facilities, and asset operations | Partial | Backend tests 42/42; PostgreSQL migration upgrade/downgrade; frontend build; Playwright screenshot | Facility profiles, equipment inventory, checkout/return, maintenance work orders, facility bookings, overlap rejection, asset summary metrics, and console workflows are implemented; procurement, supplier scoring, RFID scanning, photo uploads, lease billing, and AI optimization remain. |
| 015 - Commercial operations fast surface | Partial | Backend ruff; PostgreSQL migration upgrade; frontend typecheck | Sponsors, sponsorship agreements, fundraising campaigns, donations, ticket products/orders/QR tickets/check-in, invoices, payments, commercial summary, and console workflows are implemented; payment gateway settlement, refunds, tax, accounting exports, sponsorship dashboards, and full verification remain. |
| 016 - Reporting and intelligence fast surface | Partial | Backend ruff; PostgreSQL migration upgrade; frontend typecheck | Report definitions, generated reports, scheduled delivery, intelligence insights, predictive risk scores, export jobs, reporting summary, and console workflows are implemented; real AI model execution, rendered PDF/Excel generation, charts, benchmark models, and full verification remain. |
| 017 - SaaS billing and subscription fast surface | Partial | Backend ruff; PostgreSQL migration upgrade; frontend typecheck | Billing plans, tenant subscriptions, usage meters/records, SaaS invoices/payments, entitlements, billing summary, and console workflows are implemented; Stripe/processor webhooks, dunning automation, tax localization, plan-change proration, and full verification remain. |
| 018 - Frontend Keycloak session handling | Partial | Frontend typecheck; diff check | Browser OIDC authorization-code + PKCE login, bearer-token API attachment, session display, logout URL wiring, and local-mode fallback are implemented; live Keycloak redirect and deployed smoke test remain. |
| 019 - Communication delivery adapters | Partial | Backend ruff; communications tests 4/4; frontend typecheck | Configurable HTTP webhook dispatch, channel-specific adapter URLs, secured provider delivery callbacks, in-app delivery handling, failure capture, and console dispatch are implemented; real provider credentials and background delivery hardening remain. |
| 020 - Agent task execution fast surface | Partial | Backend ruff; agent tests 2/2; frontend typecheck | Agent tasks can now execute through a deterministic local executor or provider-neutral webhook boundary, produce output refs/review notes, and update console task state; live provider workers, model credential vaulting, run history tables, and governance telemetry remain. |
| 021 - Communication inbox, digests, and AI drafting | Partial | Backend tests 44/44; frontend typecheck/build | Person inbox, manager/self access, digest generation, notification-preference channel selection, deterministic AI-assisted drafts, console draft/digest/inbox controls, and read actions are implemented; provider-specific credentials, background digest scheduler, and full parent portal remain. |
| 022 - Asset procurement and utilization intelligence | Partial | Backend tests 45/45; frontend typecheck/build | Scan lookup, photo metadata updates, procurement recommendations, supplier scorecards, lease quotes, utilization recommendations, and console controls are implemented; supplier ordering workflows, file uploads, RFID hardware integration, and accounting lease billing remain. |
| 023 - Commercial finance controls | Partial | Backend tests 46/46; frontend typecheck/build | Tax quote estimates, settlement summaries, ticket refunds, invoice refunds, accounting export rows, sponsorship dashboard metrics, and console controls are implemented; live payment provider webhooks, tax authority filing, accounting API sync, and sponsor-facing dashboards remain. |
| 024 - Reporting output intelligence | Partial | Backend ruff; frontend typecheck; diff check | Report artifact rendering metadata, verification scoring, chart-ready summaries, benchmark models, deterministic AI review insights, and console controls are implemented; real binary PDF/Excel generation, report file storage, visual chart rendering, and full verification remain. |
| 025 - SaaS billing operations | Partial | Backend ruff; frontend typecheck; diff check | SaaS tax quotes, plan-change proration quotes, dunning notice preparation, signed payment webhook intake, invoice status mutation, and console controls are implemented; external dunning delivery, tax filing integrations, and full verification remain. |
| 026 - Competition automation | Partial | Backend ruff; frontend typecheck; diff check | Round-robin fixture generation, bracket projections, conflict detection, and console controls are implemented; advanced tournament advancement, bracket visualization polish, optimization algorithms, and full verification remain. |
| 027 - AI training plan generation | Partial | Backend ruff; frontend typecheck; diff check | AI-assisted plan generation from readiness, assessments, observations, drills, and upcoming competition fixtures is implemented with generated blocks and console controls; live model generation, availability calendars, post-session feedback, and full verification remain. |
| 028 - Performance ingestion and review | Partial | Backend tests 29/29; backend ruff; frontend typecheck/build; migration graph check; diff check | Provider-neutral evidence ingestion, structured provider payload parsing, WHOOP/Garmin/Apple Health/Fitbit/Polar/Oura/Catapult-style schema normalization, signed wearable webhook ingestion with replay protection, wearable provider connection records, OAuth authorization/callback state handling, token lifecycle auditing, configurable provider pull adapters with pagination/rate-limit audit, provider webhook registration auditing, sync-run auditing, metric-specific narrative parsing, model-assisted narrative extraction, parser/model confidence metadata, benchmark-suite evaluation, persisted benchmark datasets, pending-review observations, human review/correction, and console controls are implemented; browser QA, live model/provider smoke tests, and full verification remain. |
| 029 - AI agent governance telemetry | Partial | Backend ruff; frontend typecheck; diff check | Derived run records, governance summary metrics, credential-boundary status, and console telemetry cards are implemented; live model providers, OpenBao secret fetch, persisted run history, audit immutability, and full verification remain. |
| 030 - Binary report artifacts | Partial | Backend ruff; frontend typecheck; diff check | Authenticated PDF/XLSX/CSV/API/HTML artifact download, byte-level checksums, filenames, render metadata, and console download controls are implemented; object storage persistence, visual report QA, and full verification remain. |
| 031 - Report artifact storage boundary | Partial | Backend ruff; frontend typecheck; diff check | Local persisted report artifacts, configurable artifact directory/prefix, stored artifact URLs, and download headers are implemented; MinIO persistence, signed URLs, lifecycle policy, and full verification remain. |
| 032 - Signed report artifact access | Partial | Backend ruff; frontend typecheck; diff check | Short-lived HMAC signed artifact links, unauthenticated signed local artifact serving, checksum headers, signing TTL configuration, and console link minting are implemented; MinIO persistence, lifecycle policy, visual report QA, and full verification remain. |
| 033 - Reporting chart visualization | Partial | Frontend typecheck; diff check | Reporting throughput, insight severity, and predictive risk chart payloads now render as compact bars and donut cards in the console; deeper dashboard interactions, visual QA, and full verification remain. |
| 034 - Billing webhook signature boundary | Partial | Backend ruff; frontend typecheck; diff check | SaaS payment webhook intake now supports timestamped HMAC signature enforcement, configurable tolerance, signature result reporting, and console visibility; external dunning delivery, tax filing integrations, plan-change application, and full verification remain. |
| 035 - Billing dunning delivery | Partial | Backend ruff; frontend typecheck; diff check | Overdue invoice dunning notices can now be delivered through a configurable provider-neutral webhook, record-only mode stays usable locally, delivery keys and timeouts are configurable, and the console shows delivery outcomes; tax filing integrations, plan-change application, and full verification remain. |
| 036 - Billing plan-change application | Partial | Backend ruff; frontend typecheck; diff check | Proration quotes can now be applied to tenant subscriptions, updating negotiated price, optional plan identity, billing cycle, subscription status, and plan-change notes from the console; tax filing integrations and full verification remain. |
| 037 - Billing tax filing delivery | Partial | Backend ruff; frontend typecheck; diff check | Invoice tax totals can now be packaged by jurisdiction and period, prepared in record-only mode, or delivered through a configurable tax filing webhook with separate keys/timeouts and console result visibility; full billing verification remains. |
| 038 - Tournament round advancement | Partial | Backend ruff; frontend typecheck; diff check | Confirmed non-draw winners from a source stage/round can now be advanced into the next knockout round with duplicate protection, bye reporting, generated fixture visibility, and console controls; bracket visualization polish, scheduling optimization, broadcast operations, and full verification remain. |
| 039 - Bracket visualization lanes | Partial | Frontend typecheck; diff check | Competition brackets now render as structured responsive lanes with match slots, projected/byed teams, fixture status, and winner visibility instead of compressed text; scheduling optimization, broadcast operations, and full verification remain. |
| 040 - Competition schedule optimization | Partial | Backend ruff; frontend typecheck; diff check | Pending fixtures can now be rescheduled by a deterministic optimizer that preserves confirmed finals, enforces team rest windows, avoids venue overlaps, updates fixture notes, and exposes moved/protected counts in the console; ticketing, broadcast operations, and full verification remain. |
| 041 - Competition broadcast operations | Partial | Backend ruff; frontend typecheck; diff check | Competition updates can now be broadcast to rostered participants and guardians through the existing communications delivery pipeline, with generated status/fixture summaries, channel selection, dispatch counts, and console visibility; ticketing and full verification remain. |
| 042 - Competition ticketing integration | Partial | Backend ruff; frontend typecheck; diff check | League and tournament fixtures can now open ticket sales through the existing commercial ticket product system, auto-create linked match events when needed, show ticket capacity/sales in the competition console, and feed the commerce summary/check-in/refund workflows; full competition verification remains. |
| 043 - Training readiness and feedback loops | Partial | Backend ruff; frontend typecheck; diff check | Training sessions now collect readiness, soreness, sleep, mood, actual RPE, duration, completion, athlete feedback, coach notes, load deltas, and recommendations from the console; live model generation, availability calendars, and full verification remain. |
| 044 - Training availability suggestions | Partial | Backend ruff; frontend typecheck; diff check | Coaches can now request ranked training slots that account for existing events, training sessions, competition fixtures, and facility bookings, then apply the best slot to the session planner; live calendar sync and full verification remain. |
| 045 - Supplier ordering workflow | Partial | Backend ruff; frontend typecheck; diff check | Procurement recommendations can now become supplier orders, open orders are visible in the asset console, and receiving an order updates equipment total/available quantities; real supplier API submission, invoice sync, and full verification remain. |
| 046 - Equipment file uploads | Partial | Backend ruff; frontend typecheck; diff check | Equipment records now accept real local file uploads through a base64 API, persist bytes with checksum-based storage names, keep upload metadata, can mark uploaded images as the equipment photo, and expose files in the asset console; MinIO persistence, lifecycle policy, and full verification remain. |
| 047 - RFID scan intake | Partial | Backend ruff; frontend typecheck; diff check | RFID reader events can now be recorded as durable equipment scan events, preserve unmatched codes for reconciliation, update matched item audit dates and reader locations, expose scan history filters through the API, and show latest scans in the asset console; live reader gateway provisioning and full verification remain. |
| 048 - Supplier API submission | Partial | Backend ruff; frontend typecheck; diff check | Supplier orders can now be submitted through a provider-neutral webhook or prepared in record-only mode, update order submission status and notes, expose delivery outcomes in the API, and provide Submit controls in the asset console; supplier-specific adapters, invoice sync, and full verification remain. |
| 049 - Equipment lease invoicing | Partial | Backend ruff; frontend typecheck; diff check | Equipment lease estimates can now be converted into draft commercial invoices with person/team billing targets, invoice numbers, due dates, memo context, and console visibility; accounting API sync, lease payment schedules, and full verification remain. |
| 050 - RFID reader gateway provisioning | Partial | Backend ruff; frontend typecheck; diff check | RFID readers can now be provisioned with hashed reader keys, listed by organization, accept hardware-style gateway scan posts with a reader key, update reader heartbeat/last-scan metadata, and appear in the asset console; live device fleet rollout and full verification remain. |
| 051 - Equipment lease payment schedules | Partial | Backend ruff; frontend typecheck; diff check | Equipment leases can now create persisted finance invoices, lease schedules, and monthly installment rows with due dates, person/team targets, console Schedule controls, and lease schedule visibility; installment payment reconciliation and full verification remain. |
| 052 - Equipment lease payment reconciliation | Partial | Backend ruff; frontend typecheck; diff check | Lease schedules can now record commercial payments against their backing invoice, mark due installments paid in sequence, update invoice paid/partial state, expose remaining balances, and provide console Reconcile/Pay controls; partial-installment allocation and full verification remain. |
| 053 - Supplier invoice sync | Partial | Backend ruff; frontend typecheck; diff check | Received, submitted, and ordered supplier orders can now be packaged for supplier invoice/accounting sync through record-only mode or a provider-neutral webhook, update order sync status and notes, and expose Sync controls/results in the asset console; supplier-specific adapters and full verification remain. |
| 054 - Partial lease installment allocation | Partial | Backend ruff; frontend typecheck; diff check | Lease installment rows now persist amount paid, reject overpayment beyond invoice balance, allocate payments across installments including partial fills, update partial/paid installment status, and show paid/partial counts in the console; full verification remains. |
| 055 - Background communication digest scheduler | Partial | Backend ruff; frontend typecheck; diff check | Daily/weekly notification preferences can now be processed in batch through a manager-triggered API, console control, and unified due-worker lane, skipping empty inboxes and reusing per-person digest creation; live host journal smoke, provider delivery credentials, browser QA, and full verification remain. |
| 056 - Family inbox portal | Partial | Backend ruff; frontend typecheck; diff check | Authenticated users can now load their own communication inbox, mark messages read, and use a dedicated `/family` portal surface with inbox metrics and message detail; live Keycloak guardian mapping and full verification remain. |
| 057 - Family athlete consent dashboard | Partial | Backend ruff; frontend typecheck; diff check | Guardian accounts can now retrieve linked athlete summaries with relationship, consent authority, pending consent counts, and latest consent state, with child cards exposed in `/family`; live Keycloak guardian mapping and full verification remain. |
| 058 - Guardian account email binding | Partial | Backend ruff; diff check | First login now binds an `AppUser` to an existing `Person` with the same verified email, so guardian/member accounts can access their linked family data without duplicate person records; live Keycloak smoke and full verification remain. |
| 059 - Family schedule and clearance view | Partial | Backend ruff; frontend typecheck; diff check | Guardian accounts can now see upcoming linked-athlete events with attendance and safeguarding clearance state in `/family`; live guardian account smoke and full verification remain. |
| 060 - Family event RSVP | Partial | Backend ruff; frontend typecheck; diff check | Guardians can confirm or decline linked child events from `/family`, with confirmations gated by the same safeguarding clearance logic as operator check-in; live guardian account smoke and full verification remain. |
| 061 - Family portal consent responses | Partial | Backend ruff; frontend typecheck; diff check | Guardians can now list pending consent requests and grant or deny them from `/family`, fulfilling requests and updating consent records through existing safeguarding flows; live guardian smoke and full verification remain. |
| 062 - Branded public organization sites | Partial | Backend ruff; frontend typecheck; diff check | Organization slugs/subdomains now expose public branded site profiles and `/site/[slug]` renders tenant colors, logo/name, mission, contact links, teams, and upcoming events; live subdomain routing and full verification remain. |
| 063 - Public registration inquiry funnel | Partial | Backend ruff; frontend typecheck; diff check | Public organization sites now collect persisted player/parent registration inquiries and operators can list them through an authenticated organization API; full verification remains. |
| 064 - Registration inquiry conversion | Partial | Backend ruff; frontend typecheck; diff check | Operators can convert public inquiries into athlete person/profile records, athlete membership, team roster entries, and guardian relationships from the console; full verification remains. |
| 065 - Registration inquiry review workflow | Partial | Backend ruff; frontend typecheck; diff check | Operators can now triage public inquiries through review, contacted, waitlisted, and rejected states, capture review notes, set follow-up times, preserve reviewer metadata, and still convert accepted inquiries into roster records; full verification remains. |
| 066 - Registration inquiry follow-up messaging | Partial | Backend ruff; frontend typecheck; diff check | Operators can now queue an email follow-up directly from a public registration inquiry, automatically create or reuse the contact person, mark the inquiry contacted, append review context, and surface the message in the existing communications console; full verification remains. |
| 067 - Persisted AI agent run ledger | Partial | Backend ruff; frontend typecheck; diff check | Agent queueing, execution, and manual review updates now append durable run records with execution mode, model policy, timing, operator, governance notes, idempotency keys, and hash chaining, and the console shows the latest ledger hashes; full migration/live-provider verification remains. |
| 068 - AI agent ledger verification | Partial | Backend ruff; frontend typecheck; diff check | The agent ledger now has a verification API that recomputes record hashes, checks hash-chain continuity by ledger sequence, reports broken record IDs, and surfaces ledger validity plus latest hash in the console; full migration/live-provider verification remains. |
| 069 - MinIO-compatible object storage adapter | Partial | Backend ruff; frontend typecheck; diff check | Report artifacts and equipment uploads now use a shared storage adapter that supports local files by default and S3-compatible MinIO writes/reads through SigV4-signed backend requests, with environment examples and MinIO deployment notes; live MinIO smoke remains. |
| 070 - Authenticated equipment file downloads | Partial | Backend ruff; frontend typecheck; diff check | Equipment file records now have an authenticated download endpoint that reads through the same local/MinIO storage adapter, verifies checksums before returning bytes, and adds console download controls; live MinIO download smoke and browser QA remain. |
| 071 - Safeguarding incident reporting | Partial | Backend ruff; frontend typecheck; diff check | Operators can now log injury, medical, safeguarding, misconduct, facility, transport, weather, and other incidents with severity, event/team/athlete context, immediate action, medical follow-up and regulatory-report flags, then triage, investigate, and resolve them from the console; full compliance workflows and external reporting remain. |
| 072 - Background checks and compliance credentials | Partial | Backend ruff; frontend typecheck; diff check | Operators can now request background checks for athlete-facing people, track provider/reference/status/risk/expiry, record safeguarding, first-aid, coaching, officiating, driver, medical, and other credentials with renewal dates, receive signed provider screening results, then update compliance state from the console; provider-specific adapters and evidence upload review remain. |
| 073 - Compliance dashboard and expiry reconciliation | Partial | Backend ruff; frontend typecheck; diff check | Managers now get a derived safeguarding compliance summary with overall compliance percentage, check/credential/incident counters, blocker queues, renewal queues, investigation queues, a reconciliation action, and a unified due-worker lane that marks expired checks/credentials and near-due renewals on schedule; access-control automation and provider-specific scoring policies remain. |
| 074 - Incident regulatory report packages | Partial | Backend ruff; frontend typecheck; diff check | Reportable safeguarding incidents can now generate auditable regulatory packages with agency, jurisdiction, due date, narrative, checklist payload, submission payload, external reference, draft/ready/submitted/accepted/rejected workflow controls, Markdown/PDF exports, signed stored artifacts, and provider-neutral regulatory portal submission in the console; provider-specific government portal schemas and live credentials remain. |
| 075 - Incident insurance claim tracking | Partial | Backend ruff; frontend typecheck; diff check | Injury, liability, equipment, property, travel, and other incident claims can now be drafted from safeguarding incidents with insurer, policy, claim number, coverage verification, claimed/approved/paid/reserve amounts, documentation checklist, submission payload, communication log, tracking URL, and ready/submitted/approved/paid/denied workflow controls; provider-specific claim schemas and live insurer sandbox credentials remain. |
| 076 - Return-to-play medical clearance | Partial | Backend ruff; frontend typecheck; diff check | Injury and medical incidents can now spawn medical clearance reviews with athlete binding, provider, assessed/valid dates, return-to-play stage, restrictions, documentation pointer, pending/restricted/cleared/not-cleared/expired workflow controls, and provider-neutral medical portal submission/status sync in the console; provider-specific medical portal schemas/live credentials remain. |
| 077 - Attendance medical clearance gate | Partial | Backend ruff; frontend typecheck; diff check | Event check-in now blocks confirmed/present attendance when an athlete has an open injury or medical incident without an active cleared or restricted return-to-play clearance, surfaces medical clearance status/reason in attendance rows, and returns structured conflict details for operators; browser QA remains. |
| 078 - Emergency action plan management | Partial | Backend ruff; frontend typecheck; diff check | Facilities can now maintain emergency action plans with emergency contacts, evacuation routes, medical/weather/communication protocols, equipment locations, assembly points, special-needs plans, review dates, plan status controls, live activation records, assigned responders, guidance steps, communication logs, outcomes, and response-time tracking; mobile emergency UI, automated alerts, and map/media integrations remain. |
| 079 - Emergency activation alert dispatch | Partial | Backend ruff; frontend typecheck; diff check | Emergency activations can now create urgent quiet-hours-override alert messages through the existing communications recipient expansion, including guardian copies for minors, message IDs, recipient counts, and console dispatch controls; live provider delivery, escalation trees, and mobile map/media UI remain. |
| 080 - Emergency escalation matrix | Partial | Backend ruff; frontend typecheck; diff check | Emergency action plans now capture incident command roles, tiered escalation matrices, and external agency contacts, activations carry an escalation level, alert bodies include escalation context, and the console can escalate active responses; live mobile command UI and automated escalation timers remain. |
| 081 - Emergency post-incident reporting | Partial | Backend ruff; frontend typecheck; diff check | Emergency activations can now generate or reuse linked safeguarding incident reports with mapped incident type, severity, activation narrative, immediate action, medical follow-up, regulatory-report flags, and console linkage; richer after-action review packages and browser QA remain. |
| 082 - Event weather safety decisions | Partial | Backend ruff; frontend typecheck; diff check | Events can now store provider-neutral weather assessments with temperature/heat index/WBGT, humidity, AQI, lightning distance, wind, precipitation, deterministic alert classification, proceed/monitor/modify/delay/evacuate decisions, recommended actions, and console weather checks; live multi-source provider ingestion and automated participant broadcasts remain. |
| 083 - Event weather alert dispatch | Partial | Backend ruff; frontend typecheck; diff check | Weather assessments can now generate urgent event-scoped communication alerts through the existing broadcast recipient expansion, including guardian copies, quiet-hours override, message IDs, recipient counts, and console dispatch controls; live weather-triggered automation and provider delivery remain. |
| 084 - Event travel planning and risk | Partial | Backend ruff; frontend typecheck; diff check | Events can now carry travel plans with destination, transport mode, departure/return times, route, driver/vehicle checks, staff/passenger manifests, lodging/meals/equipment, emergency contacts, medical access, consent due dates, costs, deterministic travel risk classification, status workflow, and console controls; live GPS tracking, carpool matching, receipts, and payment collection remain. |
| 085 - Event travel consent generation | Partial | Backend ruff; frontend typecheck; diff check | Event travel plans can now generate event-scoped guardian consent requests for event participants using the existing one-time token consent workflow, with minor/unknown-age filtering, primary signing guardian selection, duplicate pending request detection, destination/token return, and console controls; multi-level school/association approval, payment collection, and automated reminder delivery remain. |
| 086 - Event travel consent reminders | Partial | Backend ruff; frontend typecheck; diff check | Event travel plans can now send guardian reminders for pending event-scoped travel consent requests through the existing communications pipeline, with selectable email/SMS/WhatsApp/Telegram/push/in-app channels, pending request counts, recipient counts, message linkage, and console controls; scheduled reminder automation, provider delivery credentials, and per-request reminder history remain. |
| 087 - Event travel safety manifest | Partial | Backend ruff; frontend typecheck; diff check | Event travel plans can now produce a staff safety manifest with event participants, guardian names, guardian contact destinations, medical clearance status/reason, emergency contacts, medical access plan, participant counts, and console controls; exportable PDFs, offline mobile access, and fine-grained medical data permissions remain. |
| 088 - Event travel fee invoicing | Partial | Backend ruff; frontend typecheck; diff check | Event travel plans can now generate deterministic per-participant trip fee invoices through the existing finance invoice system, bill guardians for minors, skip minors without signing guardians, reuse existing invoice numbers to prevent duplicate charges, total generated fees, and expose console controls; online payment checkout, payment links, waivers/scholarships, and automated payment reminders remain. |
| 089 - Event travel approval workflow | Partial | Backend ruff; frontend typecheck; migration upgrade; diff check | Event travel plans can now maintain approval requirements with approval level, assigned approver, pending/approved/rejected/cancelled status, decision actor/time, unique per-plan levels, and console controls to require, list, approve, or reject approvals; automatic routing to school/association approvers, notification tasks, and readiness gating remain. |
| 090 - Event travel inspection checklists | Partial | Backend ruff; frontend typecheck; migration upgrade; diff check | Event travel plans can now seed, list, and update digital checklist items for pre-trip inspections and other travel phases, with default safety items, per-item pending/completed/blocked/not-applicable status, operator completion actor/time, evidence URL, notes, uniqueness per checklist item, and console controls; photo upload attachment, checklist templates by trip type, and departure gating remain. |
| 091 - Event travel live tracking updates | Partial | Backend ruff; frontend typecheck; migration upgrade; diff check | Event travel plans can now record provider-neutral GPS/location updates with phase, source, timestamp, recorder, latitude/longitude, speed, heading, notes, and linked guardian/team notifications for departure, delay, arrival, and return phases through the existing communications pipeline; hardware GPS ingestion, geofencing, and streaming transport remain. |
| 092 - Event travel expense and reimbursement tracking | Partial | Fast backend/frontend checks; migration authored | Event travel plans can now capture travel expenses with category, vendor, amount, currency, incurred time, paid-by person, receipt URL, notes, reimbursement status, approving actor, reimbursement time, and console controls to submit, list, approve, reimburse, or reject trip costs; live receipt uploads, accounting sync, and richer reimbursement policy remain. |
| 093 - Offline travel manifest export | Partial | Fast backend/frontend checks | Travel manifests can now be exported as CSV or text payloads with deterministic filenames, participant IDs, guardian names/contacts, medical clearance state, medical reasons, emergency contacts, and console export controls; direct file download packaging, PDF layout, and signed offline links remain. |
| 094 - Travel carpool coordination | Partial | Fast backend/frontend checks; migration authored | Event travel plans can now track carpool ride requests and offers with rider/driver person links, pickup/dropoff locations, optional pickup/dropoff coordinates, seat counts, pickup windows, open/matched/confirmed/cancelled status, match scores, matched time, notes, APIs, and console controls to create, load, match, confirm, or cancel rides; live map routing remains. |
| 095 - Travel departure readiness gate | Partial | Fast backend/frontend checks | Event travel plans can now run a departure readiness gate that combines deterministic travel risk, approvals, rejected approvals, checklist pending/blocked counts, seeded checklist presence, pending guardian travel consents, and console blocker/warning display with recommended ready/draft status; automatic status enforcement, scheduled rechecks, and browser workflow QA remain. |
| 096 - Travel route optimization | Partial | Fast backend/frontend checks | Event travel plans can now generate deterministic route optimizations with balanced/fastest/safest/carpool-dense strategies, ordered origin/carpool/destination stops, pickup windows, seat context, traffic/weather delay modifiers, latest weather-assessment reroute guidance, recommended strategy overrides, recommended departure time, route risk warnings, and console optimization controls; live map provider routing, live traffic/geocoding APIs, and weather provider rerouting remain. |
| 097 - Travel fee checkout links | Partial | Fast backend/frontend checks | Existing travel fee invoices can now be converted into provider-neutral checkout/session payloads with invoice IDs, payer IDs, open amounts, provider labels, deterministic payment URLs, processor-style session IDs, session URLs/status, client references, return URLs, optional expiry, aggregate open amount, and console payment-session controls; reminder automation and live payment provider handoff remain. |
| 098 - Travel receipt uploads | Partial | Fast backend/frontend checks | Travel expenses can now accept real receipt uploads from the console, decode base64 payloads, persist files through the local/MinIO-compatible object storage adapter, checksum stored content, update expense receipt URLs, and return upload metadata; authenticated receipt download/review queues and accounting attachment sync remain. |
| 099 - Travel geofence breach checks | Partial | Fast backend/frontend checks | Event travel plans can now evaluate the latest recorded vehicle location against a configurable circular safety zone, compute distance from center, flag breaches, send urgent event-scoped guardian/team alerts through the communications pipeline, and expose console geofence controls; persistent geofence zones, hardware GPS ingestion, and streaming automation remain. |
| 100 - Travel approval routing | Partial | Fast backend/frontend checks | Event travel plans can now automatically recommend and seed operations, school, association, medical, and finance approval records from trip risk, consent requirements, medical context, destination level, weather risk, and cost signals, then show created/existing approval routes in the console; notification tasks, delegated approver assignment, and policy-admin configuration remain. |
| 101 - Travel checklist evidence uploads | Partial | Fast backend/frontend checks | Travel checklist items can now accept real photo/PDF evidence uploads from the console, decode base64 payloads, persist files through the local/MinIO-compatible object storage adapter, checksum stored content, update checklist evidence URLs, and mark uploaded items completed; authenticated evidence download/review queues and checklist templates by trip type remain. |
| 102 - Travel consent reminder automation | Partial | Fast backend/frontend checks | Events can now run an automated travel consent reminder pass that scans consent-required travel plans due within a configurable horizon, counts pending event consent requests, deduplicates guardian recipients, sends one scheduled-style event reminder through the communications pipeline, and surfaces due plan/reminder results in the console; background cron workers, per-request reminder history, and provider delivery credentials remain. |
| 103 - Travel carpool auto-matching | Partial | Fast backend/frontend checks | Travel carpool requests and offers can now be automatically matched from existing ride records using seat capacity, pickup/dropoff token overlap, optional coordinate distance scoring, and departure-window compatibility, update request/offer statuses with deterministic match scores, and expose auto-match controls/results in the console; live geocoder/provider enrichment remains. |
| 104 - Signed offline travel manifest links | Partial | Fast backend/frontend checks | Travel manifests can now be persisted as CSV/text files through the local/MinIO-compatible object storage adapter, minted into short-lived HMAC signed offline links, served without an authenticated session until expiry, and opened from the operations console; PDF layouts and live MinIO smoke remain. |
| 105 - Travel manifest PDF layout | Partial | Fast backend/frontend checks | Signed offline travel manifests can now be rendered as dependency-free PDF artifacts with destination, participant counts, emergency contacts, medical access plan, guardian contacts, medical clearance state, multi-page pagination, HMAC signed delivery, and console PDF-link controls; richer visual branding and live MinIO smoke remain. |
| 106 - Hardware GPS travel ingest | Partial | Fast backend checks | Hardware GPS devices and provider gateways can now post travel-plan location updates through a dedicated ingest endpoint with optional HMAC timestamp signatures, device/provider metadata, accuracy, battery state, external event IDs, automatic travel status transitions, and normal tracking history reads; live provider smoke remains. |
| 107 - Persistent travel geofence zones | Partial | Fast backend/frontend checks; migration authored | Travel plans can now store reusable named geofence zones with center coordinates, radius, optional provider-linked polygon vertices, active state, alert channel, breach alert policy, notes, list/create APIs, saved-zone breach checks, and console save/list/check controls; map drawing and scheduled streaming checks remain. |
| 108 - Travel geofence zone administration | Partial | Fast backend/frontend checks | Saved travel geofence zones can now be renamed, moved, resized, re-channeled, annotated, linked to provider zone IDs/revisions, assigned polygon boundaries, deactivated, reactivated, duplicate-label guarded, and updated from the console geofence form; map drawing and scheduled streaming checks remain. |
| 109 - Travel GPS device provisioning | Partial | Fast backend/frontend checks; migration authored | Travel plans can now provision hardware GPS devices with provider IDs, labels, status, vehicle assignment, installation time, notes, list/create/update APIs, console device controls, active/disabled/maintenance state management, and ingest-time last-seen/battery/accuracy health updates; per-device secret rotation and live provider smoke remain. |
| 110 - Travel device secret rotation | Partial | Fast backend/frontend checks; migration authored | Provisioned travel GPS devices can now rotate one-time ingest secrets, store per-device callback keys, surface secret configuration/rotation time, validate location-ingest HMAC signatures with the device secret before falling back to the global transition key, and expose console rotation controls; external vault write/fetch, replay nonces, and live provider smoke remain. |
| 111 - Travel device replay protection | Partial | Fast backend/frontend checks; migration authored | Travel device ingest now persists provider external event IDs per trip/provider/device, rejects duplicate external event IDs before creating location updates, links accepted provider events to the resulting GPS update, and returns replay-protection metadata in ingest responses; live provider smoke remain. |
| 112 - Travel replay nonce expiry policy | Partial | Fast backend/frontend checks | Travel device replay records now follow a configurable retention window, prune expired provider event IDs before duplicate checks, and return retention/prune metadata in ingest responses so replay protection is durable but bounded; live provider smoke remain. |
| 113 - Travel driver ratings | Partial | Fast backend/frontend checks; migration authored | Travel plans can now collect post-trip driver ratings with driver/person references, vehicle labels, category scores, reviewer identity, incident flags, use-again recommendations, aggregate summaries, marketplace matching inputs, API routes, and console create/list controls; provider-sourced safety scores remain. |
| 114 - Travel backup driver network | Partial | Fast backend/frontend checks; migration authored | Travel plans can now maintain backup driver rosters with driver/person references, phone, vehicle, capacity, license and screening status, availability, response time, priority ordering, notes, APIs, status updates, marketplace ranking inputs, and console controls; live credential-provider checks remain. |
| 115 - Travel backup driver dispatch | Partial | Fast backend/frontend checks; migration authored | Travel plans can now automatically select an eligible backup driver from the roster using availability, capacity, priority, response time, optional verification filters, and marketplace-compatible scoring signals, mark the driver dispatched, record dispatch reason/time/actor, notify person-linked drivers through communications, return dispatch rationale, and expose console dispatch controls; live credential-provider checks remain. |
| 116 - Travel GPS fleet inventory | Partial | Fast backend/frontend checks | Organizations can now view travel GPS devices across all travel plans with fleet totals, active/maintenance/disabled/lost counts, stale-device detection, low-battery detection, per-device trip destination, vehicle assignment, last-seen health, secret configuration, API access, and console inventory controls; live device fleet rollout and streaming telemetry remain. |
| 117 - Travel reimbursement payout execution | Partial | Fast backend/frontend checks; migration authored | Approved travel expenses can now execute provider-neutral reimbursement payouts with generated or external payout references, provider/status/timestamp/processor audit fields, adapter mode, payout destination, idempotency key, provider status code/response metadata, automatic reimbursed-state transition, API response metadata, and console payout controls; live provider HTTP submission and accounting sync remain. |
| 118 - Travel provider idempotency windows | Partial | Fast backend/frontend checks | Travel device ingest can now configure per-provider replay retention windows through `AFROLETE_TRAVEL_DEVICE_PROVIDER_IDEMPOTENCY_DAYS`, prune expired replay IDs only for the active provider before duplicate checks, and return whether the provider-specific or default idempotency window was used; live provider smoke and streaming transport remain. |
| 119 - Travel route map rendering | Partial | Fast backend/frontend checks | Travel plans can now expose a provider-neutral route map payload from recorded GPS path points and active geofence zones, calculate map bounds, identify origin/latest-position/geofence markers, and render a console map summary with marker and latest-position context; live map SDK tiles, provider routing, and dense-route browser QA remain. |
| 120 - Geocoded carpool scoring | Partial | Fast backend/frontend checks; migration authored | Carpool ride requests and offers can now store optional pickup/dropoff latitude/longitude values, auto-match with distance-bucket scoring when coordinates are present, return pickup/dropoff match distances, retain text-token fallback for ungeocoded rides, and expose coordinate capture/results in the console; live geocoder lookup, map-assisted pickup selection, and browser QA remain. |
| 121 - Mobile travel manifest offline cache | Partial | Fast frontend typecheck; diff check | The operations console can now cache a fetched travel manifest into browser storage, restore it later without a network call, show cache timestamp/version metadata, and expose Cache/Offline controls for field/mobile use; service-worker prefetch and browser QA remain. |
| 122 - Travel payment processor sessions | Partial | Fast backend/frontend checks | Travel fee checkout generation now emits provider-ready processor session metadata for each invoice, including stable session IDs, hosted-session URLs, client references, open/paid session state, success/cancel return URLs, and console session visibility; live processor API creation remains. |
| 123 - Provider-backed geofence polygons | Partial | Fast backend/frontend checks; migration authored | Travel geofence zones can now store provider names, provider zone IDs, provider revisions, and polygon coordinate vertices, evaluate latest vehicle positions with point-in-polygon containment before falling back to radius checks, return boundary type/vertex counts, and expose polygon/provider controls in the console; map drawing UX, scheduled streaming checks, and live map provider sync remain. |
| 124 - Weather-aware travel rerouting | Partial | Fast backend/frontend checks | Route optimization now consumes the latest event weather assessment and route weather risk, upgrades fastest/high-risk trips to safer strategies, estimates deterministic traffic and weather delay buffers, flags reroute-required decisions, returns reroute reasons/actions plus latest weather decision metadata, and surfaces the guidance in the console; live traffic APIs, live weather provider feeds, and map-provider reroute geometry remain. |
| 125 - Verified driver marketplace matching | Partial | Fast backend/frontend checks | Travel plans can now rank available backup drivers as marketplace candidates using license/background verification, capacity, response time, availability, and post-trip rating/incident history, return recommended driver IDs, score rationales, verified counts, and expose marketplace controls/results in the console; live credential-provider verification, external driver marketplace inventory, and provider-sourced safety scores remain. |
| 126 - Travel payout adapter audit | Partial | Fast backend/frontend checks; migration authored | Travel reimbursement payouts now persist bank/mobile-money adapter mode, payout destination, idempotency key, provider status code, provider response payload, and console-visible adapter audit details while retaining deterministic local execution; live provider HTTP submission, callback reconciliation, and accounting sync remain. |
| 127 - Travel telemetry streaming transport | Partial | Fast backend/frontend checks | Travel location tracking now exposes authenticated NDJSON telemetry streams with no-cache stream headers, replayable GPS update rows, stream discovery metadata, latest update context, replay window metadata, and console stream controls; long-lived push workers, provider smoke tests, and browser streaming QA remain. |
| 128 - Encrypted travel manifest offline cache | Partial | Fast frontend typecheck; diff check | Offline travel manifests are now stored as versioned Web Crypto AES-GCM envelopes bound to the operator identity and travel plan, include SHA-256 payload checksums, carry seven-day expiry metadata, reject expired restores, and keep legacy-cache restore compatibility; service-worker prefetch, background refresh, browser crypto QA, and multi-device key recovery remain. |
| 129 - Travel device secret vault reference custody | Partial | Fast backend/frontend checks; migration authored | Travel GPS device secret rotation can now stamp each rotated secret with a configurable custody mode, OpenBao-style vault provider, and deterministic vault reference path, expose that metadata through device, fleet, and rotation responses, and render the custody reference in the operations console; live OpenBao write/fetch, removing database fallback storage, and provider smoke tests remain. |
| 130 - Hosted travel fee payment page | Partial | Fast backend/frontend checks | Travel fee checkout sessions now resolve to a public hosted payment-page payload, render a dedicated `/pay/sessions/{sessionId}` portal with invoice status, due dates, method selection, outstanding totals, and confirmation controls, and accept provider-neutral settlement events that record finance payments and mark invoices partial/paid; live processor redirects, PCI payment fields, and browser payment QA remain. |
| 131 - Signed travel fee payment webhooks | Partial | Fast backend/frontend checks | Travel fee payment providers can now post settlement callbacks to a dedicated webhook endpoint with timestamped HMAC signature enforcement, configurable signing key/tolerance, duplicate external-reference protection through the settlement path, and signature-required/validated response metadata; provider smoke tests and browser payment QA remain. |
| 132 - Travel fee provider payload normalization | Partial | Fast backend/frontend checks | The signed travel fee webhook endpoint now accepts raw provider callback bodies and normalizes provider-neutral, Stripe Checkout, M-Pesa STK callback, and PayPal capture payloads into the settlement path, extracting invoice/session metadata, amount/currency, external payment references, method names, and payment status; live provider sandbox smoke tests, reconciliation dashboards, and browser payment QA remain. |
| 133 - Travel fee payment reconciliation | Partial | Fast backend/frontend checks | Travel plans now expose a fee reconciliation API and console control that summarize generated travel invoices, checkout session IDs, paid/partial/unpaid counts, total due/paid/open amounts, per-invoice payment counts, latest provider references, and payment rows from the finance ledger; live provider sandbox smoke tests and browser payment QA remain. |
| 134 - Travel fee reconciliation exceptions | Partial | Fast backend/frontend checks | Travel fee reconciliation now derives operator exceptions for overpaid invoices, paid invoices without payment rows, overdue open balances, payment currency mismatches, missing provider references, and invoice/payment ledger total drift, returning exception severity/action guidance and surfacing exception counts in the console; live provider sandbox smoke tests and browser payment QA remain. |
| 135 - Travel fee exception resolution actions | Partial | Fast backend/frontend checks | Operators can now resolve actionable travel fee reconciliation exceptions by applying open-balance waivers, attaching missing provider references, rebuilding missing payment rows from invoice paid totals, and refund-adjusting overpayments, with each action returning a refreshed reconciliation and console feedback; currency conversion review, live provider sandbox smoke tests, and browser payment QA remain. |
| 136 - Travel fee ledger drift repair | Partial | Fast backend/frontend checks | Travel fee reconciliation now detects per-invoice paid-total drift against finance payment rows and gives operators a sync action that resets the invoice paid total to the payment ledger, refreshes invoice status, and clears the aggregate ledger mismatch when all affected invoices are repaired; live provider sandbox smoke tests and browser payment QA remain. |
| 137 - Travel fee currency mismatch repair | Partial | Fast backend/frontend checks | Operators can now resolve travel fee payment currency mismatches by rebooking the affected payment into the invoice currency, preserving prior amount/currency in notes, recomputing the invoice paid total from payment rows, refreshing invoice status, and returning an updated reconciliation to the console; live provider sandbox smoke tests and browser payment QA remain. |
| 138 - Travel payout callback reconciliation | Partial | Fast backend/frontend checks | Travel reimbursement payouts now accept provider-neutral callback reconciliation by payout reference or idempotency key, optionally enforce timestamped HMAC signatures, normalize provider statuses into paid/queued/failed/cancelled/returned outcomes, update reimbursement state, preserve callback payload audit metadata, and expose console callback controls; live payout provider smoke tests and browser QA remain. |
| 139 - Weather-triggered alert automation | Partial | Fast backend/frontend checks | Event weather assessments can now be processed by an automation run that filters by alert severity, skips already-alerted assessment/channel pairs, dispatches urgent event-scoped weather alerts with guardian copies through the existing communications pipeline, supports dry-run mode for scheduled workers, and exposes automation results in the console; live weather provider ingestion, real worker/cron scheduling, and browser QA remain. |
| 140 - Urgent communication escalation workflow | Partial | Fast backend/frontend checks | Urgent messages can now be escalated to unresolved queued/failed/suppressed recipients through an alternate channel, generating a new quiet-hours-override message with escalation level context, parent-message linkage, trigger timestamp/reason metadata, target/skipped counts, recipient expansion, manager-triggered timer runs, unified due-worker scheduling, repeat suppression, and console controls/results; provider credentials and browser QA remain. |
| 141 - AI model transparency reporting | Partial | Fast backend/frontend checks | Agent governance now exposes a model transparency report derived from the hash-chained run ledger, summarizing model policies, assigned agent counts, run counts, execution modes, failed/review runs, latest evidence, risk bands, credential boundary, ledger validity, recommendations, and console-visible model cards; live model-provider telemetry, formal model registry records, and browser QA remain. |
| 142 - Signed agent worker execution | Partial | Fast backend checks | Live agent webhook execution now serializes deterministic JSON payloads with task idempotency keys and sends timestamped HMAC signatures using the configured agent webhook key, giving external workers a stable replay-protection boundary before returning model output; live worker verification, inbound result callbacks, and OpenBao secret fetch remain. |
| 143 - Signed agent worker callbacks | Partial | Fast backend/frontend checks | External AI workers can now post signed result callbacks with task IDs, statuses, output references, review notes, idempotency keys, and raw provider payload metadata; callbacks update governed agent tasks, append hash-chained worker callback ledger records, ignore duplicate idempotency keys, and expose a console callback simulation control; live worker verification and OpenBao secret fetch remain. |
| 144 - AI model registry records | Partial | Fast backend/frontend checks; migration authored | Organizations can now register AI model policies with provider, family/version, use case, risk tier, review status, documentation, evaluation, limitation, bias, data-residency, owner, and approval metadata; model transparency includes registry status/risk/doc fields and the console can register current agent policies; live model-provider telemetry, approval workflow automation, and browser QA remain. |
| 145 - AI model registry approval controls | Partial | Fast frontend typecheck; diff check | Operators can now approve, block, or retire registered AI model policies from the console, updating governance review status and refreshing the transparency report; multi-person approval routing, policy expiry reminders, and browser QA remain. |
| 146 - AI bias audit records | Partial | Fast backend/frontend checks; migration authored | Registered model policies can now produce persisted fairness/bias audit records with audit dimension, population slice, sample size, disparity proxy score, pass/watch/fail/insufficient-data status, severity, findings, recommendations, mitigation status, reviewer provenance, API access, and console audit controls; automated mitigation, appeal workflows, and browser QA remain. |
| 147 - AI decision appeals | Partial | Fast backend/frontend checks; migration authored | Agent task outputs can now generate decision appeal records with model policy, reason, reviewer question, simple and technical explanations, data summary, alternative options, supporting evidence references, seven-day due dates, pending/review/resolution statuses, resolver provenance, APIs, and console controls to submit, uphold, modify, or overturn appeals; public family/athlete appeal portal, downloadable forms, and browser QA remain. |
| 148 - Ethical AI scorecard | Partial | Fast backend/frontend checks | Agent governance now computes an ethical AI scorecard with a 0-100 score, grade, approved/blocked/undocumented model counts, bias audit pass/fail counts, open mitigations, pending/resolved appeals, human-review queues, ledger validity, public summary text, improvement actions, API access, and console display; public scorecard page, quarterly publication workflow, comments, and browser QA remain. |
| 149 - Public ethical AI scorecard | Partial | Fast frontend typecheck; diff check | Branded public organization sites now load and display the tenant ethical AI scorecard with score, grade, public summary, model approval counts, fairness-audit counts, open appeals, ledger status, and improvement actions using tenant colors; standalone shareable scorecard routes, quarterly publication workflow, comments, and browser QA remain. |
| 150 - Family AI appeal portal | Partial | Fast backend/frontend checks | Family portal users can now list their submitted AI decision appeals, see open appeal counts, and submit a new appeal against an agent task ID with a question and supporting family evidence reference through authenticated `/agents/my-appeals` APIs; task discovery for affected family recommendations, downloadable appeal forms, and browser QA remain. |
| 151 - Family AI recommendation discovery | Partial | Fast backend/frontend checks | Family portal users can now load linked AI recommendation tasks from `/agents/my-family-tasks`, constrained to their guardian-linked athlete profiles and protected again during appeal submission, then choose an AI recommendation from a selector instead of pasting opaque task IDs; richer recommendation context, downloadable appeal forms, and browser QA remain. |
| 152 - Family AI appeal form downloads | Partial | Fast backend/frontend checks | Family portal users can now download a prefilled plain-language AI decision appeal form for any linked recommendation task, with guardian visibility checks reused server-side, model/task context included, existing appeal status shown, and browser download controls added to `/family`; richer recommendation context, PDF rendering, and browser QA remain. |
| 153 - Family AI recommendation context | Partial | Fast backend/frontend checks | Family-visible AI recommendation tasks now include plain-language explanations, source data summaries, alternative review paths, and governance notes in `/agents/my-family-tasks`, and `/family` renders that context before appeal submission or form download; PDF rendering and browser QA remain. |
| 154 - Standalone public AI scorecards | Partial | Fast frontend typecheck; diff check | Branded public sites now link to a dedicated shareable `/site/[slug]/ai-scorecard` route that loads the public organization profile and ethical AI scorecard, renders headline score/grade, model approval, audit, mitigation, appeal, human-review, ledger, generated-date, and improvement-plan context with tenant colors; quarterly publication workflow, comments, and browser QA remain. |
| 155 - Public AI scorecard comments | Partial | Fast backend/frontend checks; migration authored | Public AI scorecards now accept persisted community feedback with optional private submission, list consented published comments, expose comment APIs, and render a scorecard comment list/form on `/site/[slug]/ai-scorecard`; moderation workflows, abuse controls, quarterly publication workflow, and browser QA remain. |
| 156 - AI scorecard comment moderation | Partial | Fast backend/frontend checks | Operators can now list all public-scorecard feedback, see contact/private submission context in the operations console, and moderate comments as published, hidden, flagged, or private feedback through authenticated manage-organization APIs that prevent publishing non-consented comments; abuse controls, quarterly publication workflow, and browser QA remain. |
| 157 - AI scorecard comment abuse controls | Partial | Fast backend/frontend checks; migration authored | Public scorecard comments now persist abuse score/reason metadata, auto-flag likely spam from link floods, spam keywords, long/repeated text, recent duplicates, and rapid repeat submissions, show held-for-review feedback to submitters, and expose abuse context in the operator moderation cards; quarterly publication workflow and browser QA remain. |
| 158 - Quarterly AI scorecard publications | Partial | Fast backend/frontend checks; migration authored | Operators can now publish immutable period-labeled AI scorecard snapshots with score, grade, model/audit/appeal metrics, comment counts, improvement actions, publisher, timestamp, and snapshot hash; public scorecard pages show the latest publication and the operations console lists recent publications; scheduled reminders, PDF export, and browser QA remain. |
| 159 - AI scorecard publication readiness | Partial | Fast backend/frontend checks | Organizations now get a publication-readiness reminder endpoint for the current quarter with due date, days remaining, latest publication context, flagged-comment and pending-appeal blockers, live score/grade, readiness status, and recommended action, plus an operations-console card that refreshes after publishing; scheduled delivery, PDF export, and browser QA remain. |
| 160 - AI scorecard publication artifacts | Partial | Fast backend/frontend checks | Published AI scorecard snapshots can now produce portable Markdown artifacts with period, publication metadata, score, governance metrics, public summary, improvement actions, and snapshot-hash verification text, downloadable from both the public scorecard page and operations console; PDF rendering, object storage persistence, and browser QA remain. |
| 161 - AI scorecard publication reminder delivery | Partial | Fast backend/frontend checks | Operators can now send or schedule manager-facing AI scorecard publication reminders through the existing communications pipeline, with current-quarter readiness, score, blockers, due date, recommended action, recipient counts, and console delivery feedback; real cron scheduling, provider credentials, PDF rendering, object storage persistence, and browser QA remain. |
| 162 - AI scorecard due reminder runs | Partial | Fast backend/frontend checks | Organizations now have a worker-triggerable AI scorecard reminder run that checks the current publication due window, skips already-published or not-yet-due periods, sends manager reminders through communications when eligible, and reports run status in the console; real cross-tenant cron orchestration, provider credentials, PDF rendering, object storage persistence, and browser QA remain. |
| 163 - AI scorecard persisted PDF artifacts | Partial | Fast backend/frontend checks | Published AI scorecard snapshots now render persisted Markdown and dependency-free PDF artifacts through the local/MinIO-compatible object storage adapter, returning checksums, byte sizes, storage keys, storage URLs, and browser-downloadable PDF payloads from both public scorecard pages and the operations console; richer branded PDF layouts, signed share links, live MinIO smoke, and browser QA remain. |
| 164 - AI scorecard signed artifact links | Partial | Fast backend/frontend checks | Published AI scorecard artifacts now have short-lived signed share links with HMAC expiry validation, public artifact serving from persisted storage, checksum headers, and public/operator UI controls for opening shared PDF artifacts; richer branded PDF layouts, live MinIO smoke, artifact access audit logs, and browser QA remain. |
| 165 - AI scorecard artifact access audit | Partial | Fast backend/frontend checks; migration authored | Signed AI scorecard artifact links now record link-created and artifact-opened audit events with publication, organization, format, filename, content type, checksum, byte size, expiry, and timestamp metadata, and the operations console lists recent artifact access activity for managers; requester IP/user-agent attribution, richer analytics, live MinIO smoke, and browser QA remain. |
| 166 - AI scorecard artifact requester attribution | Partial | Fast backend/frontend checks; migration authored | Scorecard artifact audit events now persist requester IP, user-agent, and source labels for link creation and signed artifact opens, and the operations console surfaces the client attribution beside each artifact event; richer access analytics, live MinIO smoke, and browser QA remain. |
| 167 - AI scorecard artifact access analytics | Partial | Fast backend/frontend checks | Managers now get a derived scorecard artifact access summary with total events, link-created/opened counts, PDF/Markdown mix, unique requester count, latest access timestamp, and source buckets in the operations console; chart rendering, anomaly alerts, live MinIO smoke, and browser QA remain. |
| 168 - AI scorecard artifact anomaly alerts | Partial | Fast backend/frontend checks | Scorecard artifact access summaries now include deterministic anomaly alerts for created links with no opens, high open-to-link ratios, many requester IPs, and missing requester attribution, with severity, evidence, and recommended action cards in the operations console; time-series charts, alert notifications, live MinIO smoke, and browser QA remain. |
| 169 - AI scorecard artifact anomaly notifications | Partial | Fast backend/frontend checks | Scorecard artifact anomalies can now be delivered to organization managers through the communications pipeline with channel choice, recipient expansion/fallback, urgent warning handling, message status, recipient attribution, and console send/result controls; scheduled alert runs, provider credential smoke, live MinIO smoke, and browser QA remain. |
| 170 - AI scorecard artifact anomaly alert runs | Partial | Fast backend/frontend checks | Worker-triggerable scorecard artifact anomaly alert runs now evaluate current access anomalies, optionally deliver manager alerts, return sent/skipped state with message and recipient counts, and expose an operations-console run control/result card; real cross-tenant scheduling, provider credential smoke, live MinIO smoke, and browser QA remain. |
| 171 - AI scorecard artifact access trends | Partial | Fast backend/frontend checks | Scorecard artifact access summaries now include 14-day daily trend buckets for created links, opened artifacts, and total activity, with operations-console trend cards for recent daily movement; richer chart visualization, live MinIO smoke, and browser QA remain. |
| 172 - AI scorecard artifact trend visualization | Partial | Fast frontend/backend checks | The operations console now renders signed scorecard artifact daily trend buckets as compact stacked bars for links versus opens, reusing the existing chart-card system without adding dependencies; browser visual QA, live MinIO smoke, and deeper chart interactions remain. |
| 173 - AI scorecard automation orchestration | Partial | Fast backend/frontend checks | A scheduler-ready scorecard automation endpoint now scans explicit or managed organizations, permission-checks each tenant, runs due publication reminders and artifact anomaly alert checks, returns per-organization sent/skipped/message counts, and exposes a selected-tenant console automation control; Temporal/cron deployment wiring, provider credential smoke, and browser QA remain. |
| 174 - Agent webhook OpenBao secret resolution | Partial | Fast backend checks | Agent webhook execution and signed worker callback validation can now resolve the webhook signing key from OpenBao KV when `AFROLETE_AGENT_WEBHOOK_KEY_SECRET_PATH` is configured, while retaining env-key/local modes and exposing credential-boundary recommendations; live OpenBao smoke, provider worker replay, and browser QA remain. |
| 175 - Communication webhook OpenBao secret resolution | Partial | Fast backend/frontend checks | Provider communication dispatch and delivery callback validation can now resolve the shared delivery webhook key from OpenBao KV when `AFROLETE_COMMUNICATION_WEBHOOK_KEY_SECRET_PATH` is configured, reusing a shared OpenBao helper while retaining env-key/local modes; live OpenBao smoke, provider delivery replay, and browser QA remain. |
| 176 - Object storage OpenBao credential resolution | Partial | Fast backend/frontend checks | The MinIO/S3-compatible object storage adapter can now resolve access and secret keys from OpenBao KV references while retaining env-key/local filesystem modes, so report, equipment, travel, and AI scorecard artifacts share the same production secret-custody boundary; live MinIO/OpenBao smoke, lifecycle policy, and browser QA remain. |
| 177 - Billing provider OpenBao secret resolution | Partial | Fast backend/frontend checks | Billing payment webhook signatures, dunning delivery webhooks, and tax-filing delivery webhooks can now resolve provider keys from OpenBao KV references while retaining env-key/local modes; live OpenBao smoke, provider replay, tax authority filing smoke, and browser QA remain. |
| 178 - Supplier provider OpenBao secret resolution | Partial | Fast backend/frontend checks | Supplier order submission and supplier invoice sync webhooks can now resolve provider keys from OpenBao KV references while retaining env-key/record-only local modes; live OpenBao smoke, supplier adapter replay, accounting API sync, and browser QA remain. |
| 179 - Travel provider OpenBao secret resolution | Partial | Fast backend/frontend checks | Travel fee payment webhooks, travel expense payout callbacks, global GPS ingest keys, and per-device vault references can now resolve signing keys from OpenBao KV references while retaining env-key/database local modes; live OpenBao smoke, provider replay, hardware GPS fleet rollout, and browser QA remain. |
| 180 - Artifact signing OpenBao secret resolution | Partial | Fast backend/frontend checks | Report artifact links, AI scorecard artifact links, and travel manifest offline links can now resolve HMAC signing keys from OpenBao KV references while retaining env-key/local modes; live OpenBao smoke, signed-link replay, and browser QA remain. |
| 181 - OpenBao secret precedence normalization | Partial | Fast backend/frontend checks | Configured OpenBao secret paths now take precedence over raw env/default key values across agent, communication, billing, supplier, travel, per-device GPS ingest, and artifact signing resolvers, preventing local defaults from masking deployed vault references; live OpenBao smoke, provider replay, and browser QA remain. |
| 182 - Infrastructure readiness status surface | Partial | Fast backend checks | Platform operators now have a secret-safe infrastructure status endpoint covering Postgres, Keycloak, SpiceDB, OpenBao, object storage, Redis, and Temporal configuration readiness, plus Redis/Temporal environment settings for deployment alignment; live dependency probes and browser QA remain. |
| 183 - Infrastructure readiness operations UI | Partial | Fast frontend/backend checks | The operations console now renders a responsive infrastructure readiness ribbon backed by the platform readiness endpoint, showing sanitized runtime posture for Postgres, Keycloak, SpiceDB, OpenBao, object storage, Redis, and Temporal with refreshable operator feedback; browser visual QA and live dependency probes remain. |
| 184 - Live infrastructure dependency probes | Partial | Fast frontend/backend checks | The platform now exposes secret-safe live dependency probes for Postgres, Keycloak, SpiceDB, OpenBao, object storage, Redis, and Temporal, and the operations console folds probe status and latency into the readiness ribbon; live deployed dependency validation and browser visual QA remain. |
| 185 - Developer ecosystem foundation | Partial | Fast backend/frontend checks; migration authored | Tenant developer applications with one-time client secrets, webhook subscriptions with one-time signing secrets, marketplace listings, review/install counters, summary metrics, APIs, and operations-console controls are implemented; public developer docs, OAuth consent, external app review workflow, live webhook fanout, and browser QA remain. |
| 186 - Developer sandbox API keys | Partial | Fast backend/frontend checks; migration authored | Developer applications can now issue tenant-prefixed sandbox/production API keys with one-time key reveal, hash-only storage, scopes, rate-limit metadata, list/revoke controls, key-inspection authentication, usage counters, last-used attribution, summary metrics, and operations-console management; route-level API-key authorization, public SDK docs, quota enforcement windows, and browser QA remain. |
| 187 - SDK API-key route authorization | Partial | Fast backend checks | Developer API keys now authorize real SDK-facing routes for credential inspection, tenant organization profile access, and training drill list/create workflows with tenant isolation, scope enforcement, usage tracking, and tests through the documented SDK-style training-drill path; broader SDK route coverage, quota enforcement windows, public SDK docs, OAuth consent, and browser QA remain. |
| 188 - Developer API-key quota enforcement | Partial | Fast backend/frontend checks; migration authored | Developer API keys now persist per-minute quota windows with window start, request count, last rate-limited timestamp, usage counters, 429 enforcement, console quota visibility, and tests proving limited keys are blocked after exceeding their configured allowance; distributed/Redis-backed counters, quota plan tiers, broader SDK coverage, public SDK docs, and browser QA remain. |
| 189 - Developer webhook event fanout | Partial | Fast backend/frontend checks; migration authored | Developer webhook subscriptions now receive persisted delivery records from SDK product events, starting with `training.drill.created`; delivery rows capture event IDs, payloads, target URLs, attempts, status, failure details, record-only/manual delivery mode, signed HTTP webhook attempts, operator listing APIs, console delivery cards, and tests for event-to-delivery fanout; retry scheduling, webhook replay controls, broader event catalog coverage, external webhook smoke tests, and browser QA remain. |
| 190 - Developer webhook replay controls | Partial | Fast backend/frontend checks | Operators can now replay persisted developer webhook deliveries through a managed API and console action, reusing the original payload/subscription, incrementing attempt counts, refreshing status/failure fields, and proving replay behavior in the developer workflow test; scheduled retry queues, bulk replay, external webhook smoke tests, and browser QA remain. |
| 191 - Developer webhook retry runner | Partial | Fast backend/frontend checks | Developer webhook deliveries can now be replayed in tenant-scoped retry runs with max-attempt guards, due-status filtering, optional recorded/manual inclusion, status buckets, console retry control, and focused test coverage added for later execution; scheduled background workers, exponential backoff, external webhook smoke tests, and browser QA remain. |
| 192 - Developer integration catalog | Partial | Fast backend/frontend checks | Tenant operators now get a managed developer catalog for API auth headers, SDK entry points, scope descriptions, configured webhook event types, active/cataloged event payload contracts, webhook signing metadata, console cards, and focused test coverage; public hosted docs, SDK package publishing/generated clients, OAuth consent, and browser QA remain. |
| 193 - Public developer docs portal | Partial | Fast backend/frontend checks; frontend build | AfroLete now exposes public developer documentation at `/developers` backed by `/api/v1/developers/public/docs`, including quickstarts, auth headers, SDK surfaces, TypeScript SDK package cataloging, marketplace lanes, webhook contracts, signature guidance, security requirements, and console navigation; SDK package publishing/generated clients, OAuth consent, and browser QA remain. |
| 194 - Developer OAuth consent grants | Partial | Fast backend/frontend checks; migration applied | Developer applications now support persisted OAuth-style authorization grants with registered redirect URI validation, requested/granted scope enforcement, one-time authorization codes, code redemption into expiring AfroLete API tokens, authorization listing, console grant controls, and focused tests; PKCE/public-client exchange, refresh tokens, and browser QA remain. |
| 195 - Developer OAuth PKCE public clients | Partial | Fast backend/frontend checks; migration applied | OAuth-style developer grants now support PKCE public-client proof with S256/plain code challenges, verifier-required token exchange without client secrets, confidential-client fallback, console PKCE controls, docs quickstart updates, and focused tests for missing/verifiable code verifiers; refresh tokens and browser QA remain. |
| 196 - Developer OAuth refresh-token rotation | Partial | Fast backend/frontend checks; migration applied | OAuth-issued developer tokens now include hash-only refresh tokens with 90-day expiry, a `/developers/oauth/refresh` rotation endpoint, old access-token revocation on refresh, single-use refresh-token invalidation, refreshed token metadata in API-key reads, docs quickstart coverage, and focused tests; refresh-token family risk detection and browser QA remain. |
| 197 - Developer OAuth refresh-family compromise detection | Partial | Fast backend/frontend checks; migration applied | OAuth refresh tokens now carry family lineage and parent-token references; replay of a rotated refresh token marks the reused token, revokes every active token in the family, clears refresh hashes, exposes compromise timestamps to operators, and has focused tests for old-token replay invalidating the replacement access token; richer incident automation and browser QA remain. |
| 198 - Developer OAuth replay incident automation | Partial | Fast backend checks; migration authored | Replay of a rotated developer OAuth refresh token now creates one deduplicated high-severity tenant security incident, preserves application/family/key evidence, records the immediate revocation action, and links compromised token-family notes to the incident; notification delivery, richer SOC dashboards, and browser QA remain. |
| 199 - SDK event route coverage | Partial | Fast backend checks | Tenant developer API keys can now list and create events through `/api/v1/sdk/events` with event-scope enforcement, team validation, active `events.created` webhook fanout, catalog updates, SDK entry-point metadata, public docs quickstart coverage, and TypeScript SDK wrappers; broader SDK write coverage, SDK package publishing/generated clients, and browser QA remain. |
| 200 - Developer webhook retry backoff | Partial | Fast backend/frontend checks; migration authored | Developer webhook deliveries now persist last-attempt and next-attempt timestamps, failed deliveries receive exponential retry scheduling, retry-due runs skip future-backoff deliveries, recorded/delivered attempts clear retry scheduling, and the console shows the next retry time; scheduled background workers, external webhook smoke tests, and browser QA remain. |
| 201 - Developer webhook retry worker | Partial | Fast backend checks | Developer webhook retry scheduling now has a deployment-ready CLI worker that processes due queued/failed deliveries across all tenants or one tenant, reuses the shared retry engine, honors max-attempt/limit/include-recorded controls, and emits JSON run summaries for cron, Temporal activities, or container job logs; external webhook smoke tests, scheduler deployment wiring, and browser QA remain. |
| 202 - Agent task execution worker | Partial | Fast backend checks | Queued AI agent tasks now have a deployment-ready CLI worker that executes due tenant tasks outside the console, reuses the same deterministic/webhook execution engine as the API, preserves hash-chained run ledger records, supports tenant/limit scoping, and emits JSON run summaries for cron, Temporal activities, or container job logs; live provider worker smoke tests, scheduling deployment wiring, and browser QA remain. |
| 203 - Unified due-worker runner | Partial | Fast backend checks | AfroLete now has one deployment-ready due-worker command that can run all scheduler-ready lanes or target agent-task/developer-webhook lanes individually, combines tenant scoping and lane limits, and emits aggregate JSON summaries for cron, Temporal activities, or container job logs; production scheduler manifests, live provider smoke tests, and browser QA remain. |
| 204 - Systemd due-worker scheduling | Partial | Unit-file parse checks; fast backend command checks | The unified due-worker now has PJS-style systemd service and timer units that reuse the backend OpenBao-rendered environment file, run the Python worker every minute by default, expose batch/retry knobs, and document installation/override commands; live host installation, journal smoke tests, Temporal replacement workflows, and browser QA remain. |
| 205 - TypeScript SDK package | Partial | SDK typecheck; Python compile check | AfroLete now has a repository TypeScript SDK package at `packages/sdk-typescript` with a dependency-free typed client for `/api/v1/sdk/me`, organization reads, event list/create, and training drill list/create; public developer docs now catalog the TypeScript SDK as a repository package and include a typed quickstart; package publishing, generated clients from OpenAPI, Python SDK implementation, and browser QA remain. |
| 206 - SDK team directory route | Partial | SDK typecheck; Python compile check | Developer API keys can now list tenant teams through `/api/v1/sdk/teams` with `read:organization` or `read:teams` scope, the developer scope/catalog metadata advertises team and read-training scopes, and the TypeScript SDK exposes `client.teams.list`; roster write APIs, broader SDK route coverage, browser QA, and full backend tests remain. |
| 207 - Python SDK package | Partial | Python compile check | AfroLete now has a repository Python SDK package at `packages/sdk-python` with a standard-library client for `/api/v1/sdk/me`, organization reads, team listing, event list/create, and training drill list/create; public developer docs now catalog the Python SDK as a repository package and include a Python quickstart; package publishing, generated clients from OpenAPI, richer typed models, and browser QA remain. |
| 208 - SDK team creation route | Partial | Focused compile/type checks | Developer API keys with `write:teams` can now create tenant teams through `/api/v1/sdk/teams`, preserving the organization-member-team authorization relationship, advertising the write scope in the developer catalog, and exposing `client.teams.create` in both TypeScript and Python SDKs; roster mutation APIs, generated SDK clients, browser QA, and full backend tests remain. |
| 209 - SDK roster assignment route | Partial | Focused compile/type checks | Developer API keys with `write:roster` or `write:teams` can now assign existing people to team rosters through `/api/v1/sdk/teams/{team_id}/members`, creating athlete profiles when needed, preserving team-person authorization relationships, and exposing roster assignment helpers in both SDKs; guardian-aware minor onboarding, generated SDK clients, browser QA, and full backend tests remain. |
| 210 - SDK person creation route | Partial | Focused compile/type checks | Developer API keys with `write:people` or `write:roster` can now create or reuse tenant person records through `/api/v1/sdk/people`, attach organization memberships with role/title metadata, and use `client.people.create` in both TypeScript and Python SDKs before team creation or roster assignment; guardian-aware minor onboarding, duplicate-resolution policy controls, generated SDK clients, browser QA, and full backend tests remain. |
| 211 - SDK guardian link route | Partial | Focused compile/type checks | Developer API keys with `write:guardians` or `write:roster` can now link parents/guardians to athlete person records through `/api/v1/sdk/people/{athlete_person_id}/guardians`, create or reuse guardian people, attach guardian organization membership, preserve athlete-guardian authorization relationships, and use guardian-link helpers in both SDKs; consent-request automation, duplicate-resolution policy controls, generated SDK clients, browser QA, and full backend tests remain. |
| 212 - SDK consent request route | Partial | Focused compile/type checks | Developer API keys with `write:consent`, `write:guardians`, or `write:roster` can now create one-use guardian consent requests through `/api/v1/sdk/people/{athlete_person_id}/consent-requests`, reusing guardian consent authority checks, organization linkage validation, destination resolution, signed token hashing, and SDK helpers in TypeScript and Python; provider delivery automation, consent reminder scheduling, generated SDK clients, browser QA, and full backend tests remain. |
| 213 - SDK performance observation intake | Partial | Focused compile/type checks | Developer API keys with `read:performance` can list tenant metric definitions and observations, while keys with `write:performance` can submit provider-sourced athlete observations through `/api/v1/sdk/performance/athletes/{athlete_profile_id}/observations` with tenant/athlete/metric/event validation, nullable machine recorder provenance, active `performance.observation.created` webhook fanout, and TypeScript/Python SDK helpers; generated SDK clients, browser QA, model extraction benchmark coverage, and full backend tests remain. |
| 214 - Performance cohort benchmarks | Partial | Focused backend/frontend checks | Performance APIs now compute tenant/sport cohort benchmarks from the latest non-rejected observation per athlete and metric, returning cohort average/min/max, athlete value, directional delta, percentile rank, cohort rank, benchmark band, and coaching recommendation through organization and athlete benchmark routes; the operations console loads and displays selected-athlete benchmark cards beside observations; age/position/regional cohorts, longitudinal trend forecasting, browser QA, and full backend tests remain. |
| 215 - Athlete performance trends | Partial | Focused backend/frontend checks | Athlete performance APIs now return metric-level trend analytics from accepted observation history, including first/latest/previous/best values, average, directional change from previous and first values, consistency index, next-value forecast, trend direction, season-period filtering, and coaching recommendation; the operations console loads selected-athlete trend cards alongside benchmarks and observations; richer charts, model-backed forecasts, browser QA, and full backend tests remain. |
| 216 - Performance goals and achievements | Partial | Focused backend/frontend checks; migration authored | Athlete performance now supports persisted metric goals, target/baseline/current tracking, direction-aware achieved status, achievement evaluation, generated goal-achieved awards, personal-best awards, badge codes, award history, and operations-console controls/cards for creating goals and scanning achievements; family/player-facing celebration surfaces, social sharing, richer badge design, browser QA, and full backend tests remain. |
| 217 - Family performance celebration surface | Partial | Focused backend/frontend checks | The family portal now has a guardian-scoped performance endpoint and UI section that exposes linked athletes' active goals, achieved goals, awards, badge codes, personal-best/goal achievement summaries, and headline goal/award counts without leaking unrelated athletes; richer visual badge design, social sharing, player self-service surfaces, browser QA, and full backend tests remain. |
| 218 - Scheduled performance achievement scans | Partial | Focused backend/worker checks | The unified due-worker now includes a `performance-achievements` lane that scans active performance goals and recent accepted observations, updates goal current values, creates goal-achieved awards and personal-best awards without operator clicks, exposes a `--performance-limit` knob, and keeps JSON worker summaries compatible with cron, Temporal, and systemd runs; live scheduler smoke, notification fanout, browser QA, and full backend tests remain. |
| 219 - Performance achievement notifications | Partial | Focused backend tests | Newly created performance awards now generate a system-authored in-app report message for the athlete and consent-authority guardians, reusing the communications inbox/recipient model so manual and scheduled achievement scans can surface recognition without another operator broadcast step; push/email fanout, richer badge visuals, browser QA, and full backend tests remain. |
| 220 - Player performance self-service portal | Partial | Focused backend/frontend checks | Athlete-linked accounts can now load their own organization performance profile through `/performance/my-profiles`, including ALS summary, recent observations, goals, awards, filtered trends, trend-series history, baseline/what-if forecast scenarios, injury-risk safety signals, cohort comparisons, and cohort benchmarks; the new `/player` portal presents that data in a responsive self-service dashboard with local identity controls; richer charts, browser QA, and full backend tests remain. |
| 221 - Player self-assessment submission | Partial | Focused backend/frontend checks; migration authored | Athlete-linked accounts can now submit their own pending-review self-assessments through `/performance/my-profiles/{athlete_profile_id}/self-assessments`, including physical/technical/tactical/mental scores, RPE, effort rating, notes, and computed ALS; `/player` now includes a self-assessment form that refreshes the player's profile after submission; browser QA and full backend tests remain. |
| 222 - Coach review for player self-assessments | Partial | Focused backend/frontend checks | Coaches can now review athlete assessments through `/performance/assessments/{assessment_id}/review`, promote pending player self-assessments to verified evidence, reject them, optionally correct score/RPE/effort fields, and use console Verify/Reject controls; current ALS now uses only verified assessments while pending submissions still count in assessment volume; richer review queues, browser QA, and full backend tests remain. |
| 223 - Performance assessment review queue | Partial | Focused backend/frontend checks | Coaches now have an organization-wide pending assessment queue at `/performance/assessments/review-queue`, including athlete names and assessment payloads, and the operations console surfaces pending player self-assessments before the selected-athlete cards with Verify/Reject controls; queue filters, assignment/SLA controls, browser QA, and full backend tests remain. |
| 224 - Performance review assignment and SLA controls | Partial | Focused backend/frontend checks; migration authored | Assessment review queues now persist reviewer assignment, due dates, priority, review notes, reviewer identity, and reviewed timestamps; `/performance/assessments/review-queue` supports assignment/SLA/priority filters, `/performance/assessments/{assessment_id}/review-assignment` lets coaches claim and triage submissions, and the operations console exposes queue filters plus Assign me/Due 24h/Urgent controls; browser QA, richer reviewer workload policies, and full backend tests remain. |
| 225 - Performance review escalation automation | Partial | Focused backend/frontend/worker checks; migration authored | Overdue and due-soon assessment reviews now have persisted escalation metadata, in-app alert generation for performance reviewers and assigned coaches, manager-triggered `/performance/assessments/review-escalations`, a unified due-worker `performance-review-escalations` lane with dry-run/repeat controls, and console escalation status cards; live scheduler deployment, provider delivery smoke, richer escalation policies, and browser QA remain. |
| 226 - Performance review queue analytics | Partial | Focused backend/frontend checks | Managers now have `/performance/assessments/review-summary` with organization-wide open, assigned/unassigned, SLA, priority, escalation, age, and reviewer workload breakdowns, and the operations console shows review-load and top-reviewer cards independent of the active queue filters; browser QA, workload trend charts, and full backend tests remain. |
| 227 - Performance visual analytics cards | Partial | Focused frontend checks | The operations console now renders visual performance dashboard cards for selected athletes, including ALS physical/technical/tactical/mental composition bars, trend runway bars comparing latest values with simple forecasts, and cohort percentile bars from benchmark data; browser visual QA, deeper dashboard interactions, and player-portal chart parity remain. |
| 228 - Player performance visual parity | Partial | Focused backend/frontend checks | Player performance profiles now include the latest verified assessment payload, and `/player` renders athlete-facing visual cards for ALS component composition, trend runway, cohort percentile standing, and goal progress; browser visual QA and model-backed forecasts remain. |
| 229 - Performance trend-series charts | Partial | Focused backend/frontend checks | Athlete performance now exposes accepted observation time-series through `/performance/athletes/{athlete_profile_id}/trend-series` and embeds trend-series payloads in player profiles; the operations console and player portal render compact history bars with point hover details, normalized direction-aware values, latest values, and forecasts; browser visual QA and deeper chart interactions remain. |
| 230 - Performance benchmark cohort scopes | Partial | Focused backend/frontend checks | Performance benchmarks now support tenant-wide, age-group, and roster-position cohort scopes with cohort labels, position filtering, age-group fallback from team labels or birth date buckets, player-profile benchmark scope selection, and operations-console benchmark scope controls; browser visual QA remains. |
| 231 - Regional performance benchmark cohorts | Partial | Focused backend/frontend checks | Performance benchmarks now support country/region cohort scopes using person `country_code`, member and athlete-intake creation can capture country codes for rostered athletes, and both the operations console and player portal expose country/region benchmark selectors; browser visual QA and richer cohort comparison controls remain. |
| 232 - Performance cohort comparison dashboard | Partial | Focused backend/frontend checks | Athlete performance now exposes `/performance/athletes/{athlete_profile_id}/cohort-comparisons` with tenant, age-group, position, region, local-association, and regional-association comparison summaries, embeds the same summaries in player profiles, and renders side-by-side comparison cards in the operations console and player portal; browser visual QA and deeper comparison controls remain. |
| 233 - Performance forecast scenarios | Partial | Focused backend/frontend checks | Athlete performance now exposes `/performance/athletes/{athlete_profile_id}/forecast-scenarios` with deterministic next-value projections, confidence, data-quality labels, risk levels, forecast ranges, four-step scenario runways, optional signed model-provider forecast overlays, and coaching recommendations from accepted observations; player profiles embed the same forecast scenarios, and both the operations console and player portal render forecast cards; provider credential smoke tests, injury-risk models, wearable/video forecast validation, browser visual QA, and richer what-if controls remain. |
| 234 - Performance what-if forecast controls | Partial | Focused backend/frontend checks | Coaches can now call `/performance/athletes/{athlete_profile_id}/forecast-scenarios/what-if` with training-load adjustment, readiness score, and horizon controls to simulate adjusted deterministic forecast runways, risk flags, confidence, recommendations, and optional signed model-provider overlays; the operations console exposes load/readiness controls and renders separate what-if cards beside baseline forecast scenarios; provider credential smoke tests, injury-risk models, browser visual QA, and wearable/video validation remain. |
| 235 - Performance injury risk prediction | Partial | Focused backend/frontend checks | Athlete performance now exposes `/performance/athletes/{athlete_profile_id}/injury-risk` with deterministic injury-risk scoring from athlete-specific training feedback, readiness, soreness, sleep quality, workload deltas, acute:chronic workload ratio, open injury/medical incidents, and declining metric trends; the operations console and player portal render injury-risk cards with drivers and recommendations; live model-provider risk inference, alert automation, browser visual QA, and clinical validation remain. |
| 236 - Performance injury risk alerts | Partial | Focused backend/frontend checks | High-risk athlete profiles can now trigger `/performance/athletes/{athlete_profile_id}/injury-risk/alerts`, which recomputes deterministic injury risk, checks a threshold, selects coaches/managers plus the athlete and consent-signing guardians, sends urgent alert messages, suppresses duplicate recent alert clicks, and exposes an operations-console Send risk alert control with delivery feedback; browser visual QA and live clinical/model validation remain. |
| 237 - Performance injury risk alert scans | Partial | Focused backend/frontend checks | Organization-wide injury-risk alert scans now expose `/performance/injury-risk/alert-scans` and a unified due-worker `performance-injury-risk-alerts` lane, scan athlete profiles by recent performance/readiness evidence, threshold deterministic injury risk, send urgent alerts, report high-risk/skipped/failed counts, and apply repeat-after duplicate suppression; cron/systemd scheduling in deployment, browser visual QA, and clinical/model validation remain. |
| 238 - Performance injury risk alert fanout | Partial | Focused backend/frontend checks | Manual and scan-based injury-risk alerts now accept repeated `channels=` values and can fan out urgent alert messages across in-app, push, SMS, WhatsApp, email, and Telegram using the existing communications recipient/destination model; the operations console exposes in-app/push/SMS/WhatsApp channel toggles and run feedback reports channel/message counts; live provider credentials, webhook delivery smoke tests, browser visual QA, and clinical/model validation remain. |
| 239 - Performance environmental injury-risk signals | Partial | Focused backend/frontend checks | Injury-risk scoring now uses linked training-session event weather assessments and venue facility surfaces, adding critical/warning/advisory weather penalties, hazardous-surface markers, environmental risk counts, weather decisions, surface driver text, and operations-console weather/surface bars; live model-provider inference, browser visual QA, and clinical validation remain. |
| 240 - Performance wearable biomarker injury-risk signals | Partial | Focused backend/frontend checks | Injury-risk scoring now consumes accepted wearable performance observations for HRV, resting heart rate, sleep duration/quality, recovery/readiness scores, hydration, strain, stress, and body temperature; the risk response includes wearable observation counts, biomarker risk counts, latest HRV/resting-HR/recovery/hydration values, wearable risk labels, v4 model-policy output, and operations-console biomarker/recovery bars; live wearable provider sync credentials, richer device-specific schemas, browser visual QA, and clinical validation remain. |
| 241 - Performance provider evidence parsers | Partial | Focused backend/frontend checks | Performance ingestion now parses structured provider payloads from wearable/stat feeds by matching metric code/name/single-metric payloads, extracts metric-specific values from video/audio/text narratives instead of blindly using the first number, preserves provider timestamps, can apply deterministic or webhook-backed model assistance to weak narrative parses, returns parser/model method/confidence reason/warnings/parsed fields, and surfaces parser plus benchmark metadata in the operations console; live provider credentialed sync, live model smoke tests, browser visual QA, and full verification remain. |
| 242 - Performance wearable provider schema normalization | Partial | Focused backend/frontend checks | Performance ingestion now accepts explicit or inferred source-provider metadata, normalizes WHOOP recovery/strain, Garmin wellness, Apple Health/HealthKit samples, Fitbit summary/sleep shapes, Polar heart-rate/HRV/sleep summaries, Oura readiness/sleep summaries, and Catapult/STATSports/Playertek-style GPS workload payloads into metric candidates, maps common aliases for wellness and GPS workload metrics, and exposes a provider selector in the operations console; credentialed OAuth/provider sync, browser visual QA, and full verification remain. |
| 243 - Performance wearable webhook ingestion | Partial | Focused backend/frontend checks; migration authored | Wearable providers can now POST server-to-server evidence into `/performance/webhooks/wearables` with optional OpenBao/env-backed HMAC timestamp signatures, provider/external-event replay protection, persisted ingest-event audit rows, payload hashes, pending-review observation creation for matched metrics only, replay-safe idempotent responses, and an operations-console webhook simulation control; live provider smoke tests and browser visual QA remain. |
| 244 - Performance wearable provider connections and sync runs | Partial | Focused backend/frontend checks; migration authored | Operators can now persist athlete-scoped wearable provider connections with provider, external athlete reference, OAuth/OpenBao secret paths, scopes, webhook-registration state, sync cursor, default metric mappings, token expiry metadata, and provider pull URLs; connection sync runs can ingest sample provider payloads or pull live provider JSON through bearer-token auth, persist run status/counts/messages/provider HTTP audit hashes, list connection history, and expose Connect/Sync/Pull controls in the operations console; browser visual QA and live provider smoke tests remain. |
| 245 - Performance wearable OAuth authorization controls | Partial | Focused backend/frontend checks; migration authored | Wearable provider connections now support OAuth authorization start/callback flows with client ID, authorization/token URLs, redirect URI, scoped authorization URL generation, single-use state hashing, state expiry, callback validation, default OpenBao token-secret paths, token-expiry metadata, authorization timestamps, and operations-console OAuth/Callback controls; browser visual QA and live provider smoke tests remain. |
| 246 - Performance wearable OAuth token lifecycle | Partial | Focused backend/frontend checks; migration authored | OAuth callbacks can now accept provider token responses or perform OpenBao-backed token endpoint exchange, hash access/refresh tokens without persisting raw token material, record token type/scope/expiry/family metadata, rotate refresh-token fingerprints, refresh tokens through a dedicated API, and expose Refresh controls plus secret-safe token lifecycle status in the operations console; browser visual QA and live provider smoke tests remain. |
| 247 - Performance wearable provider pull adapters | Partial | Focused backend/frontend checks; migration authored | Wearable sync now supports configured provider pull URLs with OpenBao/env-resolved bearer tokens, athlete/cursor/since/until query parameters, provider response hashing, HTTP status auditing, next-cursor persistence, replay-safe ingestion through the existing wearable normalizer, focused live-adapter unit coverage, and operations-console Pull controls; browser visual QA and live provider smoke tests remain. |
| 248 - Performance wearable pull pagination and rate limits | Partial | Focused backend/frontend checks; migration authored | Provider pull syncs now follow provider cursors across bounded pages, accept per-run max-page limits, hash aggregate page responses, persist page counts, gracefully record HTTP 429 rate limits with retry-after seconds instead of failing the route, and surface page/rate-limit status in the operations console; browser visual QA and live provider smoke tests remain. |
| 249 - Performance wearable pull retry worker | Partial | Focused backend/worker checks | The unified due-worker now includes a `wearable-pull-retries` lane that finds latest due rate-limited wearable sync runs, honors provider retry-after timing, retries through the same provider-pull sync boundary, reports eligible/retried/rate-limited counts, exposes max-page/default-backoff CLI knobs, and keeps JSON summaries compatible with cron/systemd orchestration; live scheduler smoke, browser visual QA, and live provider smoke tests remain. |
| 250 - Performance wearable webhook registration | Partial | Focused backend/frontend checks; migration authored | Wearable connections can now store provider webhook registration endpoints, callback URLs, event subscriptions, provider HTTP status, payload hashes, registration timestamps, and errors; managers can trigger a provider-neutral registration API that resolves the access token at the secret boundary, POSTs registration payloads or prepares provider-console payloads, updates connection registration state, and exposes Register controls/status in the operations console; live provider registration smoke tests and browser visual QA remain. |
| 251 - Performance wearable provider backoff policies | Partial | Focused backend/worker checks | Wearable pull retry workers now support provider-keyed retry-after and max-page policy maps from `AFROLETE_PERFORMANCE_WEARABLE_PROVIDER_RETRY_AFTER_SECONDS`, `AFROLETE_PERFORMANCE_WEARABLE_PROVIDER_MAX_PAGES`, and worker CLI overrides, apply those policies when providers omit `Retry-After`, clamp retry page counts per provider, and include applied policy metadata in worker JSON summaries; live provider smoke tests and browser visual QA remain. |
| 252 - Performance wearable provider schema expansion | Partial | Focused backend/frontend checks | Wearable ingestion now normalizes Polar heart-rate/HRV/sleep summaries, Oura readiness/sleep summaries, and Catapult/STATSports/Playertek GPS workload metrics such as player load, total distance, max speed, high-speed distance, accelerations, decelerations, and average heart rate; the operations console provider selector and sample webhook payloads now include these providers; live provider smoke tests and browser visual QA remain. |
| 253 - Performance model-assisted extraction boundary | Partial | Focused backend/frontend checks | Performance ingestion now has a governed model extraction boundary with deterministic local mode, optional signed webhook mode, model policy/configuration fields, model confidence and evaluation metadata in API responses, and operations-console visibility; model assistance only replaces weak parser fallbacks and preserves precise structured or metric-specific parser matches; live model-provider smoke tests and browser visual QA remain. |
| 254 - Performance model extraction benchmark suites | Partial | Focused backend/frontend checks | Operators can now run `/performance/model-extraction/benchmarks` to evaluate parser and model-assisted extraction against default, supplied, or saved benchmark cases without creating observations, returning model policy, pass/fail counts, accuracy, mean absolute error, per-case parser methods, confidence, and summaries; the operations console includes Dataset and Benchmark actions plus result cards; live model-provider smoke tests and browser visual QA remain. |
| 255 - Performance model benchmark dataset registry | Partial | Focused backend/frontend checks; migration authored | Operators can now persist tenant-scoped model extraction benchmark datasets and cases, list reusable datasets, run benchmark suites by dataset id, and update latest accuracy, mean absolute error, run time, and case-count metadata after each run; the operations console can create a production narrative-extraction baseline, reuse it for Benchmark runs, and show dataset status/cases; live model-provider smoke tests and browser visual QA remain. |
| 256 - Performance association-region cohort benchmarks | Partial | Focused backend/frontend checks | Performance benchmark contexts now derive local and regional association cohorts from the existing tenant graph: teams or organizations registered as members of local/regional associations group athletes into subnational comparison cohorts, `/performance/athletes/{athlete_profile_id}/benchmarks` accepts `local_association` and `regional_association`, cohort comparisons include those scopes only when association-backed data exists, and the operations console exposes both selectors; browser visual QA remains. |
| 257 - Performance trend period filters | Partial | Focused backend/frontend checks | Athlete trend and trend-series endpoints now accept `period_start`/`period_end` date filters, reject invalid ranges, include selected period metadata in responses, and let the operations console filter trend cards and history bars to season windows or recent development blocks; browser visual QA remains. |
| 258 - Performance trend domain filters | Partial | Focused backend/frontend checks | Athlete trend and trend-series endpoints now accept metric `category` and `metric_code` filters, return selected filter metadata, and let the operations console focus trend cards and history bars on a performance domain or single metric code; browser visual QA and deeper chart interactions remain. |
| 259 - Player performance trend controls | Partial | Focused backend/frontend checks | `/performance/my-profiles` now accepts player-facing trend category, metric-code, and date-window filters, reuses the same metric analytics semantics as coach views, and the `/player` portal persists controls for benchmark cohort plus focused trend dashboards; browser visual QA and deeper player chart interactions remain. |
| 260 - Player performance what-if forecasts | Partial | Focused backend/frontend checks | Player performance profiles now include deterministic what-if forecast scenarios, `/performance/my-profiles` accepts player-facing load/readiness/horizon controls, what-if scenarios honor the selected metric domain/code, and `/player` renders persisted what-if cards/runways beside baseline forecasts; browser visual QA with loaded profile data, provider credential smoke tests, and deeper chart interactions remain. |
| 261 - Player injury-risk safety cards | Partial | Focused backend/frontend checks | Player performance profiles now embed the deterministic injury-risk read model, including score, risk band, confidence, readiness/load context, drivers, and recommendation; `/player` renders safety summary cards, driver bars, and a top-level Safety insight beside momentum and benchmark focus; browser visual QA with seeded profile data, clinical validation, and live model-provider risk inference remain. |
| 262 - Performance biomechanical video risk signals | Partial | Focused backend/frontend checks | Injury-risk scoring now consumes verified video-analysis, agent-extracted, and coach-evaluation movement observations for asymmetry, landing mechanics, movement quality, gait, and cutting/deceleration control; risk responses expose biomechanical observation counts, risk labels, movement/asymmetry latest values, v4 model-policy output, and operations/player movement risk bars; live computer-vision provider validation, deeper sport-specific thresholds, browser visual QA, and clinical validation remain. |
| 263 - Performance forecast model-provider boundary | Partial | Focused backend/frontend checks | Forecast scenarios and what-if runways now support an optional signed model-provider webhook overlay with HMAC headers, deterministic baseline payloads, bounded response validation, model-policy surfacing, confidence/data-quality/risk overrides, and operations/player model-policy display; live provider credential smoke tests, forecast benchmark datasets, browser visual QA, and provider drift monitoring remain. |
| 264 - Performance forecast validation runs | Partial | Focused backend/frontend checks; migration authored | Operators can now persist forecast validation runs that hold out each athlete metric's latest accepted observation, replay deterministic forecast logic from the prior history, compute absolute/relative error, pass/drift counts, aggregate drift levels, recommendations, and per-metric details, then list/run Forecast QA from the operations console; live provider drift calibration, browser visual QA, and full regression remain. |
| 265 - Scheduled performance forecast validation | Partial | Focused backend worker checks | The unified due-worker now includes a `performance-forecast-validations` lane that can run forecast drift validation across one organization or all organizations with accepted performance observations, aggregate metric/evaluated/drift/watch/high counts, and emit JSON summaries compatible with cron, Temporal, and systemd runners; live scheduler deployment, provider drift calibration, browser visual QA, and full regression remain. |
| 266 - Performance forecast drift alerts | Partial | Focused backend/frontend checks | Watch/high forecast validation runs can now fan out manager alerts across in-app, push, SMS, WhatsApp, email, or Telegram using the existing communications recipient/destination model, suppress duplicate recent alerts, include drift metrics and outlier details, and expose Drift alert controls/results in the operations console; live provider delivery credentials, browser visual QA, and full regression remain. |
| 267 - Scheduled performance forecast drift alerts | Partial | Focused backend worker checks | The unified due-worker can now opt into scheduled forecast-drift auto-alerting after validation runs, choose alert channels, enforce repeat-after suppression, dry-run the policy, and emit alert sent/skipped/message-id/reason summaries for cron, Temporal, and systemd observability; live scheduler deployment, live provider delivery credentials, browser visual QA, and full regression remain. |
| 268 - Due-worker production scheduler policy | Partial | Unit-file parse checks; worker CLI help check | The PJS systemd due-worker manifest now wires the current scheduler-ready lanes with explicit environment controls for forecast validation, forecast drift auto-alert repeat/channel policy, performance review escalations, injury-risk alert scans, wearable pull retries, generic performance throughput, and webhook retry attempts; infra/backend deployment docs now include install, override, and dry-smoke guidance; live host installation, journal smoke tests, multi-channel provider credentials, Temporal replacement workflows, and browser QA remain. |
| 269 - Scheduled communication digest worker | Partial | Focused backend worker checks | The unified due-worker now includes a `communication-digests` lane that scans daily/weekly notification preferences, creates digest messages for unread non-digest inbox items without requiring a manager click, reports organization/message/created/skipped/failed counts, exposes CLI limit/frequency controls, and is wired into the PJS systemd service policy; live host journal smoke, provider delivery credentials, browser QA, and broader notification regression remain. |
| 270 - Scheduled travel consent reminder worker | Partial | Focused backend worker checks | The unified due-worker now includes an `event-travel-consent-reminders` lane that scans consent-required travel plans with approaching deadlines, sends guardian reminders through the existing communications pipeline, supports limit/horizon/channel/repeat-suppression/dry-run controls, reports event/message/due-plan/pending/recipient counts, and is wired into the PJS systemd policy; live host journal smoke, provider delivery credentials, per-request reminder history, browser QA, and broader event regression remain. |
| 271 - Docker demo stack | Complete | `docker compose build`; `docker compose up -d --force-recreate`; backend health/API smoke; frontend HTTP smoke; targeted backend tests; frontend typecheck | The repo now includes Dockerfiles for FastAPI and Next.js, a root `docker-compose.yml` with PostgreSQL, backend, frontend, and due-worker services, automatic backend migrations, local demo seed data for the default identity, local in-memory authz hydration for seeded memberships, and demo instructions in README/docs; browser walkthrough and hosted demo hardening remain. |
| 272 - Docker demo walkthrough hardening | Complete | Browser screenshot; backend 4xx/5xx log scan; targeted platform/assets tests; frontend typecheck; Docker rebuild/recreate smoke | Demo mode now reports intentionally absent Keycloak, SpiceDB, OpenBao, Redis, and Temporal services as standby/skipped instead of failures, creates local artifact directories on container startup, sends local identity headers through agent/developer console telemetry loaders, fixes empty-tenant asset summary revenue crashes, documents the port-3000 collision recovery path, and verifies the first screen loads Demo City FC without generic fetch or identity errors; deeper guided click-through and hosted demo hardening remain. |
| 273 - Public commercial support showcase | Complete | Focused organization/public-site test; frontend typecheck; Docker frontend build; public-site API/HTTP smoke; Playwright screenshot | Branded organization sites now expose public sponsor highlights, active fundraising campaigns, and match-day ticket products from the existing commercial workspace without leaking private sponsor contacts, the public site UI renders a support section with partner value, campaign progress, and ticket availability, and Next 16 client dynamic route params now resolve correctly for organization sites, AI scorecards, and consent-token pages; live checkout redirects, sponsor self-service login, and deeper browser journey QA remain. |
| 274 - Sponsor self-service portal | Complete | Focused commercial tests; frontend typecheck; backend lint | Sponsor contacts can now authenticate by their identity email to `/commercial/sponsor-portal` without organization-admin membership, see only sponsor records matching their contact email, review linked agreements, deliverables, activation notes, event exposure, sponsor invoices, outstanding balances, and public-site links in the new `/sponsors` portal; the Docker demo seed/docs include a sponsor contact, agreement, and invoice for local walkthroughs; full browser visual QA remains. |
| 275 - Sponsor invoice hosted checkout | Complete | Focused commercial tests; frontend typecheck; backend lint; Docker build/recreate; live API smoke; Playwright screenshot | Sponsor portal invoices now expose deterministic hosted payment sessions, the shared `/pay/sessions/[sessionId]` page can open commercial invoice checkouts with provider-neutral settlement intake, successful settlements create idempotent finance payments, mark invoices paid or partial, hide paid links from the sponsor portal, and update demo documentation for the sponsor payment walkthrough; live payment-provider redirects/webhooks and full commercial browser QA remain. |
| 276 - Commercial invoice payment webhooks | Complete | Focused commercial tests; backend lint | Commercial sponsor invoices now accept signed provider webhook callbacks at `/commercial/invoice-payment-webhooks`, normalize provider-neutral, Stripe, M-Pesa, and PayPal payload shapes into hosted checkout settlements, validate optional HMAC signatures from env or OpenBao-backed secret paths, reject stale or invalid signatures when configured, and avoid duplicate ledger application on provider replay; live provider credential replay and full commercial browser QA remain. |
| 277 - Commercial accounting sync | Complete | Focused commercial tests; frontend typecheck; backend lint | Commercial accounting exports can now be pushed through an authenticated `/commercial/accounting-export/sync` workflow with local record-only mode, webhook delivery mode, OpenBao/env-resolved webhook keys, signed accounting payload headers, deterministic sync references, console Sync controls, and result cards showing delivered or prepared state; live accounting-provider credential replay and full commercial browser QA remain. |
| 278 - Commercial tax authority filing | Complete | Focused commercial tests; frontend typecheck; backend lint | Commercial invoices can now be packaged by jurisdiction and filing period through authenticated `/commercial/tax-filing/deliver`, using due dates or creation dates for period inclusion, configurable tax rates/reverse-charge handling, local record-only mode, webhook delivery mode, OpenBao/env-resolved tax filing keys, signed tax filing payload headers, and console File tax controls/result cards; live tax-authority credential replay and full commercial browser QA remain. |
| 279 - Commercial settlement payout execution | Complete | Focused commercial tests; frontend typecheck; backend lint | Commercial settlement summaries can now be executed through an authenticated `/commercial/settlements/payout` workflow with local record-only mode, webhook delivery mode, OpenBao/env-resolved payout keys, signed payout payload headers, deterministic payout batch references, console Payout controls, and result cards showing delivered or prepared state; live payout-provider credential replay and full commercial browser QA remain. |
| 280 - Commercial payout callback reconciliation | Complete | Focused commercial tests; migration authored; frontend typecheck; backend lint | Commercial settlement payouts now persist provider, idempotency, delivery, amount, status, response, and callback audit fields; operators can list payout batches in the console, provider callbacks can reconcile by payout reference, batch reference, or idempotency key with optional HMAC signatures from env/OpenBao-backed keys, and the console can mark a payout paid for demo/provider replay workflows; live payout-provider sandbox replay and full commercial browser QA remain. |
| 281 - Commercial payment provider sessions | Complete | Focused commercial tests; frontend typecheck; backend lint | Sponsor invoice checkout sessions can now create provider-ready payment sessions from the hosted payment page, using local demo redirects by default or a signed webhook adapter with env/OpenBao-resolved session keys to request provider redirect URLs, session IDs, success/cancel URLs, customer email, and client references; live payment-provider sandbox redirect credentials and full commercial browser QA remain. |
| 282 - Commercial payment session audit | Complete | Focused commercial tests; migration authored; frontend typecheck; backend lint | Commercial payment-provider session creation now persists organization, invoice, sponsor, provider, local/provider session IDs, client reference, amount, currency, redirect URL, method, webhook status, provider response, and failure metadata, and exposes recent sessions in the operations console for replay/debugging; live payment-provider sandbox redirect/replay credentials and full commercial browser QA remain. |
| 283 - Agent task approval routing | Complete | Focused agent tests; migration authored; frontend typecheck; backend lint | Governed AI agent task outputs now support explicit approval requests with required approval counts, durable reviewer slots, approve/reject decisions, approval counters/status on task records, completion blocking while approvals are pending or rejected, automatic completion after required approvals, governance summary counts, hash-chained approval-request/decision run records, and operations-console approval controls; live model-provider smoke, multi-person identity assignment UX, and deeper governance workflows remain. |
| 284 - Family AI appeal PDF artifacts | Complete | Focused agent tests; frontend typecheck; backend lint | Family-visible AI recommendation appeal forms now render as either Markdown or PDF artifacts with checksums, byte sizes, content type metadata, and base64 PDF payloads, using the same lightweight PDF artifact renderer as public AI scorecards; the family portal exposes separate Markdown and PDF form downloads for human review and offline escalation; richer appeal packet branding and signed long-lived storage links remain. |
| 285 - Incident regulatory report exports | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Incident regulatory report packages now render Markdown and PDF artifacts with generated timestamps, download filenames, content types, checksums, byte sizes, and base64 PDF payloads from the current incident/package records; the operations console exposes Markdown/PDF download controls and artifact integrity metadata for compliance walkthroughs; richer branded layouts, electronic government portal submission, and insurer/medical portal integrations remain. |
| 286 - Scheduled compliance expiry reconciliation | Complete | Focused worker tests; backend lint; worker CLI help check | The unified due-worker now includes a `compliance-reconciliation` lane that scans organizations on a schedule, marks expired background checks, expires stale credentials, promotes near-due verified credentials to expiring-soon, emits JSON counts compatible with Docker, cron, Temporal, and systemd logs, and exposes systemd/Docker throughput controls; live host journal smoke, access-control automation, screening-provider risk feeds, and broader safeguarding regression remain. |
| 287 - Screening-provider background-check risk intake | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Background checks now accept provider-neutral signed screening result callbacks with timestamped HMAC validation from env or OpenBao-backed keys, locate checks by ID or provider reference, update status/risk/completion/expiry/summary fields, preserve provider result notes, and expose local provider replay controls in the operations console; provider-specific submission adapters and live provider replay remain. |
| 288 - Signed incident regulatory artifact links | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Incident regulatory report packages can now mint expiring signed Markdown/PDF links with HMAC signatures from env or OpenBao-backed keys, stable generation timestamps, checksum headers, public read routes, and operations-console Link controls; richer branded layouts, electronic government portal submission, and insurer/medical portal integrations remain. |
| 289 - Persisted incident regulatory artifact storage | Complete | Focused safeguarding tests; frontend typecheck; backend lint; Docker config/entrypoint parse checks | Incident regulatory Markdown/PDF package generation now writes artifacts to the shared local or MinIO-compatible object-storage abstraction under dedicated safeguarding artifact settings, signed public links serve stored bytes instead of regenerating documents, Docker demo startup creates the artifact directory, and the console exposes stored URLs/keys with wrapping-safe display; richer branded layouts, electronic government portal submission, and insurer/medical portal integrations remain. |
| 290 - Insurer claim provider adapter | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Incident insurance claims can now be sent to a provider-neutral insurer adapter in local record-only mode or signed webhook mode, payloads include incident/claim context, provider responses can update claim number, tracking URL, amounts, notes, and status, operators can poll provider status from the console, and communication logs preserve submission/status-sync events; provider-specific claim schemas and live insurer sandbox credentials remain. |
| 291 - Regulatory portal package submission | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Incident regulatory packages can now be filed through a provider-neutral regulator adapter in local record-only mode or signed webhook mode, payloads include incident context and stored artifact metadata, webhook responses can update portal references, status, accepted timestamps, and notes, and the console exposes a File action plus submission result card; provider-specific government portal schemas/live credentials, richer branded layouts, and insurer/medical portal integrations remain. |
| 292 - Medical clearance provider adapter | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Return-to-play medical clearances can now be sent to a provider-neutral medical portal adapter in local record-only mode or signed webhook mode, payloads include incident/athlete/clearance context, provider responses can update status, assessment timestamps, validity windows, restrictions, return-to-play stage, provider references, and notes, and the console exposes Send/Poll controls with sync result cards; provider-specific medical portal schemas/live credentials remain. |
| 293 - Safeguarding provider schema profiles | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Regulatory filings, insurer claim sync, and medical clearance portal sync now select provider-specific schema profiles in auto or configured mode, include schema IDs, required fields, status mappings, field maps, and provider-shaped payloads for local safeguarding offices, school sport authorities, SafeSport-style cases, federation discipline notices, medical/property/liability/travel claims, concussion return-to-play, physiotherapy, school medical, and standard clearance workflows; console sync cards show the profile/schema used; live portal credentials and provider replay remain. |
| 294 - Screening provider submission adapter | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Background checks can now be submitted to screening providers through local record-only mode or signed webhook mode, automatically move to in-progress, preserve deterministic provider references, include provider-specific schema IDs, required fields, status mappings, field maps, and provider-shaped payloads for SafeSport-style, government clearance, Checkr, First Advantage, youth-sport staff, and standard screening workflows, and expose Send controls plus submission result cards in the console; live screening provider credentials/replay remain. |
| 295 - Incident investigation action workflow | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Safeguarding incidents now support structured investigation actions for self-assignment, escalation, findings, follow-up, and closure, each updating incident status/severity/assignment/regulatory and medical follow-up flags as appropriate, appending timestamped investigation summaries to the case notes, and surfacing Assign me, Finding, Follow-up, Escalate, and Close controls plus action result cards in the operations console; dedicated investigation evidence files, approval policies, and access-control automation remain. |
| 296 - Incident investigation evidence files | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Safeguarding incidents now accept local or MinIO-compatible evidence uploads with sanitized filenames, byte sizes, SHA-256 checksums, content types, storage keys, evidence URLs, evidence type/review status metadata, and timestamped case-note audit entries; accepted evidence moves open cases into investigation, and the operations console exposes an Incident evidence file picker, Evidence controls, and upload result cards; signed evidence links and approval policies remain. |
| 297 - Signed incident evidence links | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Safeguarding incident evidence can now mint short-lived signed links with HMAC expiry validation, checksum binding, storage-key scoping to the incident, public evidence serving with checksum headers, and console Link/Open controls on the uploaded evidence card; richer evidence review queues and approval policies remain. |
| 298 - Incident evidence review queue | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Safeguarding evidence uploads now feed a manager-visible review queue derived from case-note audit entries, validate storage keys against incident object paths, expose accept/reject/escalate review actions with checksum verification, append review decisions back to case notes, escalate incident severity/regulatory flags when appropriate, and surface queue cards plus review controls in the operations console; approval policy automation remains. |
| 299 - Incident evidence approval policies | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Safeguarding evidence review now evaluates deterministic approval policies from incident type, severity, medical follow-up, regulatory flags, athlete linkage, and evidence type; high-risk evidence is blocked from direct acceptance until escalated, policy decisions are exposed through an API and queue payloads, review responses carry the resulting policy state, and the operations console surfaces policy status/rationale with a dedicated Policy control. |
| 300 - Safeguarding incident access-control automation | Complete | Focused safeguarding/authz tests; frontend typecheck; backend lint | Safeguarding incidents now have a dedicated SpiceDB resource definition with parent organization, event/team context, reporter, case manager, assignee, athlete, guardian, medical viewer, evidence reviewer, regulator, and agent relations; incident create/update/investigation/evidence flows automatically sync case relationships, evidence operations can authorize against incident-level permissions instead of only organization manage, local authz bootstrap hydrates incident relationships, and the operations console exposes an Access sync/status card. |
| 301 - Tenant-authored safeguarding evidence policy rules | Complete | Focused safeguarding tests; frontend typecheck; backend lint; Docker migration smoke | Organizations can now create, list, and disable safeguarding evidence policy rules with unique tenant rule codes, incident/severity/evidence/medical/regulatory/athlete criteria, required approval levels, risk promotion, recommended review statuses, and acceptance blocking; active rules are applied to evidence review queues, approval-policy API responses, and review actions with matched rule codes and rationale, while the operations console can create demo escalation rules and display the active tenant rule set. |
| 302 - Safeguarding incident access grants and revocation history | Complete | Focused safeguarding/authz tests; frontend typecheck; backend lint; Alembic head check | Safeguarding incident managers can now grant explicit case/evidence/medical/regulator/reporter/guardian relations to individual people, persist the grant and reason, apply active grants to SpiceDB/local authorization, revoke grants with revocation reason/timestamp/person history, delete revoked relationships from authorization, list grant history, and use operations-console Grant review/Revoke controls for temporary incident evidence access. |
| 303 - Configurable event attendance policies | Complete | Focused event/safeguarding tests; frontend typecheck; backend lint; Alembic head check | Events can now carry persisted attendance policies that select participation statuses and independently block, warn, or allow minor-consent, no-guardian, denied/expired consent, missing/not-cleared/expired medical clearance, and restricted medical-clearance conditions; attendance writes and listing payloads return policy code, decision, and warnings, while the operations console exposes an Attendance policy control and policy/warning cards for matchday operators. |
| 304 - Background-check evidence document review | Complete | Focused safeguarding tests; frontend typecheck; backend lint; Alembic head check | Background checks can now store uploaded screening evidence documents in local/MinIO-compatible object storage with sanitized filenames, checksums, provider references, durable review status, reviewer/timestamp fields, review notes, signed short-lived access links, public checksum-bound downloads, and console Evidence/Docs/Accept/Escalate/Link controls that update the screening decision and compliance summary; live screening provider credentials/replay remain. |
| 305 - Scheduled urgent communication escalation timers | Complete | Focused communications/worker tests; frontend typecheck; backend lint; Alembic head check | Urgent communications now carry parent-message escalation metadata, scheduled escalation APIs scan unresolved queued/failed/suppressed recipients after a configurable timer, create quiet-hours-override escalation messages on an alternate channel, suppress repeat escalations within a configured window, expose Timers controls/result cards in the operations console, and run from the unified due-worker plus Docker/systemd deployment wiring; live provider credentials/replay and browser QA remain. |
| 306 - Agent worker callback replay audit | Complete | Focused agent tests; frontend typecheck; backend lint; Alembic head check | External AI worker callbacks now persist provider external event IDs, callback payload hashes, and received timestamps in the hash-chained agent run ledger; duplicate provider event IDs are ignored even when replayed with a different idempotency key, ledger verification covers the new callback audit fields, and the operations console surfaces callback IDs/hashes on recent run cards; live model-provider smoke and deeper governance policy controls remain. |
| 307 - Tenant-authored AI governance policy rules | Complete | Focused agent tests; frontend typecheck; backend lint; Alembic head check | Organizations can now create/list/update active AI governance policy rules with agent-kind, task-type, model-policy, and input-reference matching; matched rules can require automatic human approval slots or block prohibited agent tasks before queueing, persist the matched policy decision/risk/rationale on tasks, record the decision in the hash-chained run ledger, and surface policy controls/rule cards/task policy badges in the operations console; live model-provider smoke, richer reviewer-assignment UX, and deeper policy simulation/reporting remain. |
| 308 - AI governance policy simulation and reporting | Complete | Focused agent tests; frontend typecheck; backend lint | Operators can now preview a proposed agent task against the active tenant AI policy set before queueing, including matched rule, decision, risk, blocking/approval outcome, rationale, and recommendation; governance policy reports summarize active/inactive rules, blocking/approval/allow mix, risk distribution, governed versus ungoverned recent tasks, and console coverage cards plus simulation results; live model-provider smoke, richer reviewer-assignment UX, policy history analytics, and deeper model governance remain. |
| 309 - AI governance policy history analytics | Complete | Focused agent tests; frontend typecheck; backend lint | AI governance now exposes policy history analytics derived from governed agent tasks, including daily buckets, policy-code usage, approval-gated/completed/in-review/failed counts, latest governed task context, recommendations, and operations-console history cards so tenants can monitor policy adoption over time; live model-provider smoke, richer reviewer-assignment UX, and deeper model governance remain. |
| 310 - AI bias-audit mitigation lifecycle | Complete | Focused agent/worker tests; frontend typecheck; backend lint; Alembic upgrade/downgrade | Agent bias audits now persist mitigation actions, evidence references, terminal actor/timestamp metadata, and an operator-managed mitigation state machine; mitigation updates count open and in-progress risks in ethical scorecards, release blocked model-registry records back into review when a terminal mitigation is recorded, and expose Mitigate/Close/Accept risk controls in the operations console. |
| 311 - AI governance policy history exports | Complete | Focused agent tests; frontend typecheck; backend lint | AI governance policy history can now be exported as checksumed CSV or Markdown artifacts, including summary counts, timeline buckets, policy usage, latest task context, and recommendations; the operations console exposes direct CSV/Markdown download controls for tenant operators preparing governance reviews. |
| 312 - AI governance policy history snapshot persistence | Complete | Focused agent tests; frontend typecheck; backend lint; Alembic upgrade/downgrade | Organizations can now persist AI governance policy history snapshots as tenant records with label, format, checksum, content, byte size, governed-task counts, policy counts, latest policy code, recommendation, generator, and generated timestamp; managers can list recent snapshots and download saved CSV/Markdown artifacts from the operations console. |
| 313 - AI model governance evidence exports | Complete | Focused agent/worker tests; frontend typecheck; backend lint | Registered AI model policies can now produce portable Markdown/CSV governance evidence packets with registry metadata, use case, evaluation/limitations/bias notes, run counts, review/failed run counts, bias audit and mitigation counts, decision appeal counts, latest ledger hash, checksum, and console Evidence MD/CSV download controls. |
| 314 - Training calendar export | Complete | Focused training tests; frontend typecheck; backend lint | Training sessions can now be exported as checksumed iCalendar artifacts over an organization/team/date range, including stable UIDs, UTC start/end times, status, load score, RPE target, objectives, and calendar metadata; the operations console exposes a Calendar download control so coaches can sync planned sessions into external calendars. |
| 315 - Family command dashboard | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Guardian accounts now have a server-side family dashboard summary with linked-child counts, pending consent counts, unread and urgent message counts, upcoming events, RSVP needs, clearance blockers, active goals, awards, linked AI recommendation counts, open AI appeal counts, next-event timing, and prioritized family action cards; `/family` now surfaces those command cards above the detailed inbox, consent, schedule, performance, and AI appeal sections. |
| 316 - Family schedule conflict detection | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Family dashboards now detect overlapping linked-child commitments across upcoming events, return conflict windows with athlete names, event titles, event IDs, and coordination recommendations, count schedule conflicts separately, and surface conflict cards/action items in `/family` so parents can spot pickup, carpool, or rescheduling problems before matchday. |
| 317 - Guardian portal account readiness | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Tenant operators can now review guardian portal onboarding readiness through `/safeguarding/guardian-account-readiness`, which maps guardian relationships to linked AppUser/Keycloak subjects, detects invite-ready contacts, pending email-based account links, phone-only records, email conflicts, and missing contact data, and surfaces family-account readiness cards in the safeguarding console so clubs can complete authenticated family onboarding. |
| 318 - Family portal invitation workflow | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Tenant operators can now send family portal invitations from guardian readiness cards through `/safeguarding/guardian-account-readiness/{relationship_id}/invite`, creating auditable communication messages and recipients with channel destination, portal URL, account-linking instructions, message IDs, and delivery status; the operations console queues invites and shows the latest invitation result. |
| 319 - Family portal invite dispatch status | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Guardian portal invites can now opt into immediate delivery dispatch through the existing communications adapter boundary, returning attempted/sent/delivered/queued/failed/suppressed counts alongside recipient delivery status; the safeguarding console sends invites with dispatch enabled and shows the delivery outcome so operators can distinguish delivered in-app invites from queued provider-backed email/SMS/WhatsApp messages. |
| 320 - Guardian invite audit on readiness cards | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Guardian account readiness now derives the latest family portal invitation from communication messages and recipients, returning last invite message ID, channel, destination, delivery status, created timestamp, and sent timestamp; safeguarding readiness cards show the prior invite state and switch to an Invite again action so operators can avoid blind duplicate onboarding nudges. |
| 321 - Bulk family portal onboarding invites | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Tenant operators can now run `/safeguarding/guardian-account-readiness/invite-batch` to invite all portal-ready guardians in one pass, dispatch through the communications pipeline, skip recently invited guardians, skip linked accounts unless explicitly included, report missing destinations and not-ready records, aggregate dispatch counts, and trigger the workflow from the safeguarding console with a batch result card. |
| 322 - Scheduled family portal invite reminders | Complete | Focused worker tests; backend lint; syntax check | The unified due-worker now includes a `family-portal-invite-reminders` lane that scans older family portal invitation messages, skips guardians already linked to AppUser accounts, suppresses repeat reminders inside a configurable window, creates communication reminder messages for unlinked guardians through selectable channels, supports dry-run mode, and reports eligible/executed/reminded/skipped/failed counts for cron/systemd scheduling. |
| 323 - Family portal invite deep links | Complete | Focused safeguarding tests; frontend typecheck; backend lint | Family portal invitations now generate deep links carrying organization, relationship, guardian subject, email, and display-name hints; `/family` consumes those hints to prefill the local/demo identity, auto-load the invited workspace, and exercise the identity bridge so first guardian access links the signed-in AppUser to the existing guardian person while preserving server-side identity checks. |
| 324 - Communication delivery readiness | Complete | Focused communications tests; frontend typecheck; backend lint | Communications now expose `/communications/delivery-readiness`, a secret-safe channel-by-channel readiness report that distinguishes in-app delivery, record-only mode, configured channel/shared webhooks, missing webhook URLs, OpenBao/env key source, missing delivery keys, and live-ready versus dispatch-ready status; the operations console shows live provider coverage and blocked channels before operators rely on email/SMS/WhatsApp/Telegram/push dispatch. |
| 325 - Provider delivery callback normalization | Complete | Focused communications tests; frontend typecheck; backend lint | Provider callbacks can now post to `/communications/delivery-events/provider` with Twilio/SendGrid/WhatsApp/Telegram/push-style status strings, provider event/message IDs, raw payload metadata, and the same shared delivery key guard; AfroLete normalizes provider statuses into queued/sent/delivered/read/failed/suppressed recipient states, records delivery/read timestamps, preserves failure reasons, rejects channel mismatches, and exposes typed response payloads for replay/sandbox tests. |
| 326 - Scheduled communication dispatch worker | Complete | Focused worker and communications tests; frontend typecheck; backend lint | Scheduled communication messages now have a `communication-scheduled-dispatch` due-worker lane that finds due `scheduled` messages, dispatches them through the existing in-app/webhook/record-only delivery adapter path, marks messages sent with `sent_at`, reports eligible/executed/dispatched/skipped/failed counts, supports dry-run mode, contributes to worker summaries, and leaves future scheduled messages untouched. |
| 327 - Scheduled communication console controls | Complete | Focused communications route test; frontend typecheck; backend lint | Operators can now choose a scheduled send time in the Communications composer, create `scheduled` messages without immediate send timestamps, see scheduled/sent status and due times in the message list and delivery summary, manually run due scheduled dispatches from the console through a protected `/communications/scheduled-dispatch/run` API, and run the same due-send lane from the Docker demo worker command. |
| 328 - Mobile emergency response console | Complete | Frontend typecheck; production build; Docker rebuild/live route smoke; mobile Playwright screenshot; backend log check | A dedicated `/emergency` mobile-first command surface now resolves a public organization slug or raw organization ID, loads emergency action plans and activations, creates a starter EAP when none exists, activates emergencies with location/responders, sends urgent organization alerts, escalates active responses, resolves activations with response-time capture, logs safeguarding incidents, links from the operations rail, and is documented in the Docker demo entry points. |
| 329 - Emergency escalation timer worker | Complete | Focused assets and worker tests; frontend typecheck; backend lint; Docker config check | Active emergency activations can now be automatically escalated after configurable unresolved/repeat timers through `/assets/emergency-escalations/run`, a unified due-worker `emergency-escalations` lane, Docker demo worker wiring, and a mobile emergency console Run timers control; each timer escalation raises the level up to five and appends an auditable communication-log entry. |
| 330 - Recurring SaaS invoice automation | Complete | Focused billing and worker tests; frontend typecheck; backend lint; Docker rebuild/live route smoke | Tenant subscriptions whose `next_billing_on` date is due can now generate open SaaS invoices through `/billing/recurring-invoices/run`, the unified due-worker `billing-recurring-invoices` lane, Docker/systemd scheduling knobs, and a billing-console Run billing control; the run suppresses duplicate period invoices, advances service periods, honors cancel-at-period-end, aggregates period usage overages, and reports invoice totals. |
| 331 - Scheduled SaaS dunning automation | Complete | Focused billing and worker tests; migration authored; frontend typecheck; backend lint | Overdue SaaS invoices now persist dunning count, last sent time, and last severity, and can be processed through `/billing/dunning/run`, the unified due-worker `billing-dunning` lane, Docker/systemd scheduling knobs, and a billing-console Run dunning control; the run suppresses repeats, prepares or delivers notices through the existing provider-neutral dunning adapter, records invoice dunning state, marks active/trial subscriptions past due, and reports severity/outstanding totals. |
| 332 - Automated SaaS late-fee application | Complete | Focused billing and worker tests; migration authored; frontend typecheck; backend lint; Docker config check | Overdue open/partial SaaS invoices now persist late-fee totals, counts, and repeat-suppression dates, and can be processed through `/billing/late-fees/run`, the unified due-worker `billing-late-fees` lane, Docker/systemd scheduling knobs, and a billing-console Run fees control; the run applies configurable fixed and percentage fees with caps, appends invoice audit notes, marks active/trial subscriptions past due, and reports applied totals. |
| 333 - Scheduled SaaS payment retry automation | Complete | Focused billing and worker tests; migration authored; frontend typecheck; backend lint; Docker config check | Overdue open/partial SaaS invoices now persist payment retry counts, last/next attempt timestamps, provider references, failure reasons, and status, and can be processed through `/billing/payment-retries/run`, the unified due-worker `billing-payment-retries` lane, Docker/systemd scheduling knobs, and a billing-console Retry pay control; the run supports record-only local preparation plus webhook provider handoff, repeat suppression, max-attempt policy, provider labels, collection application when providers return successful amounts, and subscription past-due marking. |
| 334 - Hosted SaaS invoice checkout | Complete | Focused billing checkout test; frontend typecheck; backend lint | SaaS invoices can now mint managed checkout links from the billing console, expose public hosted checkout payloads through `/billing/invoice-checkout-sessions/{session_id}`, settle from the shared `/pay/sessions/{session_id}?kind=saas` payment page, apply idempotent SaaS payments, cap overpayment to the open balance, and refresh invoice paid/partial status without requiring manager-only payment entry. |
| 335 - Subscription lifecycle controls | Complete | Focused billing lifecycle test; frontend typecheck; backend lint | Tenant subscriptions now expose a protected lifecycle API and billing-console controls for cancel-at-period-end, undo cancellation, pause, resume, and immediate cancellation semantics, reusing existing subscription status and cancellation fields, recording audit notes, restoring billing dates when reactivated, and keeping lifecycle state visible on subscription cards. |
| 336 - Subscription entitlement enforcement | Complete | Focused billing entitlement test; frontend typecheck; backend lint | Billing entitlements now have a protected enforcement run that reconciles purchased feature limits with subscription state, usage counts, cancellation, pause, and past-due conditions; it updates entitlement statuses to active, active-ending, over-limit, paused, past-due, or cancelled, reports blocked/over-limit/active counts, and exposes an operations-console Enforce control with a guard summary card. |
| 337 - Offline PWA app shell and replay outbox | Complete | Frontend typecheck; production build; diff check | The frontend now ships an installable web manifest, service-worker app-shell caching, online/offline console status, a local replayable mutation outbox, idempotency headers for queued writes, manual Replay controls, and offline queuing for selected field-critical organization/team/attendance/performance writes. |
| 338 - Athletics AI video coaching | Complete | Focused video-coaching backend test; backend lint; frontend typecheck | Coaches can now submit athletics video evidence through `/performance/athletes/{athlete_profile_id}/video-coaching` or the operations console; AfroLete creates/reuses video movement metrics, extracts or estimates stride/posture/ground-contact/arm-drive/rhythm scores, records pending-review video-analysis observations, drafts a pending-review assessment, and returns a coach-verifiable correction plan and next actions. |
| 339 - Pose/gait video benchmarking and annotation | Complete | Focused pose/gait backend test; backend lint; frontend typecheck; migration head check | Performance videos can now be uploaded to local/S3-compatible object storage, replayed in the console with slow-motion controls, annotated by coaches at exact timestamps/body regions, analyzed against world-class sprint pose/gait benchmark ranges, projected toward optimal movement targets, and converted into pending-review AI coaching observations and assessments. |
| 340 - Provider-neutral pose landmark ingestion | Complete | Focused pose/gait backend test; backend lint; frontend typecheck | Performance videos now accept stored MediaPipe/MoveNet-style normalized pose keypoint samples with source provider, frame/timestamp, phase, contact foot, stride index, and confidence metadata; pose/gait analysis derives torso lean, knee drive, ground contact, arm symmetry, and stride frequency from those landmarks before falling back to text evidence/templates, surfaces sample provenance in the API/UI, and keeps coach review required. |
| 341 - Movement reference profile library | Complete | Focused pose/gait backend test; backend lint; frontend typecheck | Tenant coaches can now create/list movement reference profiles for world-class performers or optimal movement models with sport, benchmark profile, performer/source/consent metadata, metric target ranges, and optional pose samples; pose/gait analysis can compare athlete video landmarks against a selected reference profile, returning reference provenance in API/UI outputs while preserving coach review boundaries. |
| 342 - MediaPipe video pose-processing worker | Complete | Focused pose-processing backend test; backend lint; frontend typecheck | Stored performance videos can now be decoded frame-by-frame by a MediaPipe/OpenCV worker, sampled with configurable frame limits and detection confidence, converted into normalized pose keypoints, written through the existing `/pose-samples` storage contract, optionally analyzed immediately, and run through a dedicated CLI or the unified Docker due-worker lane. |
| 343 - Object storage lifecycle policy | Complete | Storage/platform/worker tests; backend lint; Docker config check | Local and MinIO/S3-compatible object storage now have a configurable retention policy with local stale-file pruning, S3 bucket lifecycle XML configuration through the existing SigV4 adapter, secret-safe infrastructure status metadata, a dedicated lifecycle CLI, and a unified due-worker lane for scheduled cleanup. |
| 344 - Endpoint-posting video pose worker | Complete | Focused worker test; backend lint; Docker config check | The dedicated video pose worker and unified due-worker lane can now run in endpoint-ingest mode: they decode stored video bytes, run the configured MediaPipe/OpenCV pose extractor, post normalized keypoint batches to `/api/v1/performance/videos/{video_asset_id}/pose-samples` with Keycloak bearer or local demo headers, update video processing status/metadata, and keep Docker demo processing on the same API contract as external pose providers. |
| 345 - AI agent reviewer assignment queue | Complete | Focused agent workflow test; backend lint; frontend typecheck; migration authored | Agent tasks now persist review assignee, due date, priority, and assignment notes; managers can claim tasks, mark urgent work, inspect `/agents/tasks/review-queue` with assignment/SLA/priority filters, read `/agents/tasks/review-summary` queue counts, and use console queue cards plus Assign/Urgent/Done controls while assignment changes are recorded in the hash-chained agent run ledger. |
| 346 - AI review workload trend analytics | Complete | Focused agent workflow test; backend lint; frontend typecheck | Agent governance now exposes `/agents/tasks/review-trends` with rolling daily opened/completed/assigned/urgent/approval-pending buckets, reviewer workload breakdowns, overdue/urgent/open totals, and operational recommendations; the console shows review trend and reviewer-load cards so managers can rebalance AI-generated work before review queues stall. |
| 347 - AI outcome cohort comparisons | Complete | Focused agent workflow test; backend lint; frontend typecheck | Agent governance now exposes `/agents/outcome-cohorts` with cohort comparisons by task type, agent kind, model policy, policy code, risk level, or approval status; operators can compare completion, failure, review, approval-gate, rejection, appeal, age, and highest-variance outcomes in the console before model or workflow bias becomes invisible. |
| 348 - Distributed developer quota counters | Complete | Focused developer workflow tests; backend lint | SDK API-key quota enforcement can now use an optional Redis-backed minute counter keyed by tenant/API key/window, mirrors shared counts into durable API-key audit fields, keeps database counters as the local/demo fallback, reports the active counter mode from credential inspection, and documents the deployment knobs for multi-process SaaS API traffic. |
| 349 - SDK attendance recording coverage | Complete | Focused developer workflow test; backend lint; TypeScript SDK typecheck; Python SDK compile | Tenant developer API keys can now list and record event attendance through `/api/v1/sdk/events/{event_id}/attendance` with read/write attendance scopes, tenant isolation, attendance-policy-aware upserts, event participant relationship writes, TypeScript/Python SDK helpers, and SDK README examples for matchday kiosk imports. |
| 350 - SDK AI-agent task coverage | Complete | Focused developer workflow test; backend lint; TypeScript SDK typecheck; Python SDK compile | Tenant developer API keys can now list first-class AI agents, queue governed agent tasks, and list queued task status through `/api/v1/sdk/agents*` with read/write agent scopes, active `agents.task.queued` webhook cataloging, governance policy matching, approval-slot creation, null person-provenance for API-key-originated tasks, TypeScript/Python SDK helpers, and SDK README examples for trusted coaching and orchestration integrations. |
| 351 - SDK training workflow coverage | Complete | Focused developer workflow test; backend lint; TypeScript SDK typecheck; Python SDK compile | Tenant developer API keys can now create/list training plans, add/list plan items, create/list sessions, record/list post-session feedback, request availability suggestions, and export calendar artifacts through `/api/v1/sdk/training/*`, preserving tenant validation, machine provenance, TypeScript/Python SDK helpers, and active webhook catalog entries for plan/session/feedback events. |
| 352 - SDK communications workflow coverage | Complete | Focused developer workflow test; backend lint; TypeScript SDK typecheck; Python SDK compile | Tenant developer API keys can now create/list communication templates, create/list scoped messages, inspect message recipients, and dispatch messages through `/api/v1/sdk/communications/*`, preserving tenant recipient expansion, null human provenance for API-key messages, TypeScript/Python SDK helpers, and active webhook catalog entries for message-created and dispatch events. |
| 353 - SDK billing usage coverage | Complete | Focused developer workflow test; backend lint; TypeScript SDK typecheck; Python SDK compile | Tenant developer API keys can now list billing plans, subscriptions, usage meters, usage records, invoices, entitlements, and billing summaries, and can record tenant usage through `/api/v1/sdk/billing/*` with read/write billing scopes, tenant validation, TypeScript/Python SDK helpers, SDK README examples, and active `billing.usage.recorded` webhook cataloging. |
| 354 - Billing proration and tax filing verification | Complete | Focused billing tests; backend lint | SaaS billing verification now covers mid-period proration quotes and applied plan changes against changed plan identity, billing cycle, negotiated price, net charge/credit math, and audit notes; billing tax filing delivery now sends timestamped HMAC-signed provider webhook packages and is tested for invoice aggregation, partial-payment outstanding totals, destination/timeout handling, key headers, and signature integrity. |
| 355 - Competition tournament workflow verification | Complete | Focused competition tests; backend lint | Competition verification now covers single-elimination tournament setup, participant seeding, fixture conflict detection, final result protection, duplicate-safe winner advancement, fixture ticketing/event/product creation, ticketing listing, bracket visibility, schedule optimization with rest/venue constraints, and in-app broadcasts to rostered athletes plus guardians. |
| 356 - Live model training-plan generation boundary | Complete | Focused training tests; backend lint; frontend typecheck | AI training-plan generation can now run in deterministic local mode or signed live-model webhook mode with env/OpenBao key resolution, stable provider payloads containing readiness, assessment, observation, team, and competition context, provider-authenticated HMAC headers, model/provider metadata in API/UI responses, valid provider-authored plan and session-item persistence, and deterministic fallback when provider configuration or delivery fails. |
| 357 - Live model reporting insight boundary | Complete | Focused reporting tests; backend lint | Reporting insight generation can now run in deterministic local mode or signed live-model webhook mode with env/OpenBao key resolution, canonical reporting/risk context payloads, provider-authenticated HMAC headers, provider-authored title/severity/confidence/evidence/recommendation/model persistence, provider references in insight evidence, and deterministic fallback when provider configuration or delivery fails. |
| 358 - Environment-configured video pose worker | Complete | Focused worker tests; backend lint; diff check; CLI help smoke | The video pose worker and unified due-worker now read endpoint-ingest URL, Keycloak bearer token, local demo identity headers, and API timeout from `AFROLETE_PERFORMANCE_POSE_WORKER_*` settings while preserving CLI overrides, so container, systemd, cron, or Temporal deployments can run the OpenCV/MediaPipe decoder and post keypoints into `/pose-samples` without hardcoded command strings. |
| 359 - Asset accounting sync boundary | Complete | Focused asset accounting test; backend lint; frontend typecheck | Asset operations now produce balanced supplier procurement, supplier payable, equipment lease receivable/revenue, and lease payment accounting rows, expose `/assets/accounting-export`, and can deliver signed provider-neutral GL sync payloads through `AFROLETE_ASSET_ACCOUNTING_*` webhook settings with console Export GL/Sync GL controls. |
| 360 - Supplier adapter payload profiles | Complete | Focused asset adapter test; backend lint; frontend typecheck | Supplier order submission and supplier invoice sync now support explicit adapter profiles for teamwear, Decathlon-style club purchasing, local sports vendors, QuickBooks bills, Xero bills, Sage bills, and auto-detection, with adapter-specific payload sections surfaced in webhook deliveries and adapter labels returned to the console. |
| 361 - Docker-ready endpoint video pose worker | Complete | Focused worker test; backend lint; Docker compose config check | Endpoint-ingest pose processing now honors the disabled pose-provider setting before decoding frames, reports provider status in worker summaries, and the Docker demo mounts a shared local performance-video volume into both the FastAPI backend and due-worker containers so uploaded clips can be decoded by the worker and posted back through `/pose-samples`. |
| 362 - Searchable developer docs portal | Complete | Focused developer docs test; backend lint; frontend typecheck | `/api/v1/developers/public/docs` now accepts a search query and returns filtered quickstarts, scopes, webhook contracts, SDK entries, marketplace lanes, security requirements, normalized query metadata, and result counts; `/developers` exposes a responsive search control and no-results state so partners can find API/SDK/webhook guidance quickly. |
| 363 - SDK webhook verification helpers | Complete | Focused developer docs test; backend lint; TypeScript SDK typecheck/build; Python SDK compile/helper smoke; Node helper smoke | TypeScript and Python SDK packages now include timestamped HMAC-SHA256 webhook signature helpers, exported entry points, README examples, and public developer catalog entries so partner integrations can verify AfroLete webhook deliveries without reimplementing the signing contract. |
| 364 - Hosted developer OAuth consent page | Complete | Focused developer docs test; backend lint; frontend typecheck | `/developers/oauth/consent` now provides a tenant-manager review surface for third-party OAuth requests, accepts standard query parameters for organization, client, redirect, scopes, state, and PKCE challenge, displays scope descriptions from the public docs catalog, creates the existing OAuth authorization grant with local/Keycloak identity headers, and shows the one-time authorization code plus redirect URL. |
| 365 - Batched video pose endpoint worker | Complete | Focused worker test; backend lint | Endpoint-ingest video pose processing now splits decoded MediaPipe/OpenCV keypoint samples into `/pose-samples`-sized batches, preserves `replace_existing` only on the first batch, accumulates posted sample counts from endpoint responses, and reports posted batch totals in worker metadata so longer clips can be processed through the existing API contract without exceeding request limits. |
| 366 - SDK route manifest for generated clients | Complete | Focused developer docs test; backend lint; frontend typecheck | Public and tenant developer catalogs now expose a structured SDK endpoint manifest with method, path, product category, summary, accepted scopes, TypeScript/Python helper names, and emitted webhook events; the developer docs UI renders the manifest as a searchable generated-client contract, and backend tests assert the manifest stays in sync with the Raw HTTP SDK catalog. |
| 367 - Python SDK typed models | Complete | Python SDK compile/import smoke; diff check | The Python SDK now ships a PEP 561 `py.typed` marker and standard-library `TypedDict` contracts for current SDK request/response models across organization, people, guardians, consent, teams, events, attendance, AI agents, communications, billing, training, and performance; client resource methods return and accept those named contracts, and the README shows typed imports through `afrolete_sdk.types`. |
| 368 - SDK release automation | Complete | SDK artifact verifier; Python compile; backend ruff; diff check | TypeScript and Python SDK releases now share a repository verifier that builds the TypeScript package, packs the npm tarball, builds the Python wheel/source distribution, asserts release metadata and typed artifacts, preserves durable artifacts under `dist/sdk-release`, documents release verification in the root and package READMEs, and wires a GitHub Actions SDK Release workflow for tag/manual verification plus token-gated npm/PyPI publishing. |
| 369 - Generated SDK endpoint manifests | Complete | Generator check; focused developer docs test; SDK release verifier; TypeScript SDK build; Python SDK compile/import; backend ruff; diff check | The backend developer SDK catalog now generates checked TypeScript and Python endpoint manifests shipped inside both SDK packages, exports `AFROLETE_SDK_ENDPOINTS` for integration tooling, verifies drift through a generator `--check` mode and backend test, and extends the release verifier so npm/Python artifacts prove the generated endpoint files are included before publishing. |
| 370 - Travel manifest service-worker prefetch | Complete | Frontend typecheck; production build; diff check | Signed travel-manifest PDFs can now be minted and prefetched into a dedicated service-worker cache, preserving cache metadata in the operations console, retaining the encrypted structured-manifest cache, and giving field operators an Open cached control that loads the prefetched artifact from browser cache when network access is unreliable. |
| 371 - Dedicated video pose worker operations | Complete | Focused worker test; Docker Compose config check; backend lint | The MediaPipe/OpenCV video-processing worker can now target a specific stored video asset for controlled retries, exposes that selector through both the dedicated worker and unified due-worker CLI, and ships an optional Docker Compose `video-pose` profile that continuously decodes clips, runs pose extraction, and posts normalized keypoint batches into the existing `/pose-samples` endpoint. |
| 372 - Branded travel safety manifest PDFs | Complete | Focused event test; backend lint; frontend typecheck; diff check | Travel manifests now carry organization branding, contact, event, venue, route, vehicle, driver, risk, consent, and timing context into the API response; signed offline PDFs render as branded field documents with tenant colors, trip overview, route/safety sections, departure checklist, and participant medical/guardian details while the console surfaces the richer manifest metadata. |
| 373 - Competition transfer and eligibility certificates | Complete | Focused competition tests; backend lint; frontend typecheck; Alembic head check | Competition registrars can now record approved athlete transfers between teams, list transfer clearances, issue persisted eligibility certificates for a competition/team/athlete, run roster, team-registration, transfer, age, medical, compliance, quota, academic, citizenship, and disciplinary checks, surface blockers/warnings in the operations console, and keep the certificate history tied to tournament readiness. |
| 374 - Opposition scouting video reports | Complete | Focused performance test; backend lint; frontend typecheck; Alembic head check | Coaches can now upload opponent match videos scoped to an organization, team, competition, and event, persist the asset in the shared performance-video object store, generate deterministic tactical scout reports with formation detection, weaknesses, threats, recommendations, and set-piece notes, list recent scouting videos/reports, and operate the workflow from the performance console before match planning. |
| 375 - Volunteer management operations | Complete | Focused volunteer test; backend lint; frontend typecheck/build; Alembic head check; diff check | Clubs, schools, and associations can now create volunteer profiles with roles, skills, availability, certification and background-check readiness, publish event/team-scoped volunteer opportunities, assign volunteers with match scoring plus overlap and full-slot protection, confirm/check in/check out shifts, record training completion, log service hours, award recognition badges, review coverage/training analytics, and operate the workflow from the operations console. |
| 376 - Public volunteer recruitment signup | Complete | Focused volunteer tests; backend lint; frontend typecheck/build; diff check | Branded organization sites can now list future open volunteer opportunities that are marked for public signup, show role timing, location, open slots, and required skills, and accept unauthenticated volunteer applications that create/reuse person records, create volunteer profiles with applied onboarding status, merge repeated skills/availability updates, create applied or waitlisted assignments, preserve manager open-slot counts until staff confirm assignments, and surface applications in the volunteer operations console. |
| 377 - Corporate volunteer group programs | Complete | Focused volunteer tests; backend lint; frontend typecheck/build; Alembic upgrade/head check; diff check | Company and employer volunteer groups can now apply from branded public organization sites for a specific public opportunity with coordinator contacts, group size, requested slots, shared skills, availability, and message context; the backend persists duplicate-safe group applications, managers can list and approve group requests from the volunteer console, approved group slots appear in volunteer analytics, and public group applications remain review-only until staff approve them. |
| 378 - Team volunteer needs and family obligations | Complete | Focused volunteer tests; backend lint; frontend typecheck/build; Alembic upgrade/head check; diff check | Coaches and team managers can now record team/event volunteer need requests, optionally convert them directly into schedulable opportunities, preserve requested role/count/skills/priority/needed-by context, and manage family volunteer service obligations with required/completed/waived/remaining hours, due dates, duplicate-safe seasonal upserts, manager updates, summary deficit analytics, and operations-console workflows. |
| 379 - Automated volunteer reminders | Complete | Focused volunteer/worker tests; backend lint; frontend typecheck/build; diff check | Volunteer operations can now run due reminders for unfilled event coverage, family service-hour obligations, and incomplete or expiring volunteer training, using the existing communications recipient/delivery pipeline with repeat suppression, in-app/email/SMS/WhatsApp/Telegram channel selection, due-worker lane support, manager-triggered APIs, operations-console run controls, and reminder result analytics. |
| 380 - Emergency volunteer substitute pools | Complete | Focused volunteer tests; backend lint; frontend typecheck/build; Alembic upgrade/head check; diff check | Volunteer coordinators can now register trusted volunteers into role/team-scoped substitute pools with availability, dispatch priority, monthly caps, status, and notes, rank eligible substitutes against urgent open opportunities using skills/readiness/reliability, dispatch emergency cover through the communications pipeline, create invited backup assignments, stamp last-contacted metadata, and operate the workflow from the console. |
| 381 - Self-service registration onboarding | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | AfroLete now exposes a public organization directory/search API, a protected self-service organization onboarding API that creates the club/school/academy workspace with the signed-in owner role, public-site path, dashboard path, owner identity, and launch checklist, plus a dedicated `/register` frontend page with Keycloak/local identity handling, club/school workspace creation, public organization search, team-aware player/family inquiry submission, and next-step onboarding guidance. |
| 382 - Registration packet readiness | Complete | Focused organization tests; backend lint; frontend typecheck/build; Alembic upgrade/head check; diff check | Public player/family registration inquiries can now be completed with an onboarding packet covering date of birth, emergency contacts, medical notes, privacy and guardian consent acknowledgements, required/submitted document metadata, payment amount/method/reference/status, packet submitted time, derived missing-document checks, consent/medical/emergency/payment readiness, ready-for-review verification state, and next-step guidance in `/register`; operator inquiry cards now show packet and payment readiness. |
| 383 - Stored registration document uploads | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Registration packets can now upload browser-selected document files for proof of age, medical information, guardian consent, and photo release through a public email-guarded route, store bytes through the shared local/S3 object-storage adapter under `registration-documents/`, preserve checksum/content-type/size/object URL metadata, merge stored document metadata into later packet submissions, and expose file-picker upload controls plus stored-document readiness in `/register`. |
| 384 - Keycloak-aware family portal onboarding | Complete | Frontend typecheck/build; diff check; production route smoke | Family portal invite links now preserve guardian organization/email/name context, local demo mode keeps editable identity controls, Keycloak mode completes PKCE callbacks, displays the active realm/client/session, starts sign-in with the invited guardian email as login hint, blocks family data loading until a Keycloak session exists, detects invite email mismatches, sends bearer-token requests without spoofed local headers, and keeps the identity bridge as the server-side account-link authority. |
| 385 - Registration conversion guardian onboarding | Complete | Focused organization tests; backend lint; frontend typecheck | Staff conversion of a ready public registration inquiry can now create the athlete person/profile, roster entry, guardian relationship, and an email/in-app/SMS-capable family portal invitation in one workflow, returning the invite message ID and durable portal URL with organization, relationship, guardian email/name, and login subject context; the operations console sends the current `/family` portal URL and logs conversion plus invite readiness. |
| 386 - Starter program onboarding | Complete | Focused organization tests; backend lint; frontend typecheck | Self-service club/school/academy onboarding now creates a first team or individual-sport program alongside the tenant, owner role, public site, and checklist, returning the starter team to the `/register` UI, counting it in public directory results, exposing it on the branded public site, and allowing the first family inquiry to target a real destination immediately. |
| 387 - Public registration payment continuity | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Branded public organization sites now continue past the basic family inquiry into a full registration packet with emergency details, medical notes, document uploads, consent acknowledgements, fee metadata, hosted registration payment links, and payment-session settlement through the shared `/pay/sessions` page; backend registration checkout endpoints generate deterministic payment links, expose hosted checkout summaries, record successful fee settlement back onto the inquiry, and move the packet into staff verification readiness. |
| 388 - Dedicated admissions workspace | Complete | Frontend typecheck/build; diff check | Staff now have a focused `/admissions` workspace with Keycloak/local identity handling, owned-organization selection, searchable admissions queues, packet/payment/readiness metrics, queue filters, review notes, follow-up scheduling, waitlist/reject/contact status controls, follow-up message dispatch, and one-click conversion into athlete/guardian onboarding using the existing registration inquiry APIs; the operations rail links directly to the admissions queue instead of forcing staff through the monolithic console. |
| 389 - Tenant registration settings | Complete | Focused organization tests; backend lint; frontend typecheck; Alembic upgrade/head check | Clubs, schools, and academies can now configure public registration open/closed state, required document types, registration fee amount/currency, and payment instructions during self-service onboarding; public organization sites display those requirements, new family inquiries inherit the tenant-controlled document and fee settings, closed registration blocks public inquiry creation, and `/register` plus branded public sites prefill packet payment state from organization policy instead of asking families to invent it. |
| 390 - Keycloak account creation entrypoints | Complete | Frontend typecheck/build; diff check | Owner onboarding, admissions staff, and guardian family-portal invite flows now offer direct Keycloak account creation alongside sign-in, using the same PKCE authorization-code callback handling, login hints for invited guardians and organization owners, bearer-token API calls after callback, and local/demo identity fallback. |
| 391 - Auth readiness surface | Complete | Platform tests; backend lint; frontend typecheck/build; diff check | The platform now exposes a secret-safe `/infrastructure/auth-readiness` contract that reports Keycloak/local auth mode, issuer, audience, expected OIDC discovery/authorization/registration/token/JWKS/logout/userinfo endpoints, blockers, warnings, and next actions; the operations console loads this alongside infrastructure probes, shows identity readiness in the operator panel, and offers account creation directly from the main command surface. |
| 392 - Authorization readiness surface | Complete | Platform tests; backend lint; frontend typecheck/build; diff check | The platform now exposes a secret-safe `/infrastructure/authorization-readiness` contract for local memory versus SpiceDB mode, endpoint posture, insecure transport warnings, missing-key blockers, expected authorization resources, relation counts, permission counts, and rollout actions; the main operations console loads this with infrastructure state and shows the SpiceDB authorization model beside identity readiness. |
| 393 - SpiceDB schema export and drift check | Complete | Platform tests; backend lint; frontend typecheck/build; diff check | The checked-in SpiceDB schema now matches the authorization readiness resource model for organizations, teams, events, athlete profiles, safeguarding incidents, agents, and developer applications; the platform exposes `/infrastructure/authorization-schema` with schema content, resource list, relation/permission counts, and SHA-256 checksum, and the console displays the schema hash beside authorization readiness for deployment drift checks. |
| 394 - Secret custody readiness surface | Complete | Platform tests; backend lint; frontend typecheck/build; diff check | The platform now exposes a secret-safe `/infrastructure/secrets-readiness` contract that classifies OpenBao runtime token, SpiceDB, communications, AI-agent, training, reporting, billing, commerce, travel, safeguarding, performance, object-storage, supplier, and asset-accounting secret classes as vault-path, inline, local-default, missing, or not-required without returning values or vault paths; the operations console loads this beside identity and authorization readiness so SaaS operators can see OpenBao adoption, inline fallbacks, missing required secrets, local defaults, blockers, warnings, and rollout actions. |
| 395 - Registration address conflict recovery | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Self-service club/school onboarding now has a public handle availability API for desired organization slug and subdomain, deterministic suggestions when a public address is taken, automatic generated-slug suffixing for repeated organization names, structured subdomain conflict errors, and `/register` controls that preflight the public address, show availability/suggestions, apply suggested subdomains, and recover from a 409 by filling the next usable address instead of leaving owners stuck. |
| 396 - Family account continuity after registration inquiry | Complete | Frontend typecheck/build; diff check | Public family registration now offers immediate account continuity after an inquiry is submitted: `/register` and branded `/site/[slug]` registration packets show the guardian email, expose Keycloak account creation and sign-in actions with the guardian email as login hint in production mode, and provide a family-portal deep link in local/demo mode so families are not forced to wait for staff conversion before creating or entering their account path. |
| 397 - Registration guardian contact binding | Complete | Focused organization tests; backend lint; frontend typecheck/build; Alembic upgrade/head check; diff check | Public registration inquiries now create or reuse a normalized guardian `Person` immediately, attach that contact to the inquiry, add an organization guardian membership, expose guardian contact readiness in API responses and family account cards, reuse the same contact for repeat inquiries and staff follow-up, and carry the contact through conversion into the final guardian relationship instead of delaying account continuity until staff acceptance. |
| 398 - Inquiry account readiness handoff | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Public registration inquiries now expose an email-guarded account-readiness endpoint that reports whether the guardian contact is invite-ready, linked, pending link, phone-only, missing contact, or needs account review without leaking app-user internals; `/register` and branded organization sites load that readiness after inquiry and packet updates, label the family account card accordingly, and disable account creation or sign-in actions when the backend says the handoff is not safe. |
| 399 - Family portal pending registrations | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Authenticated family accounts can now list their own pending and converted registration inquiries by linked guardian contact or verified email, including organization site, athlete, inquiry status, packet completeness, missing documents, payment state, and account handoff status; the family portal loads those rows beside family dashboard data and shows a pending player onboarding section so families that create an account immediately after inquiry submission are not dropped into an empty portal before staff conversion. |
| 400 - Family registration packet resume | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Families can now resume an exact public registration packet from the authenticated family portal: an email-guarded public packet read endpoint returns packet readiness without exposing it to other emails, family pending-registration cards link back to `/site/[slug]` with inquiry context, and branded public organization pages preload the saved inquiry, packet state, submitted document filenames, payment metadata, consent flags, and account readiness for direct continuation. |
| 401 - Admissions packet action context | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Registration inquiry responses now carry derived packet completeness, missing-document lists, and next-step guidance; the admissions workspace surfaces those details on inquiry cards, includes guardian contact status, and sends follow-up messages with missing-document context plus a direct packet-resume link so staff can move families toward verified packets instead of sending generic reminders. |
| 402 - Conversion readiness gate | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Staff conversion now refuses incomplete registration packets with structured missing-document and next-step details, and the admissions workspace disables conversion until derived packet readiness is complete so incomplete family inquiries cannot become athlete profiles, roster entries, or guardian links prematurely. |
| 403 - Admissions staff payment resolution | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Staff can now resolve registration payment blockers from admissions by setting payment status, method, and reference on an inquiry review; backend validation recomputes packet readiness from the shared packet summary so waivers, not-required, and paid states unlock conversion while pending, failed, and cancelled states keep conversion blocked; admissions forms expose those controls beside review notes. |
| 404 - Registration launch deep links | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Club and school onboarding responses now return direct launch paths for the public site, player registration, admissions review, family portal, and console; `/register` honors `mode=player&site=...` links by preloading the selected organization, and `/admissions` honors `organization_id` and queue links so newly created workspaces can move into family registration and staff review without manual navigation. |
| 405 - Family portal registration handoff | Complete | Frontend typecheck/build; local production smoke; diff check | Family portal links created after a public registration inquiry now carry the inquiry id, guardian identity, and an autoload flag; `/family` uses those parameters to open the workspace automatically, load pending registration state, and visually focus the exact onboarding packet so parents can continue without retyping organization details or hunting through cards. |
| 406 - Converted guardian invite focus | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Guardian invitations generated during admissions conversion now include the converted athlete id and autoload flag alongside the organization, relationship, and guardian identity; the family portal parses that athlete context and visually focuses the linked child card after loading so accepted registrations land parents directly in the relevant family workspace. |
| 407 - Admissions AI review tasking | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Registration inquiries can now queue a first-class AI agent review from admissions; the backend creates or reuses an organization-scoped Admissions Intake Agent, records a governed `registration_inquiry_review` task with packet/payment/readiness context, links it into the normal agent task list and run ledger, and the admissions UI exposes an AI Review action beside follow-up and conversion controls. |
| 408 - Keycloak registration return context | Complete | Frontend typecheck/build; diff check | Keycloak login and account creation now preserve same-origin return context through PKCE callbacks; family account creation from public site or `/register` returns to an exact `/register?mode=player&site=...&inquiry_id=...&email=...` handoff, reloads the selected club or school, restores the registration packet, payment, consent, and uploaded-document state, and refreshes account readiness so families do not restart after creating an account. |
| 409 - Family portal continuation after account link | Complete | Frontend typecheck/build; diff check | Registration account cards now detect when the signed-in Keycloak email matches the inquiry and replace redundant account buttons with a direct family-portal continuation link carrying organization, inquiry, guardian, athlete, and autoload context; public organization family links now carry the same focused inquiry context, and the family portal preserves invitation/autoload URLs through its own Keycloak login and account-creation round trips. |
| 410 - Hosted payment handoff from registration | Complete | Frontend typecheck/build; diff check | The dedicated `/register` player and family flow now uses the same registration payment-session endpoint as branded public sites, so families can save their packet, open a hosted checkout link immediately, see the generated registration reference and outstanding amount, and keep payment state synchronized with the inquiry instead of switching to another surface to pay. |
| 411 - Registration payment return loop | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Hosted registration checkout responses now include athlete, guardian, public registration resume, and focused family-portal continuation context; after a registration payment is recorded, the payment page offers direct return links to the exact packet and family workspace so payment confirmation completes the family registration loop instead of ending on a generic receipt. |
| 412 - Branded-site Keycloak registration continuity | Complete | Frontend typecheck/build; diff check | Branded organization registration pages now complete Keycloak callbacks in place, resume the exact inquiry packet from the returned `inquiry_id` and email, and replace account buttons with a focused family-portal continuation once the signed-in email matches the inquiry, preserving the organization's branded registration experience through account creation. |
| 413 - Admissions direct family handoff links | Complete | Frontend typecheck/build; diff check | Admissions inquiry cards now expose direct Open Packet and Family Portal actions that carry the organization, inquiry, guardian, athlete, and autoload context, letting staff inspect the same packet families see and jump into the focused family workspace without reconstructing URLs from follow-up emails. |
| 414 - Admissions AI review visibility | Complete | Frontend typecheck/build; diff check | Admissions now loads organization AI tasks alongside registration inquiries, matches `registration_inquiry_review` tasks back to their inquiry context, shows AI review status, approval counts, and governance rationale directly on each card, and updates the card immediately when staff queue an AI review so agents remain visible operational collaborators instead of background jobs. |
| 415 - Admissions inline AI execution | Complete | Frontend typecheck/build; diff check | Staff can now run queued or failed admissions AI reviews directly from the inquiry card; completed deterministic or webhook output notes and output references appear inline with the governance context so an AI review moves from a background task into a visible, actionable staff decision aid. |
| 416 - Admissions AI review closure | Complete | Frontend typecheck/build; diff check | Admissions staff can now stage AI output into inquiry review notes and complete waiting-for-review agent tasks from the same card, preserving governed AgentTask state while turning the AI recommendation into an auditable staff review note instead of leaving it as passive output. |
| 417 - Admissions AI review idempotency | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Registration inquiry AI review queueing now reuses the existing active review task for an inquiry instead of creating duplicates, while the admissions UI disables repeat queueing once a task exists and keeps staff on the single visible AI lifecycle for that family intake. |
| 418 - Admissions AI approval clarity | Complete | Frontend typecheck/build; diff check | Admissions AI review cards now show approval requirements, approved/rejected/pending counts, and an explicit blocker message when governance approvals are still required before staff can complete the AI review, making the agent governance state visible in the intake workflow. |
| 419 - Registration journey guidance | Complete | Frontend typecheck/build; local `/register` HTTP smoke; diff check | The dedicated registration surface now shows a state-driven journey rail for club/school workspace launch and player/family onboarding, deriving readiness from owner account, public address, registration rules, starter program, family inquiry, account handoff, packet, payment, and admissions states; organization name edits now keep public name and subdomain suggestions synchronized until the owner customizes them, reducing avoidable onboarding friction. |
| 420 - Onboarding concierge agent | Complete | Focused organization tests; frontend typecheck/build; diff check | Self-service club/school/academy onboarding now creates or reuses an organization-scoped Onboarding Concierge Agent, queues an auditable `organization_onboarding_concierge` task with launch goal, registration policy, and starter-program context, returns that task in the onboarding response, and surfaces the queued AI collaborator on the registration success panel so AI agents are present from workspace launch instead of only after admissions review. |
| 421 - Runnable onboarding concierge | Complete | Focused organization tests; backend lint; frontend build; diff check | The `/register` success panel can now execute the queued Onboarding Concierge Agent task in place, update the concierge card with waiting-for-review status, deterministic review notes, and output reference, and disable reruns once the draft is ready; backend coverage now proves the concierge task executes through the shared governed agent task endpoint. |
| 422 - Task-aware deterministic agent drafts | Complete | Agent and organization tests; backend lint; diff check | Local deterministic agent execution now produces domain-specific drafts for onboarding concierge and registration inquiry review tasks: launch drafts include registration-window, starter-program, admissions, checklist, payment/consent/communications actions, while admissions drafts call out packet, payment, verification, guardian handoff, and conversion review blockers instead of returning a generic task echo. |
| 423 - Onboarding concierge acceptance | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Owners can now accept the reviewed Onboarding Concierge launch plan from the `/register` success panel, preserving the draft notes, appending an owner-acceptance audit note, marking the governed AgentTask complete through the shared task update endpoint, and hiding the action once the concierge loop is closed. |
| 424 - Admissions CSV registration import | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Admissions staff can now paste CSV registration rows for bulk intake, create normal registration inquiries with guardian contact binding, tenant document/payment defaults, team-name or team-id assignment, import audit notes, and row-level validation errors; the admissions workspace shows created/error counts and refreshes the queue without bypassing the existing packet, payment, account handoff, and AI review workflow. |
| 425 - Registration import preview | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | The admissions CSV import now supports dry-run preview before execution, returning normalized valid rows with team assignment, payment defaults, required documents, and row-level errors without creating inquiries; the admissions UI exposes separate Preview and Import actions and shows preview rows so staff can fix spreadsheets before committing them into the admissions queue. |
| 426 - Registration import template | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Admissions staff can now load and download a tenant-specific registration import CSV template with supported headers and sample data seeded from the selected organization's sport and team names, then use the same template content for preview or import so spreadsheet-based onboarding starts from valid columns instead of trial-and-error header guessing. |
| 427 - Registration readiness cockpit | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | The registration surface now has a protected readiness summary that separates owner/admin/staff workspace readiness from guardian/family membership, reports auth mode, open registration, admissions queue, ready packets, family packet progress, and direct next-action links, and renders those checks inside `/register` as a live cockpit so user, club, school, admissions, and family onboarding state is visible instead of scattered across separate pages. |
| 428 - Guided onboarding missions | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | Registration readiness now includes role-aware onboarding missions for owners, staff, and families with status, progress percentage, XP, action links, and mission copy derived from real workspace, public intake, admissions, and packet state; `/register` renders the missions as a guided launch board so onboarding behaves more like an active learning path than a static form. |
| 429 - Context-aware learning path | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check | The registration onboarding flow now includes a role, goal, skill-level, and learning-style assessment backed by a deterministic learning-path API that returns modules, duration, practice tasks, badges, accessibility supports, and first action; `/register` renders the assessment and generated modules so new owners, coaches, administrators, families, and players get a personalized first-week path before or during workspace launch. |
| 430 - School and club onboarding templates | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check; production `/register` smoke | Registration onboarding now exposes deterministic school, club, and academy launch templates with audience-specific defaults for primary sport, launch goal, starter team/program, sport format, age group, season, required documents, payment instructions, and checklist items; `/register` loads and renders the matching templates so school and club owners can apply a real operating pattern instead of hand-building every first workspace field. |
| 431 - Family multi-child coordination board | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check; production `/family` smoke | The family portal now consumes a backend family-coordination API that synthesizes linked children, pending registrations, missing packet documents, consent requests, RSVP needs, clearance blockers, performance goals, and AI family recommendations into child-by-child coordination rows with urgency scores, per-child counters, next-action copy, and packet continuation links so guardians managing multiple athletes can see what to handle first and future agents/notifications can reuse the same ordering. |
| 432 - Family coordination digest | Complete | Focused organization tests; backend lint; frontend typecheck/build; diff check; production `/family` smoke | Guardians can now generate an in-app family coordination digest from the shared coordination API, producing a reminder message with the highest-priority child actions, portal deep link, delivery status, urgency score, and action counts; `/family` exposes a Send digest control and shows the resulting digest status so urgent registration, consent, RSVP, clearance, performance, and AI-review work can move from dashboard insight into a reusable communication workflow. |
| 433 - Scheduled family coordination digests | Complete | Worker tests; backend lint; diff check | The unified due-worker now includes a `family-coordination-digests` lane with CLI options for channel, portal URL, repeat window, limit, and dry-run mode; it finds linked guardian accounts with nonzero family coordination urgency, suppresses recent duplicate digests, creates in-app reminder messages from the shared coordination model, and reports eligible/executed/created/skipped/failed counts plus guardian and message IDs for operations automation. |
| 434 - Registration launch command center | Complete | Focused organization tests; backend lint; frontend typecheck; diff check | Club and school onboarding now produces a launch command center instead of a loose pile of links: backend APIs compute readiness score, launch status, public/admissions/family/dashboard links with QR payloads, channel-ready Email/SMS/WhatsApp/poster copy, funnel metrics, readiness checks, staff actions, and idempotent Registration Growth Agent tasks with deterministic campaign drafts; `/register` renders the command center after workspace creation so operators can immediately publish, track, and agent-review a registration campaign from the same surface. |
| 435 - Training command center | Complete | Focused training tests; backend lint; frontend typecheck/build; diff check | Coaches now get a training command center that aggregates drill library depth, active plans, upcoming sessions, feedback, readiness averages, load deltas, risk signals, readiness checks, and coach actions into one operational surface; protected APIs can queue an idempotent Training Strategy Agent task, deterministic execution produces a coach-review draft, and the main console renders readiness metrics, checks, and agent status beside the plan/session workflow. |
| 436 - Grant management pipeline | Complete | Focused commercial tests; backend lint; frontend typecheck; migration authored; diff check | Commercial operations now include grant opportunities, grant applications, funder reports, and a grant dashboard with requested/awarded/matching totals, upcoming deadlines, overdue-report pressure, readiness score, pipeline status, and recommendations; the main console can create a grant pipeline, schedule grant reports, and view grant cards alongside sponsorships, fundraising, tickets, invoices, tax, payouts, and accounting. |
| 437 - Merchandise store operations | Complete | Focused commercial tests; backend lint; frontend typecheck; migration authored; diff check | Clubs and schools now have a merchandise store workflow with products, SKUs, team scoping, inventory and reorder points, personalization flags, variant metadata, paid orders, order lines, inventory decrement, fulfillment updates, revenue/margin/store-readiness dashboard metrics, and console controls for creating products, selling personalized gear, and fulfilling pickup or shipment orders. |
| 438 - Community engagement hub | Complete | Focused community test; backend lint; frontend typecheck/build; migration upgrade/downgrade; diff check | Clubs, schools, and teams now have organization/team-scoped community posts, comments, idempotent reactions, fan polls, poll options, vote updates, engagement summary scoring, recommendations, and console controls for publishing a social wall, capturing supporter/family feedback, and measuring fan pulse. |

## Capability Coverage

Status values:

- `not-started` - no implementation yet.
- `foundation` - core model or boundary exists, but product workflow incomplete.
- `partial` - usable vertical exists, but not full scope.
- `complete` - implemented and verified against product requirements.

| Capability Area | Status | Notes |
| --- | --- | --- |
| Tenant organizations, clubs, schools, associations | partial | Polymorphic membership supports associations, clubs, schools, teams, and people in the tenant graph; organization branding/contact/subdomain fields now feed public branded site profiles and `/site/[slug]` pages with registration inquiry capture, tenant-configured registration requirements/fees, packet completion, hosted fee links, and commercial support showcases; self-service `/register` onboarding can create a club/school/academy workspace for the signed-in owner, first team/program, public site, registration settings, launch checklist, public-address availability recovery, and a launch command center with readiness score, share links, QR payloads, campaign copy, funnel metrics, and staff actions, while `/admissions` gives staff a dedicated review surface for accepted families. |
| Person identity and athlete profiles | partial | `Person`, `AppUser`, and `AthleteProfile` models added; Keycloak token claims provision `AppUser` identities and bind them to existing `Person` records by verified email before creating new people; `/register` now gives owners and families a visible self-service entrypoint with direct Keycloak account creation/sign-in while preserving local identity handling, public and shared registration packets now create/reuse normalized guardian contacts, attach them to inquiries, expose inquiry-scoped account-readiness handoff status, and offer guardian-email account creation/sign-in or family-portal deep links immediately after inquiry submission, and registration packets collect date of birth, emergency contacts, medical notes, stored document artifacts, consent acknowledgements, payment references, and verification readiness before staff conversion; conversion can now issue a family portal invite immediately after creating the athlete/profile/guardian records; guardian portal account readiness maps linked AppUser/Keycloak subjects, invite-ready contacts, pending email-based account links, email conflicts, and missing-contact records for family onboarding; family portal invite deep links prefill local/demo guardian identity, Keycloak mode offers direct account creation or sign-in with invite email hints and bearer-token loading, the family portal now shows pending registration inquiries before conversion and deep-links back into the exact public packet for continuation, and first-access account mapping is still verified through the identity bridge; SDK routes can create or reuse organization-scoped person records for integrations. |
| Teams, rosters, staff, guardians | partial | Team APIs and SDK routes support team sports and individual sports with captains, vice captains, starters, bench, substitutes, reserves, individual athletes, staff/support roles, roster assignment, guardian linking, and team committees; public site inquiries can now be submitted from branded public pages or `/register`, inherit organization-controlled document and fee policy, immediately bind a reusable guardian contact and organization guardian membership, completed as readiness-checked onboarding packets with uploaded documents and hosted fee payment links, reviewed in the dedicated admissions workspace, followed up, converted into athlete profiles, roster entries, guardian links, and guardian family-portal onboarding invites. |
| Events, schedules, attendance | partial | Event scheduling APIs, SDK event list/create routes, SDK attendance list/record routes, roster invitation seeding, attendance recording/listing, consent-aware check-in, configurable attendance policies, event-scoped volunteer staffing opportunities and assignments, event weather safety assessments, weather alert dispatch, travel planning/risk workflows, event travel consent generation, guardian travel consent reminders, scheduled travel consent reminder worker lane, travel safety manifests, trip fee invoice generation, travel approval workflows, travel inspection checklists, travel tracking updates, and travel expense/reimbursement tracking are implemented. |
| Volunteer management | partial | Volunteer profiles, skill/availability/certification/background-check readiness, public self-serve opportunity listing/signup on branded organization sites, duplicate-safe public applications, corporate/group volunteer program applications with manager approval, team/event opportunities, team needs assessment with optional opportunity conversion, family volunteer service obligations with remaining-hour analytics, automated reminders for coverage gaps/family hours/training, emergency substitute pools and dispatch, assignment matching with conflict and capacity checks, shift confirmation/check-in/check-out, training records, recognition badges, hours logging, coverage/training analytics, and operations-console workflows are implemented; background-check provider automation from volunteer profiles, group chat, and full browser QA remain. |
| Performance metrics and assessments | partial | Metric definitions, observations with provenance/confidence, ALS-style assessments, summaries, provider-neutral evidence ingestion, structured provider payload parsing, WHOOP/Garmin/Apple Health/Fitbit/Polar/Oura/Catapult/STATSports/Playertek schema normalization, signed wearable webhook ingestion with replay protection, wearable provider connection/OAuth/token lifecycle/provider-pull pagination/retry-worker/provider-specific backoff/webhook-registration/sync-run auditing, metric-specific narrative parsing, AI video coaching analysis for athletics clips, stored performance video assets, opposition scouting video uploads and tactical reports, MediaPipe/OpenCV video pose extraction worker, targeted retry selector, standalone Docker pose-worker profile, batched endpoint pose-sample posting, provider-neutral pose landmark sample ingestion, tenant-managed world-class/optimal movement reference profiles, slow-motion video review, timestamped human movement annotations, pose/gait benchmarking against selected references and sprint templates, landmark-derived torso/knee/ground-contact/arm-symmetry/stride-frequency metrics, optimal movement projections, model-assisted extraction metadata, model extraction benchmark suites, persisted benchmark dataset registry, persisted forecast validation/drift runs, scheduled forecast validation worker lane, scheduled performance video pose worker lane, multi-channel forecast drift manager alerts, scheduled forecast drift auto-alert policy, PJS systemd scheduler policy wiring for performance lanes, pending-review observations, human review/correction, player-submitted self-assessments with RPE/effort ratings, coach review queues with assignment/SLA/priority triage, automated escalation, workload analytics for player submissions, visual ALS/trend/benchmark/dashboard and player safety cards, side-by-side cohort comparison cards, trend-series history bars, deterministic and signed model-provider forecast scenario cards, coach/player-controlled what-if forecast runways, operations-console and player-portal injury-risk prediction cards, environmental weather/surface injury-risk signals, wearable biomarker injury-risk signals, biomechanical video injury-risk signals, urgent multi-channel injury-risk alert dispatch, organization-wide injury-risk scans, and duplicate alert suppression, verified-only current ALS, tenant/sport/age-group/position/country-region/local-association/regional-association cohort benchmark comparisons, athlete trend analytics with consistency, season-period/category/metric-code filtering for coach and player dashboards, simple forecasts, forecast ranges, confidence, risk levels, training-load adjustment, readiness-aware simulation, workload/readiness/soreness/sleep/open-incident risk scoring, persisted metric goals, achievement awards, personal-best recognition, scheduled achievement scans, in-app achievement notifications, family-visible goal/award summaries, player self-service performance profiles, SDK metric reads/observation intake, active observation webhook fanout, and console workflows are implemented. |
| AI-assisted ingestion and analysis | partial | Agent identity, assignment, task queue, tenant-authored AI governance policy rules, policy simulation/reporting/history analytics, policy history CSV/Markdown exports, persisted policy history snapshots, model governance Markdown/CSV evidence exports, automatic approval routing and task blocking from active governance policies, SDK-facing AI-agent discovery/task-queue/status routes for trusted integrations, deterministic/webhook task execution with signed worker payloads and signed inbound callbacks, durable callback external-event replay protection, callback payload hashes, queued agent task worker execution, unified due-worker lane support, systemd due-worker scheduling, OpenBao-backed agent webhook signing-key resolution, task review assignment/SLA/priority queues, workload trend analytics, cohort-level outcome comparisons, multi-approval routing for governed agent outputs, provider-neutral performance evidence ingestion, model-assisted performance extraction boundary, deterministic opposition scouting report generation, registration launch campaign agent tasks with readiness/funnel-aware deterministic drafts, Training Strategy Agent command-review tasks with load/readiness/risk-aware deterministic coach drafts, persisted hash-chained run records, ledger verification, governance summary metrics, credential-boundary status, model transparency reports, formal model registry records, registry approval controls, bias audit records with durable mitigation lifecycle tracking, decision appeals, ethical AI scorecards, public scorecard display, standalone public scorecard routes, public scorecard comment capture/listing/moderation, comment abuse scoring and auto-flagging, quarterly AI scorecard publication snapshots, readiness reminders, communications-backed publication reminder delivery, due-window reminder runs, persisted Markdown/PDF scorecard artifacts, signed scorecard artifact links, artifact access audit logs, requester attribution, artifact access analytics/trends/visualization, artifact anomaly detection, artifact anomaly notifications, worker-triggerable artifact alert runs, scheduler-ready scorecard automation orchestration, family AI appeal portal controls, family recommendation task discovery, downloadable Markdown/PDF family appeal forms, family-facing recommendation explanations, data summaries, alternative paths, governance notes, and console workflows are implemented; live provider smoke tests, live OpenBao smoke, live model-provider extraction smoke, and Temporal workflow replacement remain. |
| Training and coaching plans | partial | Drill library, scoped plans, weekly plan blocks, session load formula, AI-assisted plan generation from readiness/performance/competition context, signed live-model provider generation boundary with deterministic fallback, readiness check-ins, post-session feedback, load-delta recommendations, schedule availability suggestions, checksumed iCalendar training session exports, protected training command-center APIs, readiness/load/risk aggregation, idempotent Training Strategy Agent review tasks, SDK-facing plan/session/item/feedback/availability/calendar routes, and console workflows are implemented; full browser and live provider smoke verification remain. |
| Competition, fixtures, officials, tournaments | partial | Competition records, participant registration, fixtures/results, officials, match events, standings, round-robin fixture generation, bracket projections, source-round winner advancement, visual bracket lanes, schedule optimization, competition broadcasts, fixture ticketing, conflict detection, athlete transfer clearances, persisted eligibility certificates, registrar checks for roster/team-registration/transfer/age/medical/compliance/quota/academic/citizenship/disciplinary policies, opposition scouting videos and tactical reports for match planning, and console workflows are implemented and backend-verified for tournament advancement, ticketing, broadcasts, scheduling, transfers, and eligibility invariants; browser QA and broader live competition scenario verification remain. |
| Community and fan engagement | partial | Organization/team-scoped community posts, comments, reactions, fan polls, vote updates, engagement summary scoring, supporter/family feedback loops, branded public commercial support showcases, public fundraising/ticket highlights, and operations-console workflows are implemented; alumni networks, gamified supporter tiers, social media syndication, advanced moderation, and live fan-commerce journeys remain. |
| Communications and notifications | partial | Templates, scoped broadcasts, SDK-facing template/message/recipient/dispatch routes, recipient expansion, emergency activation alerts, weather assessment alerts, weather-triggered alert automation, urgent message escalation, scheduled escalation timers, scheduled message dispatch worker lane, scheduled send composer controls, protected due-dispatch console runs, travel consent reminders, scheduled travel consent reminder worker lane, travel departure/delay/arrival/return notifications, public inquiry follow-up messages, configurable email/SMS/WhatsApp/Telegram/push webhook dispatch, OpenBao-backed shared delivery webhook key resolution, delivery/read callback capture, provider callback status normalization, secret-safe channel delivery readiness reporting, person inbox, self-service family inbox portal, family command dashboard, guardian portal account readiness, family portal invitation workflow with dispatch-status reporting, readiness-card invite audit, bulk onboarding invites, scheduled family portal invite reminders, and family portal invite deep links, multi-child family schedule conflict detection, family athlete consent dashboard, family schedule/clearance view, family event RSVP, family portal consent responses, digest generation, scheduled digest worker lane, AI-assisted drafts, notification preferences, quiet-hours controls, emergency override, guardian copy for minors, and console workflows are implemented; live provider credentials/replay and browser QA remain. |
| Consent, safeguarding, compliance, incidents | partial | Guardian relationships, SDK guardian linking, SDK consent request creation, consent requests, one-use web links, SMS/WhatsApp/Telegram/email/manual consent capture, family portal consent responses, minor event clearance, configurable attendance policy gates, event travel consent generation and reminders, travel safety manifests with guardian and medical clearance context, background-check tracking with provider-specific screening submission adapters, provider-sourced screening risk/status intake, durable screening evidence document review, and signed background-check evidence access, compliance credential renewal tracking, derived compliance dashboards, manual and scheduled expiry reconciliation, blocker/renewal/investigation queues, structured incident investigation actions with evidence file uploads, signed evidence links, manager review queues, deterministic and tenant-authored evidence approval policies, incident-level access-control automation, and explicit incident access grants with revocation history, operator incident reporting/triage, developer OAuth replay security incidents, emergency activation incident generation, regulatory report packages with Markdown/PDF exports, persisted object storage, signed artifact links, provider-neutral regulatory submission with provider-specific schema profiles, incident insurance claim tracking with provider-neutral submission/status adapters and provider-specific schema profiles, return-to-play medical clearance with provider-neutral medical portal sync and provider-specific schema profiles, and medical-clearance-aware attendance gating are implemented; live screening provider credentials/replay, live government portal credentials/replay, live insurer credentials/replay, and live medical portal credentials/replay remain. |
| Equipment, facilities, assets | partial | Facility profiles, emergency action plans and activations, mobile emergency response console, emergency escalation timer worker, incident command roles, escalation matrices, external agency contacts, emergency alert dispatch from activations, emergency activation incident linkage, equipment inventory, checkout/return, maintenance work orders, booking overlap checks, asset readiness metrics, scan lookup, RFID scan intake/history, RFID reader provisioning and keyed gateway intake, photo metadata, equipment file upload/download through local or MinIO-compatible object storage with OpenBao-backed credential resolution, scheduled object lifecycle cleanup, procurement recommendations, supplier scorecards, supplier orders/receiving/submission/invoice sync with OpenBao-backed provider key resolution, supplier-specific adapter payload profiles, lease quotes, lease invoice creation, lease payment schedules/installments, lease payment reconciliation with partial installment allocation, balanced asset accounting exports, signed asset accounting webhook sync, utilization recommendations, and console workflows are implemented; automated provider delivery, live MinIO smoke, live device fleet rollout, and full verification remain. |
| Travel and logistics | partial | Event travel plans now cover trip itinerary, transport mode, route, driver/vehicle checks, digital inspection checklists with evidence uploads, manifests, CSV/text offline manifest exports, signed offline manifest links with branded PDF artifact rendering, tenant colors, safety checklist, trip context, and OpenBao-backed signing-key resolution, mobile manifest offline cache controls with encrypted expiring storage, service-worker prefetch for signed manifest artifacts, lodging, meals, equipment, emergency contacts, medical access, consent timing, guardian travel consent request generation, operator-triggered guardian reminders, scheduled travel consent reminders, safety manifests, cost estimates, trip fee invoice generation, hosted travel fee payment pages with provider-neutral settlement intake, signed travel fee payment webhooks with Stripe/M-Pesa/PayPal normalization and OpenBao-backed key resolution, travel fee payment reconciliation with derived exceptions, resolution actions, ledger drift repair, and currency mismatch repair, travel fee checkout links with processor-session metadata, approval workflow records, automated approval routing, provider-neutral GPS/location tracking updates, provider-neutral route map payload/rendering, authenticated NDJSON telemetry streaming, hardware GPS/provider ingest with replay protection and OpenBao-backed global/per-device ingest key resolution, nonce expiry policy, provider-specific idempotency windows, travel GPS device provisioning/health tracking, fleet-level GPS inventory, per-device ingest secret rotation with vault-reference custody metadata, departure/delay/arrival/return notifications, ad hoc and persistent geofence breach checks with saved-zone administration, provider-backed geofence polygon storage/checking, trip expense/reimbursement records with receipt uploads, bank/mobile-money payout adapter audit, and payout callback reconciliation with OpenBao-backed key resolution, carpool requests/offers, automatic carpool matching with optional coordinate scoring, post-trip driver ratings and summaries, backup driver rosters with automated dispatch, verified driver marketplace matching, departure readiness gating, deterministic route optimization, traffic/weather-aware reroute guidance, risk scoring, and status controls; live map provider routing, live geocoder enrichment, live traffic APIs, live weather-provider reroute geometry, live payment processor redirects, live OpenBao write/smoke, external driver marketplace inventory, long-lived push workers, and browser streaming/crypto QA remain. |
| Finance, sponsorship, fundraising, ticketing | partial | Sponsors, sponsorship agreements, fundraising campaigns, donations, grant opportunities, grant applications, grant funder reports, grant pipeline dashboards, merchandise products/SKUs, personalized merchandise orders, inventory decrement, fulfillment tracking, store revenue/margin dashboards, ticket products/orders/QR tickets/check-in, invoices, trip fee invoices, travel fee checkout links, hosted sponsor invoice checkout sessions, persisted signed commercial payment provider session creation, signed commercial invoice payment webhooks, travel expense reimbursement records with receipt evidence and payout execution, payments, settlement summaries, persisted signed commercial settlement payouts with callback reconciliation, refunds, tax quotes, signed commercial tax filing, accounting exports and signed accounting sync, sponsorship dashboards, commercial summary, public support showcases on branded organization sites, authenticated sponsor self-service dashboards, and console workflows are implemented; live payment-provider sandbox redirect/replay credentials, live payout-provider sandbox replay, live tax-authority replay credentials, live accounting-provider replay credentials, and full commercial browser QA remain. |
| Reports and intelligence | partial | Report definitions, generated reports, scheduled delivery, intelligence insights, predictive risk scores, export jobs, reporting summary, artifact rendering metadata, authenticated PDF/XLSX/CSV/API/HTML artifact download, local or MinIO-compatible artifact persistence with OpenBao-backed credential resolution, configurable object lifecycle policy, stored artifact URLs, short-lived signed artifact links with OpenBao-backed signing-key resolution, verification scoring, chart-ready summaries, visual chart cards, benchmark models, deterministic and signed live-model AI review insights, and console workflows are implemented; live model-provider smoke, live MinIO smoke, visual report QA, deeper chart interactions, and full verification remain. |
| Integrations and webhooks | partial | Keycloak OIDC bearer-token validation, frontend PKCE session handling, secret-safe auth readiness reporting for discovery/authorization/registration/token/JWKS endpoints, SpiceDB gRPC authorization adapter, secret-safe authorization readiness reporting for memory/SpiceDB mode, resource/relation/permission coverage, blockers and rollout actions, checked-in SpiceDB schema export with SHA-256 drift checks, shared OpenBao secret resolution with vault-path precedence, secret custody readiness classification for OpenBao/SpiceDB/webhook/signing/object-storage secret classes, secret-safe infrastructure readiness reporting, live dependency probe reporting, tenant developer applications, sandbox/production API keys with persisted quota enforcement, optional Redis-backed distributed API quota counters, hosted OAuth consent page, OAuth-style developer consent grants with PKCE public-client exchange, refresh-token rotation, refresh-family compromise detection, replay incident automation, SDK-facing API-key authorization for organization, people creation, guardian linking, consent request creation, team list/create, roster assignment, events, attendance recording, governed AI-agent task queueing/status, performance observation intake, training drills, training plan/session/feedback/availability/calendar workflows, communication template/message/dispatch workflows, and billing read/usage metering workflows, managed developer integration catalogs, searchable public developer docs, generated-client SDK endpoint manifests, TypeScript and Python SDK packages with typed request/response models, generated endpoint manifests, and webhook signature verification helpers, verified npm/Python release artifact automation, webhook subscriptions with persisted delivery fanout for drill/plan/session/feedback, communication message, and billing usage events, replay controls, retry-run controls with exponential backoff, retry worker CLI, unified due-worker lane support including emergency escalation timers, systemd due-worker scheduling, and marketplace listings are implemented; live service smoke tests, live registry publish credentials/smoke, OpenAPI method-generation polish, and other integrations remain future slices. |
| SaaS billing/subscriptions | partial | Billing plans, tenant subscriptions, lifecycle controls for cancellation/pause/resume, subscription-state-aware entitlement enforcement, usage meters/records, SDK-facing billing plan/subscription/usage-meter/usage-record/invoice/entitlement/summary routes with tenant-scoped usage recording, recurring SaaS invoice automation, due-worker scheduled billing, scheduled SaaS dunning automation with persisted repeat suppression, automated late-fee application with fixed/percentage/capped policies, scheduled payment retry automation with provider handoff and retry-state audit, hosted SaaS invoice checkout links and public settlement pages, SaaS invoices/payments, entitlements, billing summary, tax quotes, signed tax filing packages/delivery, proration quotes, plan-change application, dunning notice preparation and webhook delivery, signed payment webhook intake, OpenBao-backed billing provider key resolution, and console workflows are implemented; live provider/tax authority replay, billing browser QA, and remaining billing verification remain. |
| Beautiful operational UI/UX | partial | First screen is now an operational console with responsive infrastructure readiness, identity readiness, authorization readiness, secret custody readiness, tenant, roster, event, assets, commerce, reports, billing, developers, competition, community engagement, communications, attendance, performance, training, agent, and safeguarding workflows, including offline queue visibility, video-coaching controls, movement reference-profile creation/selection, slow-motion pose/gait video review, provider pose-data import, server-side video processing, landmark/reference provenance, and timestamped annotation controls; branded public organization pages, a dedicated self-service registration page, a dedicated admissions queue, family portal surfaces, sponsor portal surfaces, the player performance portal, and the mobile emergency response console are also implemented. |

## Next Actions

1. Run a live Keycloak realm redirect/token/API smoke test for `afrolete-web`
   against the deployed backend auth mode.
2. Run a live SpiceDB schema/write/check smoke test with the OpenBao-managed
   SpiceDB key.
3. Add live provider sandbox communication smoke tests, Keycloak-mode guardian
   account-linking smoke, and browser QA for family workflows.
4. Add MinIO-backed equipment storage, live RFID device fleet rollout, and full
   verification for assets and facilities.
5. Add live payment-provider sandbox redirect/replay credentials, live
   payout-provider sandbox replay, live tax-authority replay credentials,
   live accounting-provider replay credentials, and full commercial browser QA.
6. Add MinIO live smoke tests, visual report layout QA, deeper chart
   interactions, live AI-generated insights, and report verification coverage.
7. Add full billing verification coverage with provider replay tests,
   dunning/payment provider replay tests, and browser QA.
8. Add competition browser QA and broader live scenario verification for league operations, tournament operations, ticketing, broadcasts, and advancement flows.
9. Add full training browser QA and live provider smoke verification for model-generated plans.
10. Add provider webhook registration smoke tests, browser visual QA, live
   wearable provider smoke tests, live provider webhook registration smoke
   tests, and live model-provider extraction smoke tests.
11. Add live AI provider workers, live model-provider smoke tests, live
   OpenBao secret fetch smoke, and Temporal workflow replacement.
